---
phase: 06-embedder-provenance-capture
plan: 01
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements:
  - CAP-03
files_modified:
  - evaluation/harness/records.py
  - evaluation/harness/run.py
  - evaluation/harness/compare.py
  - evaluation/harness/freeze.py
  - tier-4-multimodal/scripts/eval_capture.py
  - tier-1-naive/embed_openai.py
  - tier-2-managed/main.py
  - tier-3-graph/rag.py
  - tier-4-multimodal/rag.py
  - tier-5-agentic/tools.py
  - evaluation/tests/test_eval_records.py
  - evaluation/tests/test_eval_run.py
  - evaluation/tests/test_eval_compare.py
  - evaluation/tests/test_eval_freeze.py
  - evaluation/tests/test_eval_tier4.py

max_lines:
  records_py: 95            # current 84; sketch +2 fields → 86; budget +9 LOC slack
  run_py: 420               # current 391; sketch +13 LOC (5 imports + 5 src/source assigns + 2 QueryLog kwargs + 2 amain getattrs) → 404; budget +16 slack
  compare_py: 435           # current 404; sketch +17 LOC (2 row-dict + 2 prov-dict + 3 emit_markdown footer + ~10 embedder-by-tier table) → 421; budget +14 slack
  freeze_py: 105            # current 95; sketch +2 dict assignments → 97; budget +8 slack
  eval_capture_py: 245      # current 236; sketch +3 (import + 2 kwargs) → 239; budget +6 slack
  tier_1_embed_openai_py: 85   # current 80; sketch +1 EMBEDDER_SOURCE → 81; budget +4
  tier_2_managed_main_py: 355  # current 347; sketch +2 (EMBED_MODEL + EMBEDDER_SOURCE) → 349; budget +6
  tier_3_graph_rag_py: 178   # current 173; sketch +1 EMBEDDER_SOURCE → 174; budget +4
  tier_4_multimodal_rag_py: 245  # current 239; sketch +1 EMBEDDER_SOURCE → 240; budget +5
  tier_5_agentic_tools_py: 178  # current 171; sketch +1 EMBEDDER_SOURCE → 172; budget +6

locked_decisions:
  - id: D-ROADMAP-OVERRIDE
    text: |
      ROADMAP-Phase-6 wording for Tier 5 ("OpenAI's hosted vector-store embedder
      with explicit `managed=true` flag") is FACTUALLY WRONG and OVERRIDDEN by this
      plan. Verified in 06-RESEARCH.md against tier-5-agentic/tools.py:47-50,90-101:
      Tier 5 reuses Tier 1's local ChromaDB and embeds via OpenRouter
      `openai/text-embedding-3-small` — IDENTICAL to Tier 1. Tier 5 records:
      `embedder="openai/text-embedding-3-small"`, `embedder_source="openrouter"`.
      The `managed=true` boolean flag is REJECTED in favor of deriving "managed"
      as `embedder_source == "google-managed"` in compare.py rendering — only
      Tier 2 is "managed" (Google File Search owns the indexing).
  - id: D-Q1
    text: |
      Embedder-by-tier disclosure table is emitted in compare.py during Phase 6
      (Task 5), NOT deferred to Phase 9. Costs +~10 LOC in compare.py;
      Phase 9 becomes a pure copy-paste from comparison.md. Researcher recommendation
      accepted.
  - id: D-Q2
    text: |
      Tier 2's EMBED_MODEL constant lives in tier-2-managed/main.py (sibling to
      existing INDEX_USD_PER_1M_TOKENS at line 91), NOT a new tier-2-managed/embed.py
      module. Google File Search is managed — there is no separate embed call to host
      in a dedicated module. Researcher recommendation accepted.
  - id: D-Q3
    text: |
      Pydantic field name is `embedder` (single word), NOT `embedder_model`. Mirrors
      project convention in score.py (`judge_emb`) and freeze.py manifest
      (`"embedder": judge_emb`). Researcher recommendation accepted.
  - id: D-BACKCOMPAT
    text: |
      `embedder` and `embedder_source` are `Optional[str] = None` (Pydantic v2 default).
      Legacy QueryLog JSONs in evaluation/results/queries/ (gitignored) MUST load
      cleanly with both fields = None. compare.py / freeze.py render `—` (em-dash)
      placeholder for None values, mirroring the existing missing-tier-row pattern.
      No legacy-JSON migration tool — Phase 7 re-captures all five tiers and the
      legacy gap closes naturally.
  - id: D-CAPTURE-ENTRYPOINTS
    text: |
      BOTH capture entry points are edited (Pitfall 1 of 06-RESEARCH.md):
      (1) evaluation/harness/run.py::_capture_tier (covers Tiers 1, 2, 3, 4-cached, 5)
      (2) tier-4-multimodal/scripts/eval_capture.py::_capture (Tier 4 live path)
      Each gets its own dedicated regression test; one-entry-point coverage is
      insufficient.
  - id: D-NO-LIVE-SMOKE
    text: |
      Phase 6 has zero new API behavior — every embedder identifier is a static
      literal known at code-edit time. Pure-offline TDD red→green is sufficient.
      Phase 7's 5-tier rerun naturally exercises the new field plumbing in live
      conditions; a Phase-6-specific live smoke would add zero new signal.
  - id: D-LOC
    text: |
      All ten files have explicit max_lines budgets (above) derived from raw `wc -l`
      counts taken 2026-05-06 + sketched LOC delta. Per Plan 04-01 / 05-01 lesson:
      validate via `wc -l <file>` BEFORE locking each commit. Hard cap is the
      max_lines value; if exceeded, compress without semicolon-stacking or
      string-literal-newline-removal. Current/cap pairs: records.py 84/95;
      run.py 391/420; compare.py 404/435; freeze.py 95/105; eval_capture.py 236/245;
      tier-1 embed 80/85; tier-2 main 347/355; tier-3 rag 173/178;
      tier-4 rag 239/245; tier-5 tools 171/178.

must_haves:
  truths:
    - "User can run `python -m evaluation.harness.run --tiers 1` and read the resulting `evaluation/results/queries/tier-1-*.json` and find a top-level `embedder` field with value `openai/text-embedding-3-small` and a top-level `embedder_source` field with value `openrouter` (CAP-03 / Success Criterion 1)."
    - "User can run the same flow for tiers 2, 3, 4 (cached path AND live path), 5 and confirm the recorded (embedder, embedder_source) tuples are: tier-1=(`openai/text-embedding-3-small`,`openrouter`); tier-2=(`gemini-embedding-001`,`google-managed`); tier-3=(`openai/text-embedding-3-small`,`openrouter`); tier-4=(`openai/text-embedding-3-small`,`openrouter`) FOR BOTH harness/run.py AND tier-4-multimodal/scripts/eval_capture.py paths; tier-5=(`openai/text-embedding-3-small`,`openrouter`) — overriding ROADMAP's incorrect 'OpenAI hosted managed' wording (D-ROADMAP-OVERRIDE). (Tier 4 cached path requires `--tier-4-from-cache <fixture-path>` flag passed to `python -m evaluation.harness.run`; Tier 4 live path uses `tier-4-multimodal/scripts/eval_capture.py` directly per D-CAPTURE-ENTRYPOINTS.)"
    - "User can run `python -m evaluation.harness.compare` against a results_dir that includes the new-shape captures and read the produced `comparison.md` Provenance footer to find ONE LINE PER TIER of the form `  - embedder: \`<embedder>\` (source: \`<source>\`)` AND a dedicated 'Embedder by tier' markdown table block above the NaN breakdown (Success Criterion 3 + D-Q1)."
    - "User can run `python -m evaluation.harness.compare` against a results_dir that includes ONLY legacy QueryLog JSONs (no embedder field) and confirm the comparison.md emits `embedder: \`—\` (source: \`—\`)` for those tiers — no traceback, no KeyError (D-BACKCOMPAT + Pitfall 6)."
    - "User can run `python -m evaluation.harness.freeze --version <any-string>` against the new-shape captures and find `embedder` + `embedder_source` keys populated (from the captured queries JSON top-level fields) in every `per_tier[tier-N]` entry of the produced manifest (CAP-03 + freeze provenance). NOTE: this truth is about plumbing per_tier embedder provenance through freeze.py; Phase 6 does NOT lock or own the v1.0 frozen ship-artifact filename — `--version 1.0` and `eval-numbers-v1.0.manifest.json` are owned by Phase 9 (Frozen Handoff Doc)."
    - "User can confirm `Optional[str] = None` deserialization tolerance: load any legacy capture JSON via `QueryLog.model_validate_json(legacy.read_text())` and observe `log.embedder is None` AND `log.embedder_source is None` — no validation error (D-BACKCOMPAT)."
    - "User can run `pytest evaluation/tests/ -m 'not live' -x` (with the standard `--ignore=evaluation/tests/test_eval_adapters.py --ignore=evaluation/tests/test_eval_smoke_live.py` filter) and see all 7 new tests PASS plus all pre-existing offline tests still green (no regressions in records/run/compare/freeze/tier-4 test files)."
    - "User can run `git diff evaluation/harness/score.py evaluation/harness/pipeline.py evaluation/harness/smoke_gate.py | wc -c` and observe `0` — these three modules are byte-identical post-Phase 6 (forward-contract preserved per RESEARCH § 'MUST NOT touch')."

  artifacts:
    - path: "evaluation/harness/records.py"
      provides: "QueryLog schema extended with Optional embedder + embedder_source fields"
      contains: "embedder: Optional[str] = None"
      max_lines: 95
    - path: "evaluation/harness/run.py"
      provides: "_capture_tier sets (embedder, embedder_source) per tier branch and threads into QueryLog constructor"
      contains: "embedder=embedder"
      max_lines: 420
    - path: "evaluation/harness/compare.py"
      provides: "aggregate_tier passes through embedder fields; emit_markdown emits embedder line + embedder-by-tier table; tolerates legacy None values"
      contains: "Embedder by tier"
      max_lines: 435
    - path: "evaluation/harness/freeze.py"
      provides: "per_tier manifest entry carries embedder + embedder_source"
      contains: "\"embedder\": row.get(\"embedder\")"
      max_lines: 105
    - path: "tier-4-multimodal/scripts/eval_capture.py"
      provides: "QueryLog construction includes embedder=DEFAULT_EMBED_MODEL + embedder_source=EMBEDDER_SOURCE"
      contains: "embedder_source=EMBEDDER_SOURCE"
      max_lines: 245
    - path: "tier-1-naive/embed_openai.py"
      provides: "Module-level EMBEDDER_SOURCE = \"openrouter\" constant"
      contains: "EMBEDDER_SOURCE"
      max_lines: 85
    - path: "tier-2-managed/main.py"
      provides: "Module-level EMBED_MODEL = \"gemini-embedding-001\" + EMBEDDER_SOURCE = \"google-managed\""
      contains: "EMBED_MODEL"
      max_lines: 355
    - path: "tier-3-graph/rag.py"
      provides: "Module-level EMBEDDER_SOURCE = \"openrouter\" constant"
      contains: "EMBEDDER_SOURCE"
      max_lines: 178
    - path: "tier-4-multimodal/rag.py"
      provides: "Module-level EMBEDDER_SOURCE = \"openrouter\" constant"
      contains: "EMBEDDER_SOURCE"
      max_lines: 245
    - path: "tier-5-agentic/tools.py"
      provides: "Module-level EMBEDDER_SOURCE = \"openrouter\" constant (EMBED_MODEL re-imported from tier-1, NOT duplicated — Pitfall 4)"
      contains: "EMBEDDER_SOURCE"
      max_lines: 178
    - path: "evaluation/tests/test_eval_records.py"
      provides: "+2 tests: round-trip embedder field + legacy-JSON tolerance"
      contains: "test_query_log_carries_embedder_field"
    - path: "evaluation/tests/test_eval_run.py"
      provides: "+1 test: 5 sub-cases asserting per-tier (embedder, embedder_source) lands in written queries JSON via amain"
      contains: "test_run_amain_writes_embedder_per_tier_offline"
    - path: "evaluation/tests/test_eval_compare.py"
      provides: "+2-3 tests: provenance footer with embedder line + em-dash legacy fallback + (optional) embedder-by-tier table presence"
      contains: "test_compare_emits_embedder_in_provenance_footer"
    - path: "evaluation/tests/test_eval_freeze.py"
      provides: "+1 test: per_tier manifest carries embedder + embedder_source"
      contains: "test_freeze_manifest_carries_embedder_per_tier"
    - path: "evaluation/tests/test_eval_tier4.py"
      provides: "+1 test: tier-4 eval_capture.py builds QueryLog with embedder fields"
      contains: "test_eval_capture_writes_embedder_for_tier_4"

  key_links:
    - from: "evaluation/harness/run.py:_capture_tier"
      to: "evaluation/harness/records.py:QueryLog"
      via: "QueryLog(...embedder=embedder, embedder_source=embedder_source...)"
      pattern: "embedder=embedder"
    - from: "tier-4-multimodal/scripts/eval_capture.py"
      to: "evaluation/harness/records.py:QueryLog"
      via: "QueryLog(...embedder=DEFAULT_EMBED_MODEL, embedder_source=EMBEDDER_SOURCE...)"
      pattern: "embedder=DEFAULT_EMBED_MODEL"
    - from: "evaluation/harness/compare.py:aggregate_tier"
      to: "evaluation/harness/records.py:QueryLog"
      via: "queries.get(\"embedder\") + queries.get(\"embedder_source\") (defensive .get for legacy)"
      pattern: "queries\\.get\\(\"embedder\""
    - from: "evaluation/harness/freeze.py:freeze"
      to: "evaluation/harness/compare.py:aggregate_tier"
      via: "row.get(\"embedder\") + row.get(\"embedder_source\")"
      pattern: "row\\.get\\(\"embedder\""
    - from: "tier-5-agentic/tools.py:EMBEDDER_SOURCE"
      to: "tier-1-naive/embed_openai.py:EMBED_MODEL"
      via: "from tier_1_naive.embed_openai import EMBED_MODEL (existing) + new sibling constant EMBEDDER_SOURCE = 'openrouter' (NOT a duplicate of EMBED_MODEL — Pitfall 4)"
      pattern: "EMBEDDER_SOURCE\\s*=\\s*\"openrouter\""
---

<summary>
## Phase 6 SUMMARY — Embedder Provenance Capture (Plan 06-01)

**Goal closure:** Every per-tier capture JSON (and the rolled-up `comparison.md` + frozen sidecar manifest) carries the embedding-model identifier and its provenance gateway, so the v1.0 frozen doc's embedder-confound disclosure is data-backed (CAP-03).

**Critical override (D-ROADMAP-OVERRIDE):** ROADMAP Phase 6 success criterion #2 incorrectly claims Tier 5 uses "OpenAI's hosted vector-store embedder with `managed=true` flag." Verified false in 06-RESEARCH.md against `tier-5-agentic/tools.py:47-50,90-101`. **Tier 5 actually reuses Tier 1's local ChromaDB and embeds via OpenRouter `openai/text-embedding-3-small` — IDENTICAL to Tier 1.** This plan records:

| Tier | Embedder | Source | Managed (derived) |
|------|----------|--------|-------------------|
| 1 | openai/text-embedding-3-small | openrouter | no |
| 2 | gemini-embedding-001 | google-managed | YES |
| 3 | openai/text-embedding-3-small | openrouter | no |
| 4 | openai/text-embedding-3-small | openrouter | no |
| 5 | openai/text-embedding-3-small | openrouter | no |

The `managed=true` flag is REJECTED in favor of deriving "managed" as `embedder_source == "google-managed"` in compare.py rendering — only Tier 2 qualifies.

**Open questions resolved (research recommendations accepted, see locked_decisions):**
- D-Q1: Embedder-by-tier table emitted in compare.py during Phase 6 (+~10 LOC). Phase 9 becomes pure copy-paste.
- D-Q2: Tier 2's EMBED_MODEL added to `tier-2-managed/main.py` (sibling to INDEX_USD_PER_1M_TOKENS), not a new module.
- D-Q3: Pydantic field name is `embedder` (single word), matching `judge_emb` convention.

**Backwards compatibility (D-BACKCOMPAT):** New fields are `Optional[str] = None`. Legacy QueryLog JSONs in `evaluation/results/queries/` (gitignored) load cleanly with both fields = None; compare.py + freeze.py render em-dash placeholders. No migration tool — Phase 7 re-captures all five tiers.

**Capture entrypoint coverage (D-CAPTURE-ENTRYPOINTS):** BOTH `evaluation/harness/run.py` AND `tier-4-multimodal/scripts/eval_capture.py` are edited; each gets a dedicated regression test (Pitfall 1 of 06-RESEARCH.md).

**Test posture (D-NO-LIVE-SMOKE):** Pure-offline TDD red→green. No live smoke. Phase 7's 5-tier rerun naturally exercises the new plumbing in production.

**LOC posture (D-LOC):** Ten files have explicit max_lines budgets derived from raw `wc -l` 2026-05-06 + sketch deltas. Per Plan 04-01 / 05-01 lesson, validate `wc -l` BEFORE locking each commit.

**Task-count rationale (defensive, addresses scope-sanity flag):** Task count (7) follows atomic-commit-per-task precedent (Plan 04-01: 2 tasks for 1 file; Plan 05-01: 2 tasks for 1 file; this plan: 6 source tasks for 5 tier modules + 4 harness modules + 1 verification gate = Task 7). Atomic commits aid `/gsd:undo` recovery (per project precedent — see STATE.md "atomic-commit-per-task" decision). Merging tasks would entangle commits and reduce undo granularity, increasing rollback blast radius if a downstream phase exposes a regression in (say) only the freeze.py manifest extension. Task 7 is verification-only — it ships ZERO source diffs and lands a single docs-only commit recording the audit-log gate pass; it is NOT additional implementation. Net implementation tasks = 6 (one per concern: schema / tier constants / run.py / eval_capture.py / compare.py / freeze.py).

**VALIDATION.md deviation note (addresses Dimension 8 Check 8e):** No `06-VALIDATION.md` artifact is created at plan-time by this PLAN.md. This is INTENTIONAL and follows project convention: VALIDATION.md is a `/gsd:validate-phase` retroactive artifact created AFTER `/gsd:execute-phase` completes — NOT a `/gsd:plan-phase` output. Phases 02, 03, 04 demonstrate the pattern by NOT having a VALIDATION.md at plan-time (only Phases 01 and 05 have one, and both were added recently as untracked retroactive artifacts per `git status`). Phase 6 will receive a `06-VALIDATION.md` only after `/gsd:execute-phase 6` completes and `/gsd:validate-phase 6` is run. The plan-checker's flag of this as a missing artifact at plan-time over-applied a rule that does not match this project's pattern.
</summary>

<objective>
Ship the embedder-provenance plumbing across two capture entry points, the rollup, the freeze sidecar, and the five tier embedder-source constants — all under pure-offline TDD red→green with atomic per-task commits.

Purpose: Close CAP-03. The frozen v1.0 doc's "embedder confound" disclosure must be data-backed by the capture JSONs (not narrative). After Phase 6 + Phase 7, the embedder-by-tier table in `comparison.md` is generated from the actual five capture JSONs of record.

Output: Two new fields (`embedder`, `embedder_source`) on `QueryLog`; per-tier embedder-source constants in five tier modules; new field passthrough in `_capture_tier` (run.py) + `_capture` (eval_capture.py); compare.py footer + embedder-by-tier table; freeze.py per_tier manifest entry; 7 new offline pytest cases. Zero new external deps. Zero edits to score.py / pipeline.py / smoke_gate.py.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-embedder-provenance-capture/06-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

@evaluation/harness/records.py
@evaluation/harness/run.py
@evaluation/harness/compare.py
@evaluation/harness/freeze.py
@tier-4-multimodal/scripts/eval_capture.py
@tier-1-naive/embed_openai.py
@tier-2-managed/main.py
@tier-3-graph/rag.py
@tier-4-multimodal/rag.py
@tier-5-agentic/tools.py
@evaluation/tests/conftest.py
@evaluation/tests/test_eval_records.py
@evaluation/tests/test_eval_run.py
@evaluation/tests/test_eval_compare.py
@evaluation/tests/test_eval_freeze.py
@evaluation/tests/test_eval_tier4.py

<interfaces>
<!-- Locked entry-point signatures + new field contract. Verified against source 2026-05-06. -->

records.py — QueryLog after Phase 6:
```python
class QueryLog(BaseModel):
    tier: str
    timestamp: str
    git_sha: str
    model: str
    embedder: Optional[str] = None        # NEW
    embedder_source: Optional[str] = None # NEW
    records: list[EvalRecord]
```

Per-tier (embedder, source) tuple recorded by run.py / eval_capture.py:
```python
TIER_EMBEDDERS = {
    1: ("openai/text-embedding-3-small", "openrouter"),
    2: ("gemini-embedding-001",          "google-managed"),
    3: ("openai/text-embedding-3-small", "openrouter"),
    4: ("openai/text-embedding-3-small", "openrouter"),
    5: ("openai/text-embedding-3-small", "openrouter"),  # D-ROADMAP-OVERRIDE
}
```

Existing constants reused (DO NOT duplicate):
- tier-1-naive/embed_openai.py:31  EMBED_MODEL = "openai/text-embedding-3-small"
- tier-3-graph/rag.py:56            DEFAULT_EMBED_MODEL = "openai/text-embedding-3-small"
- tier-4-multimodal/rag.py:53       DEFAULT_EMBED_MODEL = "openai/text-embedding-3-small"
- tier-5-agentic/tools.py:49        EMBED_MODEL  (re-imported from tier_1_naive.embed_openai — Pitfall 4)

New constants ADDED by this plan:
- tier-1-naive/embed_openai.py        EMBEDDER_SOURCE = "openrouter"
- tier-2-managed/main.py              EMBED_MODEL = "gemini-embedding-001" + EMBEDDER_SOURCE = "google-managed"
- tier-3-graph/rag.py                 EMBEDDER_SOURCE = "openrouter"
- tier-4-multimodal/rag.py            EMBEDDER_SOURCE = "openrouter"
- tier-5-agentic/tools.py             EMBEDDER_SOURCE = "openrouter"

Forward-contract guards (BYTE-IDENTICAL post-Phase 6):
- evaluation/harness/score.py
- evaluation/harness/pipeline.py
- evaluation/harness/smoke_gate.py
- evaluation/harness/adapters/tier_{1..5}.py (adapters construct EvalRecord, not QueryLog)
</interfaces>

<test_fixtures_to_reuse>
- evaluation/tests/test_eval_records.py — Pydantic round-trip pattern (template for Task 1 RED).
- evaluation/tests/test_eval_run.py — `monkeypatch.setattr` on adapter call sites pattern (template for Task 3 RED).
- evaluation/tests/test_eval_compare.py — fake-results-dir builder pattern (template for Task 5 RED).
- evaluation/tests/test_eval_freeze.py:_build_fixture — full results-dir fixture (template for Task 6 RED).
- evaluation/tests/conftest.py — sys.path bootstrap; no edits needed.
</test_fixtures_to_reuse>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema extension — QueryLog gains Optional embedder + embedder_source (RED + GREEN)</name>
  <files>evaluation/harness/records.py, evaluation/tests/test_eval_records.py</files>
  <action>
    **RED** (commit 1 — `test(06-01): RED — QueryLog Optional embedder fields`):

    Add 2 tests to `evaluation/tests/test_eval_records.py`:

    1. `test_query_log_carries_embedder_field`: construct `QueryLog(tier="tier-1", timestamp="2026-05-06T12:00:00Z", git_sha="abc1234", model="google/gemini-2.5-flash", embedder="openai/text-embedding-3-small", embedder_source="openrouter", records=[])`. Round-trip via `QueryLog.model_validate_json(log.model_dump_json())`. Assert `roundtrip.embedder == "openai/text-embedding-3-small"` AND `roundtrip.embedder_source == "openrouter"`. This test FAILS today because the fields don't exist (Pydantic v2 raises `ValidationError` on extra inputs OR — if `model_config = ConfigDict(extra="ignore")` — silently drops them; verify which by reading records.py:40-47 and assert the field SURVIVES the round-trip with the correct value).

    2. `test_query_log_legacy_json_loads_with_none_embedder`: build a legacy JSON string `'{"tier":"tier-1","timestamp":"2026-05-02T17:26:59Z","git_sha":"ce5c2ad","model":"google/gemini-2.5-flash","records":[]}'` (NO `embedder` field). Load via `QueryLog.model_validate_json(legacy_json)`. Assert `log.embedder is None` AND `log.embedder_source is None`. Today this test FAILS because the schema rejects the load OR (if it succeeds without the fields) the attributes don't exist on the instance.

    Run `.venv/bin/pytest evaluation/tests/test_eval_records.py -v 2>&1 | tail -20`. Both new tests must FAIL with a Pydantic ValidationError or AttributeError. Pre-existing tests (1 in the file at minimum — confirm via `grep -c '^def test_' evaluation/tests/test_eval_records.py` reports `pre + 2`) must still pass. Commit RED.

    **GREEN** (commit 2 — `feat(06-01): GREEN — QueryLog Optional embedder fields`):

    Edit `evaluation/harness/records.py:40-47` (the `QueryLog` class). Add 2 fields after `model: str`:

    ```python
    embedder: Optional[str] = None
    embedder_source: Optional[str] = None
    ```

    `Optional` is already imported at line 18 (`from typing import Optional`); no new import needed.

    Run `.venv/bin/pytest evaluation/tests/test_eval_records.py -v`. Both new tests + all pre-existing tests must PASS.

    LOC validation: `wc -l evaluation/harness/records.py` ≤ 95 (current 84 + 2 fields = 86; budget has 9 LOC slack).

    Commit GREEN.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/rag-architecture-patterns &amp;&amp; .venv/bin/pytest evaluation/tests/test_eval_records.py -v 2>&amp;1 | tail -20 ; echo === LOC === ; wc -l evaluation/harness/records.py ; echo === field-presence === ; .venv/bin/python -c "from evaluation.harness.records import QueryLog; q=QueryLog(tier='t',timestamp='2026-05-06T12:00:00Z',git_sha='abc',model='m',embedder='e',embedder_source='s',records=[]); print(q.embedder, q.embedder_source)"</automated>

Expected (post-GREEN):
- All test_eval_records.py tests PASS (pre-existing + 2 new).
- `wc -l evaluation/harness/records.py` ≤ 95.
- Field-presence probe prints `e s`.
  </verify>
  <done>
    `evaluation/harness/records.py` has `embedder: Optional[str] = None` + `embedder_source: Optional[str] = None` on `QueryLog`. 2 new tests pass; pre-existing tests still pass. LOC ≤ 95. Two atomic commits land (RED + GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-tier embedder-source constants in 5 tier modules (RED + GREEN)</name>
  <files>tier-1-naive/embed_openai.py, tier-2-managed/main.py, tier-3-graph/rag.py, tier-4-multimodal/rag.py, tier-5-agentic/tools.py</files>
  <action>
    **RED** (commit 1 — `test(06-01): RED — per-tier embedder-source constants importable`):

    There is no dedicated test file for tier-module constants today. Add a SINGLE pytest case to `evaluation/tests/test_eval_records.py` (most-natural location since it tests the schema's data inputs) named `test_per_tier_embedder_constants_importable`:

    ```python
    def test_per_tier_embedder_constants_importable():
        """Each tier module exports the (EMBED_MODEL, EMBEDDER_SOURCE) tuple
        used by run.py / eval_capture.py to populate QueryLog fields."""
        from tier_1_naive.embed_openai import EMBED_MODEL as T1_EMB, EMBEDDER_SOURCE as T1_SRC
        from tier_2_managed.main import EMBED_MODEL as T2_EMB, EMBEDDER_SOURCE as T2_SRC
        from tier_3_graph.rag import DEFAULT_EMBED_MODEL as T3_EMB, EMBEDDER_SOURCE as T3_SRC
        from tier_4_multimodal.rag import DEFAULT_EMBED_MODEL as T4_EMB, EMBEDDER_SOURCE as T4_SRC
        from tier_5_agentic.tools import EMBED_MODEL as T5_EMB, EMBEDDER_SOURCE as T5_SRC
        assert T1_EMB == "openai/text-embedding-3-small" and T1_SRC == "openrouter"
        assert T2_EMB == "gemini-embedding-001"          and T2_SRC == "google-managed"
        assert T3_EMB == "openai/text-embedding-3-small" and T3_SRC == "openrouter"
        assert T4_EMB == "openai/text-embedding-3-small" and T4_SRC == "openrouter"
        # D-ROADMAP-OVERRIDE: Tier 5 IS Tier 1's embedder, NOT a hosted vector store.
        assert T5_EMB == "openai/text-embedding-3-small" and T5_SRC == "openrouter"
    ```

    Run `.venv/bin/pytest evaluation/tests/test_eval_records.py::test_per_tier_embedder_constants_importable -v`. Test FAILS today with `ImportError: cannot import name 'EMBEDDER_SOURCE' from 'tier_1_naive.embed_openai'` (and similar for the other four). Commit RED.

    **GREEN** (commit 2 — `feat(06-01): GREEN — per-tier embedder-source constants`):

    Five surgical edits — exactly one constant added per file (Tier 2 adds two — both EMBED_MODEL and EMBEDDER_SOURCE):

    1. `tier-1-naive/embed_openai.py` — after line 31 (existing `EMBED_MODEL = "openai/text-embedding-3-small"`), add:
       ```python
       EMBEDDER_SOURCE: str = "openrouter"
       ```
       Update `__all__` if present.

    2. `tier-2-managed/main.py` — sibling to existing `INDEX_USD_PER_1M_TOKENS` constant at line 91, add (per D-Q2):
       ```python
       EMBED_MODEL: str = "gemini-embedding-001"
       EMBEDDER_SOURCE: str = "google-managed"
       ```
       (Tier 2's only existing reference is the hardcoded string in `tracker.record_embedding(...)` at line 191. The new constant becomes the single source of truth; the existing hardcode at line 191 may be replaced with `EMBED_MODEL` for symmetry but this is OPTIONAL — keep it hardcoded to minimize the diff if it's safer.)

    3. `tier-3-graph/rag.py` — after line 56 (existing `DEFAULT_EMBED_MODEL = "openai/text-embedding-3-small"`), add:
       ```python
       EMBEDDER_SOURCE: str = "openrouter"
       ```
       Update `__all__` at line 170 to include `"EMBEDDER_SOURCE"`.

    4. `tier-4-multimodal/rag.py` — after line 53 (existing `DEFAULT_EMBED_MODEL = "openai/text-embedding-3-small"`), add:
       ```python
       EMBEDDER_SOURCE: str = "openrouter"
       ```
       Update `__all__` at line 236 to include `"EMBEDDER_SOURCE"`.

    5. `tier-5-agentic/tools.py` — after the existing `from tier_1_naive.embed_openai import EMBED_MODEL` block (lines 47-50), add a NEW MODULE-LEVEL CONSTANT (NOT a re-import) per Pitfall 4:
       ```python
       EMBEDDER_SOURCE: str = "openrouter"
       ```
       (`EMBED_MODEL` is RE-USED from Tier 1 via the existing import. Do NOT duplicate the embedder string.)

       **DEFENSIVE NOTE (test fragility):** Task 2's RED test imports `from tier_5_agentic.tools import EMBED_MODEL as T5_EMB` — this works today because `tier-5-agentic/tools.py` re-exports `EMBED_MODEL` transitively via the existing `from tier_1_naive.embed_openai import EMBED_MODEL` line at tools.py:49. There is NO `__all__` declaration in tools.py today, so the transitive re-export is implicit. **DO NOT add an `__all__` to `tier-5-agentic/tools.py` that excludes `EMBED_MODEL`** — doing so would break Task 2's test silently. If a future executor must add `__all__`, list `EMBED_MODEL` explicitly in it (alongside `EMBEDDER_SOURCE` and other public names) OR change the test to import `EMBED_MODEL` directly from `tier_1_naive.embed_openai`.

    Run the test:
    ```
    .venv/bin/pytest evaluation/tests/test_eval_records.py::test_per_tier_embedder_constants_importable -v
    ```
    Must PASS. Then run the full file: `.venv/bin/pytest evaluation/tests/test_eval_records.py -v` — all 3 new tests + pre-existing tests PASS.

    LOC validation per file:
    - `wc -l tier-1-naive/embed_openai.py` ≤ 85 (current 80 + 1)
    - `wc -l tier-2-managed/main.py`        ≤ 355 (current 347 + 2)
    - `wc -l tier-3-graph/rag.py`           ≤ 178 (current 173 + 1)
    - `wc -l tier-4-multimodal/rag.py`      ≤ 245 (current 239 + 1)
    - `wc -l tier-5-agentic/tools.py`       ≤ 178 (current 171 + 1)

    Commit GREEN.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/rag-architecture-patterns &amp;&amp; .venv/bin/pytest evaluation/tests/test_eval_records.py -v 2>&amp;1 | tail -20 ; echo === LOC === ; wc -l tier-1-naive/embed_openai.py tier-2-managed/main.py tier-3-graph/rag.py tier-4-multimodal/rag.py tier-5-agentic/tools.py ; echo === constants === ; .venv/bin/python -c "from tier_1_naive.embed_openai import EMBED_MODEL, EMBEDDER_SOURCE; from tier_2_managed.main import EMBED_MODEL as T2E, EMBEDDER_SOURCE as T2S; from tier_3_graph.rag import EMBEDDER_SOURCE as T3S; from tier_4_multimodal.rag import EMBEDDER_SOURCE as T4S; from tier_5_agentic.tools import EMBEDDER_SOURCE as T5S; print(EMBED_MODEL, EMBEDDER_SOURCE, T2E, T2S, T3S, T4S, T5S)"</automated>

Expected:
- 3 new test_eval_records.py tests pass.
- Each tier file is at-or-below its LOC budget.
- Constant probe prints: `openai/text-embedding-3-small openrouter gemini-embedding-001 google-managed openrouter openrouter openrouter`.
  </verify>
  <done>
    Five tier modules export the (EMBED_MODEL, EMBEDDER_SOURCE) tuple with the values locked in the truth table above. test_per_tier_embedder_constants_importable PASSES. All five files at-or-below LOC budget. Two atomic commits land (RED + GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: run.py threads (embedder, embedder_source) per-tier into QueryLog (RED + GREEN)</name>
  <files>evaluation/harness/run.py, evaluation/tests/test_eval_run.py</files>
  <action>
    **RED** (commit 1 — `test(06-01): RED — run.amain writes embedder per tier`):

    Add ONE new test to `evaluation/tests/test_eval_run.py` named `test_run_amain_writes_embedder_per_tier_offline` (parametrized over the 5 tiers). The test:

    1. Uses `monkeypatch` to stub the relevant `evaluation.harness.adapters.tier_{N}.run_tier{N}` function (and any storage/agent builders for tiers 3 + 5) so they return a fixed `EvalRecord` without touching the network.
    2. For tier 4: pass `--tier-4-from-cache /tmp/<fixture-cache>.json` pointing to a minimal fixture JSON with one record matching the cached schema (`run_tier4` will read it).
    3. Builds an `args = Namespace(tiers=str(N), output_dir=str(tmp_path), limit=1, smoke_question_ids=None, tier_4_from_cache=fixture_path_for_4_else_None, mode="hybrid", tier1_k=5, yes=True, ...)` covering each tier's required args (mirror the existing test_eval_run.py patterns).
    4. Runs `await run.amain(args, Console())`.
    5. Reads the written queries JSON: `data = json.loads((tmp_path / "queries").glob(f"tier-{N}-*.json").__next__().read_text())`.
    6. Asserts `data["embedder"]` and `data["embedder_source"]` match the expected tuple per the truth table:
       - 1 → ("openai/text-embedding-3-small", "openrouter")
       - 2 → ("gemini-embedding-001",          "google-managed")
       - 3 → ("openai/text-embedding-3-small", "openrouter")
       - 4 → ("openai/text-embedding-3-small", "openrouter")
       - 5 → ("openai/text-embedding-3-small", "openrouter")
    7. Asserts `data["model"]`, `data["timestamp"]`, `data["git_sha"]` are still present (regression guard — Phase 5 / forward contract).

    Use `pytest.mark.parametrize("tier,exp_emb,exp_src", [(1, ..., ...), (2, ...), ...])` for the 5 sub-cases.

    Use `pytest.importorskip` ONLY if a tier's module cannot be imported in the test env (e.g., LightRAG init for Tier 3 may need monkeypatching `build_rag` to a no-op). Mirror the existing `test_eval_run.py` patterns for tier-2-store-id-file (`tmp_path / "tier-2-managed" / ".store_id"` write) and tier-3-rag-builder mock.

    Run `.venv/bin/pytest evaluation/tests/test_eval_run.py::test_run_amain_writes_embedder_per_tier_offline -v`. ALL 5 sub-cases FAIL — the queries JSON does not yet contain `embedder` / `embedder_source` because `_capture_tier` doesn't set them. Commit RED.

    **GREEN** (commit 2 — `feat(06-01): GREEN — run.py threads embedder per tier`):

    Edit `evaluation/harness/run.py:_capture_tier` (lines 146-276). Per tier branch:

    Tier 1 (lines 168-179): inside the imports, add:
    ```python
    from tier_1_naive.embed_openai import EMBED_MODEL as T1_EMBEDDER, EMBEDDER_SOURCE as T1_EMBEDDER_SOURCE
    ```
    After `model = T1_MODEL`, set:
    ```python
    embedder = T1_EMBEDDER
    embedder_source = T1_EMBEDDER_SOURCE
    ```

    Tier 2 (lines 181-194): inside the imports, add:
    ```python
    from tier_2_managed.main import EMBED_MODEL as T2_EMBEDDER, EMBEDDER_SOURCE as T2_EMBEDDER_SOURCE
    ```
    After `model = T2_MODEL`, set:
    ```python
    embedder = T2_EMBEDDER
    embedder_source = T2_EMBEDDER_SOURCE
    ```

    Tier 3 (lines 196-216): the import block already pulls `DEFAULT_EMBED_MODEL`. Add `EMBEDDER_SOURCE`:
    ```python
    from tier_3_graph.rag import (
        DEFAULT_EMBED_MODEL,
        DEFAULT_LLM_MODEL as T3_MODEL,
        EMBEDDER_SOURCE as T3_EMBEDDER_SOURCE,
        build_rag,
    )
    ```
    After `model = T3_MODEL`:
    ```python
    embedder = DEFAULT_EMBED_MODEL
    embedder_source = T3_EMBEDDER_SOURCE
    ```

    Tier 4 (cached path, lines 218-241): add inside the import block:
    ```python
    from tier_4_multimodal.rag import DEFAULT_EMBED_MODEL as T4_EMBEDDER, EMBEDDER_SOURCE as T4_EMBEDDER_SOURCE
    ```
    After `model = "google/gemini-2.5-flash"`:
    ```python
    embedder = T4_EMBEDDER
    embedder_source = T4_EMBEDDER_SOURCE
    ```

    Tier 5 (lines 243-255): add to imports:
    ```python
    from tier_1_naive.embed_openai import EMBED_MODEL as T5_EMBEDDER  # Tier 5 reuses Tier 1's embedder — Pitfall 4
    from tier_5_agentic.tools import EMBEDDER_SOURCE as T5_EMBEDDER_SOURCE
    ```
    After `model = T5_MODEL`:
    ```python
    embedder = T5_EMBEDDER
    embedder_source = T5_EMBEDDER_SOURCE
    ```

    QueryLog construction (lines 261-267) — extend with the two new kwargs:
    ```python
    log = QueryLog(
        tier=f"tier-{tier}",
        timestamp=timestamp,
        git_sha=git_sha,
        model=model,
        embedder=embedder,
        embedder_source=embedder_source,
        records=records,
    )
    ```

    Run the test: all 5 sub-cases PASS.
    Run the full file: `.venv/bin/pytest evaluation/tests/test_eval_run.py -v -m 'not live'` — no regressions in pre-existing tests.

    LOC validation: `wc -l evaluation/harness/run.py` ≤ 420 (current 391 + ~13 LOC = 404; budget has 16 slack).

    Commit GREEN.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/rag-architecture-patterns &amp;&amp; .venv/bin/pytest evaluation/tests/test_eval_run.py -v -m 'not live' 2>&amp;1 | tail -30 ; echo === LOC === ; wc -l evaluation/harness/run.py ; echo === byte-identical-guard === ; git diff evaluation/harness/score.py evaluation/harness/pipeline.py evaluation/harness/smoke_gate.py | wc -c</automated>

Expected:
- All 5 parametrized sub-cases of `test_run_amain_writes_embedder_per_tier_offline` PASS.
- Pre-existing test_eval_run.py tests still PASS.
- `wc -l evaluation/harness/run.py` ≤ 420.
- Byte-identical guard: `git diff` of score.py / pipeline.py / smoke_gate.py == 0 bytes.
  </verify>
  <done>
    `_capture_tier` in run.py reads the (embedder, source) tuple per tier branch and threads it into the `QueryLog` constructor. The 5 parametrized sub-cases pass; written queries JSON contains the correct embedder fields per tier. LOC ≤ 420. Byte-identical guards intact for score.py / pipeline.py / smoke_gate.py. Two atomic commits land (RED + GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: tier-4-multimodal/scripts/eval_capture.py threads embedder fields (RED + GREEN)</name>
  <files>tier-4-multimodal/scripts/eval_capture.py, evaluation/tests/test_eval_tier4.py</files>
  <action>
    **RED** (commit 1 — `test(06-01): RED — tier-4 eval_capture writes embedder fields`):

    Add ONE new test to `evaluation/tests/test_eval_tier4.py` named `test_eval_capture_writes_embedder_for_tier_4`. The test:

    1. Imports `tier_4_multimodal.scripts.eval_capture`.
    2. Builds a `tmp_path / "queries"` results-dir.
    3. Monkeypatches `tier_4_multimodal.rag.build_rag` to return a stub object with `aquery(...)` returning a fixture answer + contexts (avoid real MinerU / RAGAnything).
    4. Monkeypatches `tier_4_multimodal.query.run_query` if needed for fully-stubbed control.
    5. Monkeypatches `evaluation.harness.records.write_query_log` to a wrapper that captures the QueryLog instance into `captured["log"]`.
    6. Invokes `eval_capture._capture(...)` (or `eval_capture.main([...])` — pick the smaller-surface entry point) with a 1-question fixture QA list.
    7. Asserts `captured["log"].embedder == "openai/text-embedding-3-small"` AND `captured["log"].embedder_source == "openrouter"`.

    Alternative (simpler if the function structure permits): import the file as text, parse the QueryLog construction site (lines 195-201), assert the source contains `embedder=DEFAULT_EMBED_MODEL` and `embedder_source=EMBEDDER_SOURCE`. This is a STATIC source-level assertion that avoids the need to mock the heavy RAG stack. Recommended approach if mocking is fragile.

    Run `.venv/bin/pytest evaluation/tests/test_eval_tier4.py::test_eval_capture_writes_embedder_for_tier_4 -v`. Test FAILS — eval_capture.py:195-201 doesn't yet pass embedder kwargs. Commit RED.

    **GREEN** (commit 2 — `feat(06-01): GREEN — tier-4 eval_capture threads embedder fields`):

    Edit `tier-4-multimodal/scripts/eval_capture.py`:

    1. Line 46 — extend the import:
       ```python
       from tier_4_multimodal.rag import build_rag, DEFAULT_LLM_MODEL, DEFAULT_EMBED_MODEL, EMBEDDER_SOURCE
       ```
       (`EMBEDDER_SOURCE` was added to `tier_4_multimodal/rag.py` in Task 2.)

    2. Lines 195-201 — extend `QueryLog` construction:
       ```python
       log = QueryLog(
           tier="tier-4",
           timestamp=timestamp,
           git_sha=_git_sha(),
           model=DEFAULT_LLM_MODEL,
           embedder=DEFAULT_EMBED_MODEL,
           embedder_source=EMBEDDER_SOURCE,
           records=records,
       )
       ```

    Run the test: PASS. Run the rest of `evaluation/tests/test_eval_tier4.py` and `tier-4-multimodal/tests/` (if present) for regressions.

    LOC validation: `wc -l tier-4-multimodal/scripts/eval_capture.py` ≤ 245 (current 236 + 3 = 239).

    Commit GREEN.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/rag-architecture-patterns &amp;&amp; .venv/bin/pytest evaluation/tests/test_eval_tier4.py -v 2>&amp;1 | tail -20 ; echo === LOC === ; wc -l tier-4-multimodal/scripts/eval_capture.py ; echo === source-grep === ; grep -E 'embedder=DEFAULT_EMBED_MODEL|embedder_source=EMBEDDER_SOURCE' tier-4-multimodal/scripts/eval_capture.py</automated>

Expected:
- `test_eval_capture_writes_embedder_for_tier_4` PASSES.
- `wc -l tier-4-multimodal/scripts/eval_capture.py` ≤ 245.
- grep finds both `embedder=DEFAULT_EMBED_MODEL` AND `embedder_source=EMBEDDER_SOURCE` lines in the source.
  </verify>
  <done>
    `tier-4-multimodal/scripts/eval_capture.py` constructs `QueryLog` with `embedder=DEFAULT_EMBED_MODEL` + `embedder_source=EMBEDDER_SOURCE`. The dedicated regression test passes. LOC ≤ 245. Two atomic commits land (RED + GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: compare.py rollup integration — pass-through + footer + embedder-by-tier table (RED + GREEN)</name>
  <files>evaluation/harness/compare.py, evaluation/tests/test_eval_compare.py</files>
  <action>
    **RED** (commit 1 — `test(06-01): RED — compare emits embedder line + table`):

    Add 3 tests to `evaluation/tests/test_eval_compare.py`:

    1. `test_compare_emits_embedder_in_provenance_footer`:
       - Build a fake results-dir with ONE tier (tier-1) queries JSON containing `embedder="openai/text-embedding-3-small"` + `embedder_source="openrouter"` (extend the existing fixture pattern in test_eval_compare.py).
       - Invoke `compare._run(args)` with `args.results_dir=tmp_path, args.tiers="1", args.out=str(tmp_path/"comparison.md")`.
       - Read the produced markdown.
       - Assert it contains the substring: ``  - embedder: `openai/text-embedding-3-small` (source: `openrouter`)`` (note the 2-space indent under the parent capture-provenance bullet).

    2. `test_compare_emits_em_dash_for_legacy_embedder`:
       - Same fixture but the queries JSON OMITS the `embedder` and `embedder_source` keys (legacy-shape).
       - Assert the produced markdown contains ``  - embedder: `—` (source: `—`)``.
       - Assert NO Python traceback / KeyError raised — `compare._run` must return 0.

    3. `test_compare_emits_embedder_by_tier_table` (D-Q1):
       - Build a fake results-dir with ALL FIVE tiers' queries JSONs, each carrying its expected (embedder, embedder_source) per the truth table.
       - Invoke `compare._run`.
       - Assert the produced markdown contains the literal heading `**Embedder by tier:**` AND a row for each of the 5 tiers with the correct (embedder, source, managed-derived) values.
       - Specifically assert: row `tier-2 | gemini-embedding-001 | google-managed | yes` is present, AND `tier-5 | openai/text-embedding-3-small | openrouter | no` is present (closing D-ROADMAP-OVERRIDE).

    Run `.venv/bin/pytest evaluation/tests/test_eval_compare.py -v -k embedder`. All 3 FAIL. Commit RED.

    **GREEN** (commit 2 — `feat(06-01): GREEN — compare embedder pass-through + footer + table`):

    Edit `evaluation/harness/compare.py` in three places:

    1. `aggregate_tier()` (lines 130-132) — extend the returned row dict with two new fields:
       ```python
       "embedder": queries.get("embedder"),
       "embedder_source": queries.get("embedder_source"),
       ```
       (Defensive `.get()` returns None for legacy QueryLog JSONs lacking the field — D-BACKCOMPAT.)

    2. `_run()` (lines 357-365) — extend the `capture_provenance` dict build:
       ```python
       capture_provenance.append({
           "tier_label": r["tier_label"],
           "timestamp": r.get("timestamp", "—"),
           "model": r.get("model", "—"),
           "git_sha": r.get("git_sha", "—"),
           "embedder": r.get("embedder"),
           "embedder_source": r.get("embedder_source"),
       })
       ```

    3. `emit_markdown()` (lines 259-264) — extend the per-tier capture-provenance loop with an embedder line, em-dashed when None:
       ```python
       for prov in capture_provenance:
           lines.append(
               f"- `{prov['tier_label']}`: captured {prov['timestamp']} "
               f"(model `{prov['model']}`, git `{prov['git_sha']}`)"
           )
           emb = prov.get("embedder") or "—"
           src = prov.get("embedder_source") or "—"
           lines.append(f"  - embedder: `{emb}` (source: `{src}`)")
       ```

    4. `emit_markdown()` — ADD a dedicated "Embedder by tier" table block ABOVE the NaN breakdown (insert between lines 266 and 268 — D-Q1). Sketch:
       ```python
       lines.extend(["", "**Embedder by tier:**", "",
           "| Tier | Embedder Model | Source | Managed |",
           "|------|----------------|--------|---------|"])
       for prov in capture_provenance:
           emb = prov.get("embedder") or "—"
           src = prov.get("embedder_source") or "—"
           managed = "yes" if src == "google-managed" else ("no" if src != "—" else "—")
           lines.append(f"| {prov['tier_label']} | {emb} | {src} | {managed} |")
       ```
       Estimated +9 LOC for this block.

    Run all 3 new tests. ALL PASS.
    Run the full file: `.venv/bin/pytest evaluation/tests/test_eval_compare.py -v` — no regressions.

    LOC validation: `wc -l evaluation/harness/compare.py` ≤ 435 (current 404 + ~17 LOC = 421; budget has 14 slack).

    Commit GREEN.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/rag-architecture-patterns &amp;&amp; .venv/bin/pytest evaluation/tests/test_eval_compare.py -v 2>&amp;1 | tail -30 ; echo === LOC === ; wc -l evaluation/harness/compare.py ; echo === byte-identical-guard === ; git diff evaluation/harness/score.py evaluation/harness/pipeline.py evaluation/harness/smoke_gate.py | wc -c</automated>

Expected:
- 3 new compare tests PASS.
- Pre-existing compare tests PASS.
- `wc -l evaluation/harness/compare.py` ≤ 435.
- Byte-identical guard: 0 bytes diff for score/pipeline/smoke_gate.
  </verify>
  <done>
    `aggregate_tier` passes through `embedder` + `embedder_source`. `emit_markdown` emits per-tier embedder line (em-dash for None) AND a dedicated 'Embedder by tier' table block. 3 new tests pass; pre-existing tests still pass. LOC ≤ 435. Two atomic commits land (RED + GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: freeze.py per_tier manifest carries embedder fields (RED + GREEN)</name>
  <files>evaluation/harness/freeze.py, evaluation/tests/test_eval_freeze.py</files>
  <action>
    **RED** (commit 1 — `test(06-01): RED — freeze manifest carries embedder per tier`):

    Add ONE new test to `evaluation/tests/test_eval_freeze.py` named `test_freeze_manifest_carries_embedder_per_tier`:

    1. Reuse the existing `_build_fixture` helper (or extend it with a `with_embedder=True` flag) so each tier's queries JSON carries `embedder` + `embedder_source` per the truth table.
    2. Run `freeze(version="6.0", force=False, results_dir=tmp_path)`.
    3. Load the manifest JSON.
    4. For each tier 1-5: assert `manifest["per_tier"][f"tier-{N}"]["embedder"] == expected_embedder` AND `manifest["per_tier"][f"tier-{N}"]["embedder_source"] == expected_source`.
    5. Specifically assert the Tier 5 row reads `("openai/text-embedding-3-small", "openrouter")` — locking D-ROADMAP-OVERRIDE in the manifest.
    6. Assert the existing `judge.embedder` field is UNCHANGED (still `"openai/text-embedding-3-small"`) — this is the JUDGE embedder, distinct from the per-tier embedder (Phase 4 contract preserved).

    Run `.venv/bin/pytest evaluation/tests/test_eval_freeze.py::test_freeze_manifest_carries_embedder_per_tier -v`. FAILS — manifest's per_tier entries don't yet contain `embedder` / `embedder_source`. Commit RED.

    **GREEN** (commit 2 — `feat(06-01): GREEN — freeze threads embedder per tier`):

    Edit `evaluation/harness/freeze.py:53-66` (the per-tier manifest entry build). Extend the `entry` dict with two new fields:

    ```python
    entry: dict = {"status": "present", "generation_model": row.get("model"),
        "embedder": row.get("embedder"),
        "embedder_source": row.get("embedder_source"),
        "capture_timestamp": row.get("timestamp"), "capture_git_sha": row.get("git_sha"),
        "queries_path": _rel(row["queries_path"], results_dir),
        "queries_mtime": _iso_z(Path(row["queries_path"]).stat().st_mtime)}
    ```

    (Defensive `.get()` returns None for legacy rows lacking the fields — D-BACKCOMPAT.)

    Run `.venv/bin/pytest evaluation/tests/test_eval_freeze.py -v` — all tests PASS (10 pre-existing + 1 new).

    LOC validation: `wc -l evaluation/harness/freeze.py` ≤ 105 (current 95 + 2 = 97; budget has 8 slack).

    Commit GREEN.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/rag-architecture-patterns &amp;&amp; .venv/bin/pytest evaluation/tests/test_eval_freeze.py -v 2>&amp;1 | tail -20 ; echo === LOC === ; wc -l evaluation/harness/freeze.py ; echo === byte-identical-guard === ; git diff evaluation/harness/score.py evaluation/harness/pipeline.py evaluation/harness/smoke_gate.py | wc -c</automated>

Expected:
- 1 new freeze test PASSES.
- All 10 pre-existing freeze tests PASS.
- `wc -l evaluation/harness/freeze.py` ≤ 105.
- Byte-identical guard: 0 bytes diff for score / pipeline / smoke_gate.
  </verify>
  <done>
    `freeze()` per_tier manifest entry carries `embedder` + `embedder_source` (None-tolerant for legacy rows). 1 new freeze test passes; all 10 pre-existing tests pass. LOC ≤ 105. Two atomic commits land (RED + GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: End-to-end regression sweep + byte-identical guards (full-suite gate)</name>
  <files>(no source edits — verification-only task)</files>
  <action>
    **VERIFICATION-ONLY TASK — SHIPS ZERO SOURCE DIFFS.** This is the phase-level full-suite gate task — no new source code, no test additions, no module edits. Lands exactly ONE commit (a docs-only verification audit-log commit per step 6 below). Its purpose is to gate phase closure: run every cross-cutting check that no individual Task 1-6 owns end-to-end (full-suite regression sweep, byte-identical forward-contract guards across all three protected modules at once, all-files LOC budget audit, end-to-end CLI happy path, ROADMAP-override grep audit). Run all the gates in sequence; if any fails, return to the relevant prior task and fix.

    1. **Full offline test sweep:**
       ```
       .venv/bin/pytest evaluation/tests/ \
         --ignore=evaluation/tests/test_eval_adapters.py \
         --ignore=evaluation/tests/test_eval_smoke_live.py \
         -m 'not live' -x 2>&amp;1 | tail -10
       ```
       Expected: ALL pre-existing offline tests + 7-9 new tests (2 records + 1 constants + 1 run-parametrized × 5 sub-cases + 1 tier-4 + 3 compare + 1 freeze) PASS.

    2. **Byte-identical forward-contract guards:**
       ```
       git diff evaluation/harness/score.py evaluation/harness/pipeline.py evaluation/harness/smoke_gate.py | wc -c
       ```
       Expected: `0`. Phase 5 + earlier-phase forward contracts preserved (RESEARCH § 'MUST NOT touch').

    3. **All-files LOC budget audit:**
       ```
       wc -l evaluation/harness/records.py evaluation/harness/run.py evaluation/harness/compare.py evaluation/harness/freeze.py tier-4-multimodal/scripts/eval_capture.py tier-1-naive/embed_openai.py tier-2-managed/main.py tier-3-graph/rag.py tier-4-multimodal/rag.py tier-5-agentic/tools.py
       ```
       Expected (each ≤ its budget):
       - records.py ≤ 95
       - run.py ≤ 420
       - compare.py ≤ 435
       - freeze.py ≤ 105
       - eval_capture.py ≤ 245
       - tier-1 embed_openai.py ≤ 85
       - tier-2 main.py ≤ 355
       - tier-3 rag.py ≤ 178
       - tier-4 rag.py ≤ 245
       - tier-5 tools.py ≤ 178

    4. **End-to-end CLI happy path (offline-safe):**
       a. Construct a minimal results-dir under `/tmp/phase6-e2e/` with the 5-tier fixture pattern (or reuse an existing fixture from `evaluation/tests/test_eval_freeze.py:_build_fixture` via a one-liner Python invocation).
       b. Run `python -m evaluation.harness.compare --results-dir /tmp/phase6-e2e --tiers 1,2,3,4,5 --out /tmp/phase6-e2e/comparison.md`
       c. Verify `/tmp/phase6-e2e/comparison.md` contains the literal `**Embedder by tier:**` heading AND a row for each of the 5 tiers AND the per-tier `embedder: \`...\` (source: \`...\`)` lines under the capture-provenance footer.
       d. Run `python -m evaluation.harness.freeze --version 6.99-test --results-dir /tmp/phase6-e2e`
       e. Verify `/tmp/phase6-e2e/frozen/eval-numbers-v6.99-test.manifest.json` `per_tier[tier-1]` block contains both `embedder` and `embedder_source` keys.
       f. Cleanup: `rm -rf /tmp/phase6-e2e/`.

    5. **Tier-5 ROADMAP-override audit (D-ROADMAP-OVERRIDE):**
       Grep all written code for the WRONG strings — they must NOT appear:
       ```
       grep -rE 'openai-hosted-managed|openai-hosted|managed=true' \
         evaluation/harness/ tier-5-agentic/ \
         --include='*.py' | grep -v '^Binary' | grep -v 'test_' || true
       ```
       Expected: NO matches in production code (test files may reference these strings only as negative-assertion fixtures — that's allowed).

    6. **Commit:** No source diff. If steps 1-5 ALL pass, commit a verification-only commit:
       ```
       docs(06-01): full-suite gate — Phase 6 closure verified
       ```
       (commit message body summarizes test counts + LOC budgets observed). If using gsd-tools commit format, attach the artifact list explicitly.
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/rag-architecture-patterns &amp;&amp; .venv/bin/pytest evaluation/tests/ --ignore=evaluation/tests/test_eval_adapters.py --ignore=evaluation/tests/test_eval_smoke_live.py -m 'not live' 2>&amp;1 | tail -10 ; echo === byte-id === ; git diff evaluation/harness/score.py evaluation/harness/pipeline.py evaluation/harness/smoke_gate.py | wc -c ; echo === LOC === ; wc -l evaluation/harness/records.py evaluation/harness/run.py evaluation/harness/compare.py evaluation/harness/freeze.py tier-4-multimodal/scripts/eval_capture.py tier-1-naive/embed_openai.py tier-2-managed/main.py tier-3-graph/rag.py tier-4-multimodal/rag.py tier-5-agentic/tools.py ; echo === ROADMAP-override-audit === ; grep -rE 'openai-hosted-managed|openai-hosted|managed=true' evaluation/harness/ tier-5-agentic/ --include='*.py' || echo 'CLEAN'</automated>

Expected:
- Full offline test sweep: PASS (pre-existing + 7-9 new tests).
- Byte-identical guard: 0 bytes.
- All 10 LOC budgets honored.
- ROADMAP-override audit prints `CLEAN` (no production-code occurrences of the wrong strings).
  </verify>
  <done>
    Full-suite gate green: all offline tests pass, all 10 LOC budgets honored, byte-identical guards intact for score.py / pipeline.py / smoke_gate.py, ROADMAP-override audit clean (no `openai-hosted-managed` / `managed=true` in production code). One verification-only commit lands.
  </done>
</task>

</tasks>

<threat_model>
Trust boundaries: Phase 6 is pure additive instrumentation — every embedder identifier is a static literal known at code-edit time. No untrusted-input boundary, no new network surface, no new file-write surface. Threat surface area effectively zero.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I (info-disclosure) | Embedder identifier in capture JSON | accept | Identifier is a published model slug (e.g. `openai/text-embedding-3-small`) — same disclosure level as the existing `model` field already on QueryLog. No secret material. |
| T-06-02 | T (tampering) | Legacy QueryLog deserialization with absent embedder | mitigate | `Optional[str] = None` Pydantic default + defensive `.get()` in compare.py / freeze.py — legacy JSONs load cleanly without injecting "unknown" string defaults (Pitfall 2 of 06-RESEARCH.md). |
| T-06-03 | R (repudiation) | Tier-5 ROADMAP-override claim could be re-narrated | mitigate | D-ROADMAP-OVERRIDE codified in plan SUMMARY + must_haves; Task 7 step 5 grep-audit explicitly fails on `openai-hosted-managed` / `managed=true` strings; test_per_tier_embedder_constants_importable asserts Tier 5's embedder is identical to Tier 1 — three independent guards. |
| T-06-04 | D (denial) | compare.py KeyError on legacy field absence (Pitfall 6) | mitigate | All field reads in compare.py / freeze.py use `.get()` not `[]` — fall-through to `None` then to em-dash placeholder. test_compare_emits_em_dash_for_legacy_embedder is the dedicated regression. |
</threat_model>

<verification>
Phase-level checks for Plan 06-01:

1. **CAP-03 schema:** records.py:QueryLog carries Optional[embedder, embedder_source]; round-trip + legacy-load tests PASS (Task 1).
2. **CAP-03 per-tier truth:** five tier modules export the (EMBED_MODEL, EMBEDDER_SOURCE) tuple per the truth table; test_per_tier_embedder_constants_importable PASSES (Task 2).
3. **CAP-03 capture entrypoint 1 (run.py):** _capture_tier writes embedder + embedder_source per tier branch; 5 parametrized sub-cases of test_run_amain_writes_embedder_per_tier_offline PASS (Task 3).
4. **CAP-03 capture entrypoint 2 (eval_capture.py):** Tier 4 live path writes embedder fields; test_eval_capture_writes_embedder_for_tier_4 PASSES (Task 4).
5. **CAP-03 rollup:** compare.py emits per-tier embedder line + 'Embedder by tier' table block; em-dash legacy fallback works; 3 new tests PASS (Task 5).
6. **CAP-03 frozen sidecar:** freeze.py per_tier manifest carries embedder fields; test_freeze_manifest_carries_embedder_per_tier PASSES (Task 6).
7. **D-ROADMAP-OVERRIDE:** Tier 5 records (`openai/text-embedding-3-small`, `openrouter`) — verified by Task 2 test, Task 3 sub-case-5, and Task 7 step 5 grep audit.
8. **D-BACKCOMPAT:** Pure-offline TDD legacy-tolerance tests in Tasks 1, 5 PASS; legacy QueryLog JSONs load with embedder=None and render em-dash in compare/freeze outputs.
9. **D-NO-LIVE-SMOKE:** Zero `@pytest.mark.live` markers added in this plan; all new tests run offline in <2s wall time.
10. **Byte-identical forward contracts:** `git diff` of score.py / pipeline.py / smoke_gate.py == 0 bytes.
11. **LOC budgets honored:** all 10 max_lines values respected (Task 7 step 3).
12. **No regression:** full offline test sweep green (Task 7 step 1).
</verification>

<success_criteria>
1. All 7 tasks committed atomically (RED + GREEN per Tasks 1-6 = 12 commits + 1 verification-only commit for Task 7 = 13 commits total). Per-Plan 04-01 / 05-01 precedent, RED + GREEN may be combined into one commit per task IF the executor prefers — planner permits both.
2. `pytest evaluation/tests/ --ignore=evaluation/tests/test_eval_adapters.py --ignore=evaluation/tests/test_eval_smoke_live.py -m 'not live' -x` reports all pre-existing offline tests + 7-9 new tests PASS.
3. `git diff evaluation/harness/score.py evaluation/harness/pipeline.py evaluation/harness/smoke_gate.py | wc -c` reports 0.
4. All 10 LOC budgets in `max_lines` honored (Task 7 step 3).
5. CAP-03 closed: capture JSON top-level carries `embedder` + `embedder_source` for all 5 tiers; comparison.md emits embedder line + table; frozen manifest per_tier carries embedder fields.
6. D-ROADMAP-OVERRIDE locked: Tier 5 records the same embedder/source as Tier 1; grep audit (Task 7 step 5) prints CLEAN.
7. Pure-offline: zero new `@pytest.mark.live` markers; zero new API calls; zero new external dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/06-embedder-provenance-capture/06-01-SUMMARY.md` mirroring Plan 05-01-SUMMARY.md structure. Document:
- Final LOC counts for all 10 modified source files vs their budgets.
- Test counts: 7-9 new tests added by name + status; pre-existing offline test count delta.
- Confirmation that Task 7 grep-audit (step 5) returned CLEAN (no `openai-hosted-managed` / `managed=true` strings in production code).
- Snippet of the produced `**Embedder by tier:**` table from a test run, showing Tier 5's row reads `openai/text-embedding-3-small | openrouter | no` (closing D-ROADMAP-OVERRIDE).
- Any LOC compression iterations needed (per Plan 04-01 lesson — should not be needed; budgets have 4-16 LOC slack).
- Confirmation that Phase 7 (Full 5-Tier Rerun) is unblocked: the next pipeline run will populate embedder fields in every capture JSON of record.
</output>
