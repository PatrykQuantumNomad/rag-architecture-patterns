---
phase: 07-full-5-tier-rerun
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements:
  - CAP-01

files_modified:
  - tier-2-managed/.store_id  # recreated by tier-2-managed/main.py --ingest --yes (gitignored — file appears on disk; no git tracking)
  # No source-code modifications. The 6 forward-contract-guarded harness modules
  # (pipeline.py / run.py / score.py / compare.py / freeze.py / smoke_gate.py)
  # are RAW-LOCKED for this entire phase. Plan 07-01 only mutates runtime state:
  #   - tier-2-managed/.store_id (recreated, gitignored)
  #   - evaluation/results/queries/tier-{1..5}-*.json (smoke captures, gitignored)
  #   - evaluation/results/metrics/tier-{1..5}-*.json (smoke metrics, gitignored)
  #   - evaluation/results/costs/tier-{1..5}-eval-*.json (smoke capture costs, gitignored)
  #   - evaluation/results/costs/ragas-judge-tier-{1..5}-*.json (smoke judge costs, gitignored)
  # No git commits expected for this plan unless the tier-2 ingest produces a
  # provenance file the user wants to preserve. SUMMARY commit at end of plan.

cost_ack:
  total_estimate_usd: "0.50 to 1.00"
  components:
    - what: "tier-2-managed/main.py --ingest --yes (recreate .store_id via OpenAI File Search)"
      estimate_usd: "0.10 to 0.50"
      notes: "One-time. ~100-paper PDF corpus to OpenAI File Search; pricing dominated by storage tier. .store_id is gitignored so this re-runs only when missing."
    - what: "Per-tier 5q smoke × 5 tiers (capture + score)"
      estimate_usd: "0.07 to 0.10"
      notes: "T1 ~$0.001 + T2 ~$0.0005 + T3 ~$0.05 + T4 ~$0.0075 + T5 ~$0.014 + judge for all 5 ~$0.02 = ~$0.10 ceiling. Phase 5 Plan 05-02 measured T5×5q at $0.007."
    - what: "Tier 4 passthrough smoke (Open Question 1 verification)"
      estimate_usd: "0.01"
      notes: "Pipeline.py --tiers 4 --tier-4-from-cache <existing-tier-4-json> --limit 5 --yes. Re-runs against cached records; cost is judge-only since capture is passthrough."
  budget_ceiling_usd: 1.50
  ceiling_rationale: "Hard cap. If aggregate spend approaches $1.50 mid-execution, abort and surface to user. Sum well under STATE.md's $1-3 sweep envelope (Plan 07-03 burns the sweep budget separately)."

must_haves:
  truths:
    - "User can run `test -f tier-2-managed/.store_id` and see exit 0 (file present) AFTER Task 1 completes — pipeline.py's _check_prereqs no longer exits 2 on Tier 2 (Gate 3 of 07-RESEARCH.md Pre-Sweep Gating Sequence)."
    - "User can read Task 2's smoke-loop transcript and see verdict=PASS for all five tier-N smoke gates (tiers 1, 2, 3, 4, 5) — `populated/measurable >= 0.8 AND faithfulness/context_precision non-NaN on populated rows` per smoke_gate.py:147-150 (Gate 6 of 07-RESEARCH.md)."
    - "User can read Task 2's transcript and see env+storage gates 1, 2, 4, 5 reported OK ($OPENROUTER_API_KEY set, $GEMINI_API_KEY set, chroma_db/tier-1-naive/ exists, lightrag_storage/tier-3-graph/ exists, rag_anything_storage/tier-4-multimodal/ exists with the smoke graphml — current 3-paper smoke is acceptable for Plan 07-01; Plan 07-02 rebuilds)."
    - "User can run the Task 3 Tier 4 passthrough smoke (`pipeline.py --tiers 4 --tier-4-from-cache <freshest-tier-4> --limit 5 --yes`) and `jq -r '.git_sha' <newly-written-tier-4-{ts}.json>` returns the current short HEAD SHA (NOT the cached file's older git_sha) — Open Question 1 of 07-RESEARCH.md resolved with verdict 'pipeline rewrites with sweep_sha' before Plan 07-03 commits to the full sweep cost."
    - "User can run `git diff HEAD -- evaluation/harness/pipeline.py evaluation/harness/run.py evaluation/harness/score.py evaluation/harness/compare.py evaluation/harness/freeze.py evaluation/harness/smoke_gate.py | wc -c` and observe `0` — the 6 forward-contract-guarded modules are byte-identical post-Plan 07-01 (no source changes)."
  artifacts:
    - path: "tier-2-managed/.store_id"
      provides: "OpenAI File Search store ID for Tier 2 capture"
      contains: "<UUID-shape string written by tier-2-managed/main.py --ingest>"
      gitignored: true
    - path: "evaluation/results/queries/tier-{1..5}-*.json"
      provides: "Five tier-N 5q smoke QueryLogs proving each tier captures non-empty contexts at HEAD"
      gitignored: true
      min_count_per_tier: 1
    - path: "evaluation/results/metrics/tier-{1..5}-*.json"
      provides: "Five tier-N 5q smoke metrics proving each tier scores PASS at HEAD"
      gitignored: true
      min_count_per_tier: 1
  key_links:
    - from: "tier-2-managed/main.py --ingest --yes"
      to: "tier-2-managed/.store_id"
      via: "File Search store creation; UUID written to disk"
      verify_pattern: "test -f tier-2-managed/.store_id && [[ -s tier-2-managed/.store_id ]]"
    - from: "evaluation.harness.run --tiers <t> --smoke-question-ids ... --yes"
      to: "evaluation/results/queries/tier-{t}-*.json (with non-empty retrieved_contexts on populated rows)"
      via: "_capture_tier writes QueryLog per question; mtime-resolution picks freshest"
      verify_pattern: "jq '.records | length' evaluation/results/queries/tier-${t}-*.json | head -1"
    - from: "evaluation.harness.smoke_gate --tier <t>"
      to: "stdout verdict=PASS"
      via: "evaluate_smoke_from_paths reads freshest queries+metrics; thresholds in smoke_gate.py:147-150"
      verify_pattern: "python -m evaluation.harness.smoke_gate --tier <t> | grep -E 'verdict.*(PASS|pass)'"
    - from: "pipeline.py --tiers 4 --tier-4-from-cache <existing> --limit 5 --yes"
      to: "freshly-written tier-4-{ts}.json carrying current HEAD git_sha (NOT cached SHA)"
      via: "run.py:233-262 Tier 4 passthrough writes new QueryLog with git_sha=sweep_sha"
      verify_pattern: "[[ \"$(jq -r '.git_sha' <new-tier-4>)\" == \"$(git rev-parse --short HEAD)\" ]]"
---

<objective>
Pre-flight: clear all 6 environment/dependency gates, recreate the missing
`tier-2-managed/.store_id`, run a 5-question smoke through every tier, and
verify the Tier 4 passthrough SHA-rewrite assumption (07-RESEARCH.md Open
Question 1) — all BEFORE Plan 07-02 commits the user to a $15-35 / 15-25h
Tier 4 rebuild and Plan 07-03 burns the live-sweep budget.

Purpose: Three concrete gains per gate, not one. (a) `.store_id` is gitignored
and currently missing on disk — every later plan would die at Tier 2 prereq
check until this lands. (b) Per-tier smokes catch regressions cheaply (~$0.10
total) BEFORE the sweep spends $0.84+. (c) The passthrough SHA-rewrite check
is the cheapest-known way to validate that Plan 07-03's CAP-01 single-SHA
invariant is actually mechanically enforceable for Tier 4 (the ONE
special-cased tier in pipeline.py).

Output: One recreated `.store_id` on disk + 5 freshest smoke QueryLogs/metrics
under `evaluation/results/{queries,metrics}/tier-{1..5}-*.json` + a recorded
verdict on Open Question 1 written into `07-01-SUMMARY.md`. ZERO source code
changes. ZERO git commits to harness modules.

CRITICAL CONSTRAINT — RAW-LOCK: This plan MUST NOT modify any of these 6
files: `evaluation/harness/pipeline.py`, `evaluation/harness/run.py`,
`evaluation/harness/score.py`, `evaluation/harness/compare.py`,
`evaluation/harness/freeze.py`, `evaluation/harness/smoke_gate.py`. They
carry forward-contract guards from Phases 4/5/6. The Task 4 verification
gate proves byte-identical via `git diff HEAD -- ... | wc -c == 0`.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-full-5-tier-rerun/07-RESEARCH.md
@.planning/phases/02-tier-4-graphml-regeneration/02-04-SUMMARY.md
@.planning/phases/05-pipeline-driver/05-02-SUMMARY.md
@.planning/phases/06-embedder-provenance-capture/06-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. NO codebase exploration required —
     these are the locked forward-contract surfaces from Phases 1-6. -->

From evaluation/harness/run.py (LOCKED — DO NOT EDIT):
  # CLI:
  python -m evaluation.harness.run --tiers <CSV> [--smoke-question-ids <CSV>] [--limit <N>] --yes
  # Behavior:
  # - sorts tiers ascending; iterates _capture_tier per tier
  # - --smoke-question-ids filters golden_qa.json by question_id (exact match)
  # - --tier-4-from-cache <path>: required when tier 4 is in --tiers; passthrough mode
  # - exits 2 on _check_prereqs failure (e.g. tier-2-managed/.store_id missing)
  # - writes evaluation/results/queries/tier-{N}-{TS}.json + costs/tier-{N}-eval-{TS}.json
  # - threads new (Phase 6) embedder + embedder_source per tier branch into QueryLog

From evaluation/harness/score.py (LOCKED — DO NOT EDIT):
  python -m evaluation.harness.score --tiers <CSV> --yes
  # Reads freshest queries/tier-{N}-*.json per tier (mtime resolution)
  # Writes metrics/tier-{N}-{TS}.json + costs/ragas-judge-tier-{N}-{TS}.json
  # JUDGE_MAX_TOKENS=8192 (Plan 02-04 fix)
  # NaN reasons populated per row via NaNReasonTracer + _classify_post_evaluate_nan (Phase 3)

From evaluation/harness/smoke_gate.py (LOCKED — DO NOT EDIT):
  python -m evaluation.harness.smoke_gate --tier <N>
  # Reads freshest queries+metrics for tier N
  # Verdict PASS iff: populated/measurable >= 0.8 AND faithfulness/context_precision non-NaN on populated rows
  # Exit 0 = PASS; non-zero = FAIL

From evaluation/harness/pipeline.py (LOCKED — DO NOT EDIT):
  python -m evaluation.harness.pipeline --tiers <CSV> [--tier-4-from-cache <PATH>] [--limit <N>] --yes
  # Captures sweep_sha = run._git_sha() and sweep_ts = run._ts() ONCE at top
  # Threads via git_sha_override + ts_override kwargs into run.amain
  # For tier 4 passthrough: reads cache, writes NEW tier-4-{sweep_ts}.json with git_sha=sweep_sha
  # Compare stage hard-codes tiers="1,2,3,4,5" per Phase 5 D-Q4

From tier-4-multimodal/scripts/eval_capture.py (LOCKED for this phase — DO NOT EDIT):
  python tier-4-multimodal/scripts/eval_capture.py [--smoke-question-ids <CSV>] [--limit <N>] --yes
  # Standalone Tier 4 live-capture entry (D-CAPTURE-ENTRYPOINTS from Phase 6)
  # Writes evaluation/results/queries/tier-4-{TS}.json + costs/tier-4-eval-{TS}.json
  # git_sha = HEAD at time of run

From tier-2-managed/main.py (no edits expected; runtime invocation only):
  python tier-2-managed/main.py --ingest --yes
  # Creates OpenAI File Search store; writes UUID to tier-2-managed/.store_id (gitignored)
  # Idempotent — re-runs cheap if store already exists server-side; expensive on first ingest

Standard 5-question smoke set (per Phase 1 D-04 + Phase 2 Plan 02-03):
  --smoke-question-ids single-hop-001,single-hop-002,single-hop-003,multi-hop-001,multi-hop-002
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Cost-ack — authorize Plan 07-01 spend (~$0.50–$1.00, ceiling $1.50)</name>
  <decision>Authorize live-API spend for Plan 07-01 pre-flight</decision>
  <context>
    Plan 07-01 is the cheap-but-non-zero pre-flight. Total estimate $0.50–$1.00,
    hard ceiling $1.50. Breakdown:
      - Tier 2 .store_id recreation: $0.10–$0.50 (one-time OpenAI File Search ingest of ~100-paper PDF corpus; .store_id is gitignored so a fresh clone always re-runs this)
      - Per-tier 5q smoke × 5 tiers (capture + RAGAS judge): $0.07–$0.10 (Phase 5 Plan 05-02 measured T5×5q=$0.007; T1/T2 cheaper, T3/T4 pricier)
      - Tier 4 passthrough SHA verification (5q): $0.01

    Why this gate matters: every later plan in Phase 7 assumes Plan 07-01 is
    green. Plan 07-02 ($15-35 Tier 4 rebuild) and Plan 07-03 (~$1 sweep) both
    presume the prereq state is sane. If you decline, Phase 7 cannot proceed.

    Total Phase 7 cost commitment if all three plans run: ~$16-37. This task
    only commits to Plan 07-01's $0.50–$1.00 slice; Plan 07-02 and 07-03 each
    re-prompt before their bigger spends.
  </context>
  <options>
    <option id="approve">
      <name>Approve Plan 07-01 spend</name>
      <pros>
        - Phase 7 unblocks; Plan 07-02 next
        - Pre-flight catches regressions cheaply ($0.10 to expose a tier failure beats $0.84 to fail mid-sweep)
        - Open Question 1 (Tier 4 passthrough SHA) gets resolved before $15-35 rebuild commits
      </pros>
      <cons>
        - Real money spent (~$1)
        - Tier 2 ingest takes 5-15 min wall; smokes another ~10 min
      </cons>
    </option>
    <option id="defer">
      <name>Defer Phase 7 (do not authorize)</name>
      <pros>
        - Zero spend
      </pros>
      <cons>
        - Phase 7 blocked; CAP-01 stays open
        - v1.0 ship blocked (all 4 downstream phases gate on Phase 7's primary numbers)
      </cons>
    </option>
  </options>
  <resume-signal>Type `approve` to authorize Plan 07-01 spend, or `defer` to halt Phase 7.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run all 6 pre-flight gates + recreate tier-2 .store_id + 5q smoke per tier</name>
  <files>
    tier-2-managed/.store_id  (recreated by tier-2-managed/main.py --ingest --yes; gitignored)
    evaluation/results/queries/tier-{1..5}-*.json  (5 smoke captures; gitignored)
    evaluation/results/metrics/tier-{1..5}-*.json  (5 smoke metrics; gitignored)
    evaluation/results/costs/tier-{1..5}-eval-*.json  (5 capture cost JSONs; gitignored)
    evaluation/results/costs/ragas-judge-tier-{1..5}-*.json  (5 judge cost JSONs; gitignored)
  </files>
  <action>
    Execute all 6 gates from 07-RESEARCH.md "Pre-Sweep Gating Sequence" in
    order. Each gate either reports OK (continue) or remediates (only Gate 3
    has a known remediation; Gates 1, 2, 4, 5, 6, 7 are read-only checks).

    EXACT COMMAND SEQUENCE (run from repo root, in the host shell with .venv
    active; NOT in sandbox):

    # ============================================================
    # GATE 1 — Environment & API keys
    # ============================================================
    test -n "$OPENROUTER_API_KEY" && echo "OK: OPENROUTER_API_KEY set" || \
        { echo "FAIL: OPENROUTER_API_KEY missing — populate .env"; exit 1; }
    test -n "$GEMINI_API_KEY" && echo "OK: GEMINI_API_KEY set" || \
        { echo "FAIL: GEMINI_API_KEY missing — populate .env"; exit 1; }
    test -f .env && echo "OK: .env present" || \
        { echo "FAIL: .env missing"; exit 1; }

    # ============================================================
    # GATE 2 — Tier-1 storage exists
    # ============================================================
    test -d chroma_db/tier-1-naive && \
        ls chroma_db/tier-1-naive/chroma.sqlite3 >/dev/null && \
        echo "OK: chroma_db/tier-1-naive ready" || \
        { echo "FAIL: chroma_db/tier-1-naive missing — run python tier-1-naive/main.py --ingest"; exit 1; }

    # ============================================================
    # GATE 3 — Tier-2 .store_id (RECREATE if missing)
    # ============================================================
    if test -f tier-2-managed/.store_id && [[ -s tier-2-managed/.store_id ]]; then
        echo "OK: tier-2-managed/.store_id exists ($(cat tier-2-managed/.store_id))"
    else
        echo "NOTICE: tier-2-managed/.store_id missing — recreating via OpenAI File Search ingest"
        echo "        ETA: 5-15 min wall, ~\$0.10–0.50"
        python tier-2-managed/main.py --ingest --yes
        # Verify result
        test -f tier-2-managed/.store_id && [[ -s tier-2-managed/.store_id ]] || \
            { echo "FAIL: tier-2 ingest did not produce .store_id"; exit 1; }
        echo "OK: tier-2-managed/.store_id recreated"
    fi

    # ============================================================
    # GATE 4 — Tier-3 lightrag_storage exists
    # ============================================================
    test -d lightrag_storage/tier-3-graph && \
        ls lightrag_storage/tier-3-graph/graph_chunk_entity_relation.graphml >/dev/null && \
        echo "OK: lightrag_storage/tier-3-graph ready" || \
        { echo "FAIL: lightrag_storage/tier-3-graph missing"; exit 1; }

    # ============================================================
    # GATE 5 — Tier-4 storage exists (smoke-acceptable; full rebuild is Plan 07-02)
    # ============================================================
    test -d rag_anything_storage/tier-4-multimodal && \
        ls rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml >/dev/null && \
        echo "OK: rag_anything_storage/tier-4-multimodal ready (smoke-state acceptable for 07-01)" || \
        { echo "FAIL: rag_anything_storage/tier-4-multimodal missing"; exit 1; }

    # ============================================================
    # GATE 6 — Per-tier 5q smoke × 5 tiers
    # ============================================================
    SMOKE_IDS="single-hop-001,single-hop-002,single-hop-003,multi-hop-001,multi-hop-002"

    # Tiers 1, 2, 3, 5: capture via evaluation.harness.run (one tier at a time
    # so a per-tier failure surfaces immediately rather than after all four ran)
    for t in 1 2 3 5; do
        echo "=== Smoke tier ${t} ==="
        python -m evaluation.harness.run --tiers ${t} \
            --smoke-question-ids "$SMOKE_IDS" --yes || \
            { echo "FAIL: capture tier ${t}"; exit 1; }
        python -m evaluation.harness.score --tiers ${t} --yes || \
            { echo "FAIL: score tier ${t}"; exit 1; }
        python -m evaluation.harness.smoke_gate --tier ${t} || \
            { echo "FAIL: smoke gate tier ${t}"; exit 1; }
    done

    # Tier 4 separately via eval_capture.py (D-CAPTURE-ENTRYPOINTS — Tier 4
    # has TWO entry points; the live one is eval_capture.py)
    echo "=== Smoke tier 4 (eval_capture.py path) ==="
    python tier-4-multimodal/scripts/eval_capture.py \
        --smoke-question-ids "$SMOKE_IDS" --yes || \
        { echo "FAIL: tier-4 eval_capture"; exit 1; }
    python -m evaluation.harness.score --tiers 4 --yes || \
        { echo "FAIL: score tier 4"; exit 1; }
    python -m evaluation.harness.smoke_gate --tier 4 || \
        { echo "FAIL: smoke gate tier 4"; exit 1; }

    # ============================================================
    # GATE 7 — Git tree clean (recommend, not strict)
    # ============================================================
    if [[ -n "$(git status --porcelain)" ]]; then
        echo "WARN: git tree dirty — sweep_sha will reference a clean HEAD; uncommitted edits NOT in capture provenance"
        git status --short
        echo "Recommend committing or stashing before Plan 07-03."
    else
        echo "OK: git tree clean"
    fi

    echo "============================================================"
    echo "PASS: Plan 07-01 Task 2 — all 6 gates green; 5/5 tier smokes PASS"
    echo "============================================================"

    PITFALLS to avoid:
    - Pitfall 6 (07-RESEARCH.md): NEVER use bare `pytest path -v` — always `-m 'not live'`. This task does not run pytest at all (everything is direct CLI invocation), so the trap is dodged here, but Task 4 includes a regression-check pytest run that MUST use `-m 'not live'`.
    - Pitfall 1: do NOT re-order — Gate 3 must complete (or confirm `.store_id` present) BEFORE Gate 6 attempts tier 2 smoke. Pipeline.py's _check_prereqs exits 2 before any spend if `.store_id` is missing.
    - Pitfall 8: smoke output JSONs land at canonical `evaluation/results/{queries,metrics,costs}/`; do NOT pass `--results-dir` to alter location (Pitfall 5 — tracker.persist hardcodes costs dir).
    - If tier-2 ingest exits non-zero, capture stdout+stderr verbatim into 07-01-SUMMARY.md. The OpenAI File Search API has occasional transient failures; user judgment on retry-vs-investigate.
    - The smoke loop is sequential by design (per Phase 5 D-Q4 — pipeline.py is sequential and we're not changing that contract). Total wall ~10-15 min for tiers 1+2+3+5; tier 4 adds another ~5 min.
  </action>
  <verify>
    <automated>
      # All-in-one assertion: gates passed, .store_id present, 5 smoke files per tier-N exist
      test -f tier-2-managed/.store_id && [[ -s tier-2-managed/.store_id ]] && echo "GATE3_PASS"
      for t in 1 2 3 4 5; do
          Q=$(ls -t evaluation/results/queries/tier-${t}-*.json 2>/dev/null | head -1)
          M=$(ls -t evaluation/results/metrics/tier-${t}-*.json 2>/dev/null | head -1)
          test -n "$Q" && test -n "$M" || { echo "FAIL: tier-${t} smoke files missing"; exit 1; }
          # Smoke files must be from this run (mtime within last 60 min)
          # (this is a soft check — if user re-runs, mtimes update; if not, the assertion holds)
          python -m evaluation.harness.smoke_gate --tier ${t} || \
              { echo "FAIL: tier-${t} smoke gate did not PASS"; exit 1; }
      done
      echo "PASS: all 5 tier smokes verified"
    </automated>
  </verify>
  <done>
    `tier-2-managed/.store_id` present and non-empty on disk.
    Five tier-N freshest queries+metrics JSONs exist under `evaluation/results/`.
    `python -m evaluation.harness.smoke_gate --tier ${t}` exits 0 for ALL t in {1, 2, 3, 4, 5}.
    Gate 1, 2, 4, 5, 7 stdout shows "OK:" lines; Gate 3 shows either "OK:" or "OK: ... recreated".
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Tier 4 passthrough SHA-rewrite (07-RESEARCH.md Open Question 1)</name>
  <files>
    evaluation/results/queries/tier-4-{NEW_TS}.json  (~$0.01 spend; freshest tier-4 capture, gitignored)
    evaluation/results/metrics/tier-4-{NEW_TS}.json  (gitignored)
  </files>
  <action>
    07-RESEARCH.md Open Question 1: "Does pipeline.py's --tier-4-from-cache mode
    rewrite QueryLog with sweep_sha or forward cached values verbatim?"

    Run a deliberately-cheap 5q passthrough smoke through pipeline.py and verify
    the OUTPUT tier-4 JSON carries the CURRENT HEAD short SHA (the sweep_sha
    that pipeline.py captures via `run._git_sha()` at amain entry), NOT the
    cached input file's older git_sha.

    EXACT SEQUENCE:

    # 1. Identify the freshest tier-4 capture (will be Task 2's smoke tier-4 file)
    INPUT=$(ls -t evaluation/results/queries/tier-4-*.json | head -1)
    INPUT_SHA=$(jq -r '.git_sha // "null"' "$INPUT")
    echo "Input cache: $INPUT (git_sha=$INPUT_SHA)"

    # 2. Capture current HEAD SHA (this should match what pipeline.py writes
    #    when it runs run._git_sha() at amain entry)
    HEAD_SHA=$(git rev-parse --short HEAD)
    echo "Current HEAD SHA: $HEAD_SHA"

    # 3. Run passthrough smoke through pipeline.py
    #    (--tiers 4 only; --tier-4-from-cache <input>; --limit 5; --yes)
    python -m evaluation.harness.pipeline \
        --tiers 4 \
        --tier-4-from-cache "$INPUT" \
        --limit 5 \
        --yes

    # 4. Identify the OUTPUT (must be NEW file with mtime > INPUT)
    OUTPUT=$(ls -t evaluation/results/queries/tier-4-*.json | head -1)
    [[ "$OUTPUT" != "$INPUT" ]] || { echo "FAIL: pipeline did not write a new tier-4 file"; exit 1; }
    OUTPUT_SHA=$(jq -r '.git_sha // "null"' "$OUTPUT")
    echo "Output: $OUTPUT (git_sha=$OUTPUT_SHA)"

    # 5. Verdict
    if [[ "$OUTPUT_SHA" == "$HEAD_SHA" ]]; then
        echo "VERDICT: PASS — pipeline.py rewrites tier-4 git_sha to sweep_sha (=HEAD)"
        echo "         Open Question 1 resolved positively. Plan 07-03 sweep can proceed."
    elif [[ "$OUTPUT_SHA" == "$INPUT_SHA" && "$INPUT_SHA" != "null" ]]; then
        echo "VERDICT: FAIL — pipeline.py forwards cached git_sha verbatim"
        echo "         CAP-01 single-SHA invariant is BROKEN for Tier 4."
        echo "         Plan 07-03 must either (a) accept Tier 4 SHA = eval_capture SHA"
        echo "         as a documented exception, or (b) require Plan 07-03 task to fix run.py:233-262."
        echo "         Surface this finding in 07-01-SUMMARY.md and STOP for user decision."
        exit 1
    else
        echo "VERDICT: UNEXPECTED — output_sha=$OUTPUT_SHA, head_sha=$HEAD_SHA, input_sha=$INPUT_SHA"
        echo "         Investigate before proceeding to Plan 07-02."
        exit 1
    fi

    Record the verdict (PASS / FAIL / UNEXPECTED) in 07-01-SUMMARY.md.
    The verdict is load-bearing for Plan 07-03's verifier design.

    Sequencing note: this task ASSUMES Task 2's tier-4 smoke just wrote a fresh
    tier-4-{ts}.json. If the user re-runs Task 3 in isolation later, the
    `INPUT` may be from a different run — that's fine, the verdict only
    depends on output_sha vs HEAD_sha; INPUT's SHA is referenced for
    diagnostic context only.

    PITFALLS to avoid:
    - Pitfall 8: do NOT pass an OLD tier-4-*.json (pre-Plan 07-02 rebuild) as `INPUT`.
      Task 2 just wrote one against the current 3-paper smoke graph; that's
      the right input for this verification. Stale tier-4 files from Phase 2
      timestamps are deselected by `ls -t | head -1`.
    - Cost is ~$0.01 (passthrough re-runs RAGAS judge against 5 cached
      records; capture is "free" because records come from cache, not live).
  </action>
  <verify>
    <automated>
      # Find the most recent two tier-4 files; the latest is OUTPUT, second-latest INPUT
      OUTPUT=$(ls -t evaluation/results/queries/tier-4-*.json | head -1)
      OUTPUT_SHA=$(jq -r '.git_sha // "null"' "$OUTPUT")
      HEAD_SHA=$(git rev-parse --short HEAD)
      [[ "$OUTPUT_SHA" == "$HEAD_SHA" ]] || \
          { echo "FAIL: tier-4 output git_sha=$OUTPUT_SHA != HEAD=$HEAD_SHA — Open Q1 verdict FAIL"; exit 1; }
      echo "PASS: tier-4 passthrough rewrites git_sha to HEAD ($HEAD_SHA)"
    </automated>
  </verify>
  <done>
    A new tier-4-*.json exists with mtime > the input cache's mtime AND
    `jq -r '.git_sha' <new>` returns exactly `git rev-parse --short HEAD`.
    Verdict (PASS / FAIL / UNEXPECTED) recorded in 07-01-SUMMARY.md.
    If FAIL: STOP. Surface to user. Do NOT trigger Plan 07-02 cost-ack.
  </done>
</task>

<task type="auto">
  <name>Task 4: Forward-contract verification + offline regression check</name>
  <files>
    .planning/phases/07-full-5-tier-rerun/07-01-SUMMARY.md  (write per template)
  </files>
  <action>
    Two-part proof that Plan 07-01 changed nothing in the harness source tree.

    PART A — Byte-identical guard on the 6 forward-contract-locked modules:

    DIFF=$(git diff HEAD -- \
        evaluation/harness/pipeline.py \
        evaluation/harness/run.py \
        evaluation/harness/score.py \
        evaluation/harness/compare.py \
        evaluation/harness/freeze.py \
        evaluation/harness/smoke_gate.py | wc -c)
    [[ "$DIFF" -eq 0 ]] || { echo "FAIL: harness source modified ($DIFF bytes diff)"; exit 1; }
    echo "PASS: 6 harness modules byte-identical (0 bytes diff)"

    PART B — Offline pytest regression check (must use `-m 'not live'` per
    Pitfall 6 of 07-RESEARCH.md — pyproject.toml does NOT enforce
    live-deselect-by-default):

    pytest evaluation/tests/ -m 'not live' \
        --ignore=evaluation/tests/test_eval_adapters.py \
        --ignore=evaluation/tests/test_eval_smoke_live.py \
        -x

    Expected: all currently-green offline tests still pass (Phase 6 baseline
    was 126 passed; Plan 07-01 adds zero tests so the count is unchanged).

    Then write 07-01-SUMMARY.md per the standard template
    (.claude/get-shit-done/templates/summary.md) with:
    - Front-matter: phase, plan=01, status=complete, requirements=[CAP-01]
    - Executed steps + verdicts (Tasks 1, 2, 3, 4)
    - Costs incurred (capture from cost JSONs)
    - Open Question 1 verdict (PASS/FAIL/UNEXPECTED, with HEAD_SHA + OUTPUT_SHA recorded)
    - Open Question 1 implications for Plan 07-03 verifier design
    - Forward-contract verdict (0 bytes diff)
    - Pre-Plan-07-02 readiness statement: "Plan 07-02 may proceed."
      OR "Plan 07-02 BLOCKED — see Q1 verdict above."

    Commit the SUMMARY (not the smoke JSONs — those are gitignored runtime
    intermediates per project convention; .store_id is also gitignored).

    PITFALLS to avoid:
    - Do NOT commit any of `evaluation/results/queries/`, `evaluation/results/metrics/`,
      `evaluation/results/costs/` — all gitignored per project policy
      (Plan 02-04 SUMMARY documents this explicitly).
    - Do NOT amend a prior commit; create a new commit per project convention.
    - Commit message format (per project history): `docs(07-01): pre-flight verification — Open Q1 verdict {PASS|FAIL}, 5/5 tier smokes PASS`
  </action>
  <verify>
    <automated>
      # Forward-contract guard
      DIFF=$(git diff HEAD -- evaluation/harness/pipeline.py evaluation/harness/run.py evaluation/harness/score.py evaluation/harness/compare.py evaluation/harness/freeze.py evaluation/harness/smoke_gate.py | wc -c)
      [[ "$DIFF" -eq 0 ]] || { echo "FAIL: harness modified ($DIFF bytes)"; exit 1; }
      # Offline regression
      pytest evaluation/tests/ -m 'not live' --ignore=evaluation/tests/test_eval_adapters.py --ignore=evaluation/tests/test_eval_smoke_live.py -x -q || \
          { echo "FAIL: offline regression"; exit 1; }
      # SUMMARY exists
      test -f .planning/phases/07-full-5-tier-rerun/07-01-SUMMARY.md || \
          { echo "FAIL: SUMMARY not written"; exit 1; }
      echo "PASS: Task 4 forward-contract + regression + SUMMARY"
    </automated>
  </verify>
  <done>
    `git diff HEAD -- evaluation/harness/{pipeline,run,score,compare,freeze,smoke_gate}.py | wc -c` returns `0`.
    `pytest evaluation/tests/ -m 'not live' --ignore=... -x` exits 0.
    `.planning/phases/07-full-5-tier-rerun/07-01-SUMMARY.md` exists, has the
    Open Question 1 verdict, and is committed.
  </done>
</task>

</tasks>

<verification>
Phase-7-Plan-01 acceptance:
1. `tier-2-managed/.store_id` exists, is non-empty, points to a valid OpenAI File Search store (post-Task 2).
2. All 5 tier 5q smokes PASS (post-Task 2).
3. Open Question 1 verdict is PASS (Tier 4 passthrough rewrites git_sha to HEAD) — post-Task 3.
4. Forward-contract guard: 0 bytes diff on the 6 locked harness modules (post-Task 4).
5. Offline pytest baseline: ≥126 passed (Phase 6 baseline) — no regressions.
6. SUMMARY committed to git with verdict statement enabling Plan 07-02.
</verification>

<success_criteria>
Plan 07-01 is COMPLETE when:
- [x] Cost-ack approved (Task 1)
- [x] All 6 pre-flight gates green; .store_id recreated if needed; 5/5 tier 5q smokes PASS (Task 2)
- [x] Tier 4 passthrough SHA verdict = PASS (Task 3) — written to SUMMARY
- [x] 6 harness modules byte-identical; offline regression baseline preserved (Task 4)
- [x] 07-01-SUMMARY.md committed
</success_criteria>

<output>
After completion, create `.planning/phases/07-full-5-tier-rerun/07-01-SUMMARY.md`.

Per project commit convention, the SUMMARY commit is the only commit Plan
07-01 produces (no source changes). All smoke JSONs / .store_id are
gitignored runtime state; do not attempt to add them to the index.

Format the SUMMARY's front-matter status field as `complete` only if all
four task verifies pass; otherwise `blocked` with the blocker captured in
the body.
</output>
