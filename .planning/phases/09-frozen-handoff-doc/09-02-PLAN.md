---
phase: 09-frozen-handoff-doc
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - evaluation/results/frozen/eval-numbers-v1.0.md
  - evaluation/results/frozen/eval-numbers-v1.0.manifest.json
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-05
user_setup: []

must_haves:
  truths:
    - "A frozen v1.0 markdown exists under `evaluation/results/frozen/` and contains the rollup, per-class breakdown, provenance/disclosures, embedder table, and the inlined Phase 8 multi-judge evidence."
    - "A sidecar manifest exists beside the frozen markdown, and the freeze tool refuses to clobber by default."
  artifacts:
    - path: "evaluation/results/frozen/eval-numbers-v1.0.md"
      provides: "Frozen copy-pasteable handoff doc"
      min_lines: 120
    - path: "evaluation/results/frozen/eval-numbers-v1.0.manifest.json"
      provides: "Provenance manifest for v1.0 frozen doc"
      contains: "\"git_sha\""
  key_links:
    - from: "evaluation/results/frozen/eval-numbers-v1.0.md"
      to: "evaluation/results/comparison.md"
      via: "byte-for-byte freeze copy"
      pattern: "Tier Rollup"
---

<objective>
Freeze the finalized upstream markdown into `evaluation/results/frozen/eval-numbers-v1.0.md` using the existing freeze tool, producing a sidecar manifest and verifying immutability semantics (refuse-to-clobber) and required content presence.

Purpose: Produce the single immutable artifact for blog handoff with full provenance and no external lookups.
Output: Frozen markdown + manifest for v1.0, validated by deterministic content checks.
</objective>

<execution_context>
@/Users/patrykattc/work/git/rag-architecture-patterns/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/rag-architecture-patterns/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-frozen-handoff-doc/09-RESEARCH.md
@.planning/phases/09-frozen-handoff-doc/09-VALIDATION.md
@evaluation/results/comparison.md

Constraint reminders:
- Do NOT rebuild Tier 4 between now and freeze (STATE.md reminder).
- Do NOT rerun expensive evals; Phase 9 is documentation composition + freeze only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate frozen v1.0 markdown + sidecar manifest</name>
  <read_first>
    - evaluation/harness/freeze.py
    - evaluation/results/comparison.md
    - .planning/STATE.md
  </read_first>
  <files>
    evaluation/results/frozen/eval-numbers-v1.0.md
    evaluation/results/frozen/eval-numbers-v1.0.manifest.json
  </files>
  <action>
    Run the freeze tool to generate the frozen v1.0 artifacts from the current `evaluation/results/comparison.md`:
    - `python -m evaluation.harness.freeze --version 1.0`

    Important handling:
    - If the command succeeds and writes files, proceed.
    - If it refuses to clobber because the frozen file already exists, do NOT overwrite by default. Instead, verify that the existing frozen doc already contains the required sections (Task 2). Only use `--force` if you are explicitly correcting a previously-frozen file that is missing required Phase 9 content, and note this in the SUMMARY as an immutability exception.
  </action>
  <acceptance_criteria>
    - `evaluation/results/frozen/eval-numbers-v1.0.md` exists.
    - `evaluation/results/frozen/eval-numbers-v1.0.manifest.json` exists.
    - The frozen markdown includes the “Multi-judge spot-check (Phase 8)” section (meaning the upstream `comparison.md` was finalized before freeze).
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; import json; md=Path('evaluation/results/frozen/eval-numbers-v1.0.md'); mf=Path('evaluation/results/frozen/eval-numbers-v1.0.manifest.json'); assert md.exists(); assert mf.exists(); s=md.read_text(encoding='utf-8'); assert 'Tier Rollup' in s; assert 'Per-Question-Class Rollup' in s; assert 'Provenance' in s or 'Provenance & Honest Disclosures' in s; assert 'Embedder by tier' in s; assert 'Multi-judge spot-check (Phase 8)' in s; j=json.loads(mf.read_text(encoding='utf-8')); assert 'git_sha' in j and 'frozen_at' in j"</automated>
  </verify>
  <done>Frozen v1.0 markdown and manifest exist and include the inlined Phase 8 evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Validate content completeness for DOC-01..DOC-03</name>
  <read_first>
    - evaluation/results/frozen/eval-numbers-v1.0.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <files>evaluation/results/frozen/eval-numbers-v1.0.md</files>
  <action>
    Perform deterministic content checks to ensure the frozen doc contains:
    - Tier rollup table (DOC-01)
    - Per-question-class rollup table (DOC-02)
    - Provenance lines including capture timestamp, generation model, git SHA, judge model (DOC-03)
    - Embedder-by-tier table (supports DOC-04/DOC-05 completeness)
    - The Phase 8 multi-judge spot-check section (supports DOC-04/DOC-05)
  </action>
  <acceptance_criteria>
    - Frozen doc contains the Tier Rollup section and a markdown table with tier rows.
    - Frozen doc contains the Per-Question-Class Rollup section and its table.
    - Frozen doc contains per-tier capture provenance lines referencing capture timestamp + model + git sha.
    - Frozen doc contains “Judge LLM” and “Judge embedder”.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; s=Path('evaluation/results/frozen/eval-numbers-v1.0.md').read_text(encoding='utf-8'); required=['## Tier Rollup','## Per-Question-Class Rollup','**Judge LLM:**','**Judge embedder:**','**Capture provenance per tier:**','**Embedder by tier:**','## Multi-judge spot-check (Phase 8)']; missing=[x for x in required if x not in s]; assert not missing, f'Missing: {missing}'"</automated>
  </verify>
  <done>Frozen doc contains all required tables and provenance sections for DOC-01..DOC-03.</done>
</task>

<task type="auto">
  <name>Task 3: Verify freeze immutability (refuse-to-clobber)</name>
  <read_first>
    - evaluation/harness/freeze.py
    - evaluation/results/frozen/eval-numbers-v1.0.md
  </read_first>
  <files>evaluation/results/frozen/eval-numbers-v1.0.md</files>
  <action>
    Re-run freeze without `--force` and confirm it refuses to clobber (immutability guarantee):
    - `python -m evaluation.harness.freeze --version 1.0`
    Expect a non-zero exit and a message containing:
    - `already frozen — bump version or pass --force`
  </action>
  <acceptance_criteria>
    - The second freeze invocation exits non-zero.
    - Output contains the refuse-to-clobber wording (or the exact equivalent used by freeze.py tests).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import subprocess, sys; p=subprocess.run([sys.executable,'-m','evaluation.harness.freeze','--version','1.0'], capture_output=True, text=True); out=(p.stdout+p.stderr); assert p.returncode!=0; assert 'already frozen' in out and '--force' in out"</automated>
  </verify>
  <done>Immutability semantics verified: freeze refuses to overwrite v1.0 without `--force`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream markdown → frozen artifact | Risk of freezing incomplete content or freezing secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-09-03 | T | freeze process | mitigate | Freeze only after upstream `comparison.md` is finalized and contains Phase 8 section; verify required sections exist in frozen doc |
| T-09-04 | I | frozen markdown | mitigate | Content checks + explicit forbidden-token checks in Plan 09-01; keep Phase 9 limited to docs composition |
</threat_model>

<verification>
- Run Task 1 content check.
- Run Task 3 immutability check.
- Do NOT run live tests or expensive eval commands.
</verification>

<success_criteria>
- Frozen v1.0 markdown + manifest exist and are self-contained for blog handoff.
- Freeze refuses to clobber v1.0 by default (immutability verified).
</success_criteria>

<output>
After completion, create `.planning/phases/09-frozen-handoff-doc/09-02-SUMMARY.md`
</output>

