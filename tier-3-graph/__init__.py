"""Tier 3 (Graph RAG / LightRAG) package — importable as ``tier_3_graph``.

The on-disk directory uses hyphens (``tier-3-graph/``) for human readability;
``tier_3_graph/__init__.py`` is a sibling shim that re-exports modules from
this directory under the dotted, importable name. See the shim for details.
"""
