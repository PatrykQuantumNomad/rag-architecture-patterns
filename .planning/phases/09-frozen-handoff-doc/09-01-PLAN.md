---
phase: 09-frozen-handoff-doc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evaluation/results/comparison.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DOC-04
  - DOC-05
user_setup: []

must_haves:
  truths:
    - "The upstream markdown (`evaluation/results/comparison.md`) is self-contained: it includes the Phase 8 multi-judge delta evidence and all required disclaimers without linking to missing/ignored artifacts."
    - "The upstream markdown contains no secrets (no env var dumps, API keys, bearer tokens)."
  artifacts:
    - path: "evaluation/results/comparison.md"
      provides: "All tables + disclaimers + multi-judge delta section to be frozen"
      min_lines: 80
  key_links:
    - from: "evaluation/results/comparison.md"
      to: ".planning/phases/08-multi-judge-spot-check/08-02-SUMMARY.md"
      via: "inlined delta table + cost + provenance"
      pattern: "Multi-judge spot-check"
---

<objective>
Make `evaluation/results/comparison.md` the final, self-contained source-of-truth for the frozen handoff doc by inlining Phase 8 multi-judge delta evidence and tightening the honest-disclaimer section to reflect known limitations (including the LiteLLM judge-cost undercount).

Purpose: DOC-05 requires a copy/pasteable doc with no external lookups; Phase 9 constraints prohibit rerunning expensive evals and forbid rebuilding Tier 4.
Output: Updated `evaluation/results/comparison.md` ready for freezing to v1.0; ROADMAP Phase 9 plan list updated.
</objective>

<execution_context>
@/Users/patrykattc/work/git/rag-architecture-patterns/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/rag-architecture-patterns/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-frozen-handoff-doc/09-RESEARCH.md
@.planning/phases/09-frozen-handoff-doc/09-VALIDATION.md
@.planning/phases/08-multi-judge-spot-check/08-02-SUMMARY.md
@evaluation/results/comparison.md

Phase constraints to honor:
- Phase 9 is composition + freeze only; do NOT rerun expensive evals.
- DO NOT rebuild Tier 4 between now and the Phase 9 freeze (STATE.md reminder).
- Prefer inlining Phase 8 multi-judge delta evidence into the frozen markdown rather than linking to missing JSON artifacts.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline Phase 8 multi-judge delta evidence</name>
  <read_first>
    - evaluation/results/comparison.md
    - .planning/phases/08-multi-judge-spot-check/08-02-SUMMARY.md
    - .planning/STATE.md
  </read_first>
  <files>evaluation/results/comparison.md</files>
  <action>
    Append a new section to `evaluation/results/comparison.md` titled exactly:
    - `## Multi-judge spot-check (Phase 8)`

    Under it, inline **verbatim numeric evidence** from Phase 8 Plan 08-02 SUMMARY (do not link to JSON paths that may not exist in-repo):
    - Effective spot-check cost: `$0.12225` (explain that raw ledger shows `$0.00` due to LiteLLM usage parsing gap; estimator used).
    - Dual-SHA provenance: `source_capture_git_sha=75f6f1b` and `spotcheck_run_git_sha=3f37e4b` (and state the meaning: source-of-record is the capture SHA).
    - Secondary judge: `anthropic/claude-haiku-4.5`, `max_tokens=8192`.
    - Cells: `15` (5 question IDs × 3 tiers), and `0/15 secondary nan_reasons`.

    Include the per-tier mean delta table (faithfulness / answer_relevancy / context_precision) with these rows exactly:
    - tier-1: Δ_F=-0.0347, Δ_AR=-0.2115, Δ_CP=-0.2500, n_skipped_due_to_nan=0
    - tier-4: Δ_F=+0.0097, Δ_AR=+0.2426, Δ_CP=-0.2000, n_skipped_due_to_nan=0
    - tier-5: Δ_F=-0.1643, Δ_AR=+0.2003, Δ_CP=-0.0667, n_skipped_due_to_nan=0
    - overall: Δ_F=-0.0631, Δ_AR=+0.0771, Δ_CP=-0.1722, n_skipped_due_to_nan=0/15

    Add a one-paragraph “How to interpret” note:
    - “mean_delta = secondary − primary; negative means the secondary judge scored lower”.
  </action>
  <acceptance_criteria>
    - `evaluation/results/comparison.md` contains the header `## Multi-judge spot-check (Phase 8)`.
    - The delta table contains all 4 required rows (tier-1, tier-4, tier-5, overall) and all three metric columns.
    - The section explicitly states the effective cost `$0.12225`, the dual SHAs (`75f6f1b` and `3f37e4b`), secondary judge model, and `0/15` secondary nan reasons.
    - The section contains no links to tmp paths or non-committed JSON artifacts (no `tmp_path`, no `/tmp/`, no `multi-judge-spotcheck-*.json` links).
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; s=Path('evaluation/results/comparison.md').read_text(encoding='utf-8'); assert '## Multi-judge spot-check (Phase 8)' in s; assert '$0.12225' in s; assert '75f6f1b' in s and '3f37e4b' in s; assert 'claude-haiku-4.5' in s; assert '0/15' in s; assert 'tier-1' in s and 'tier-4' in s and 'tier-5' in s and 'overall' in s; assert 'Δ Faithfulness' in s and 'Δ Answer Relevancy' in s and 'Δ Context Precision' in s; assert 'tmp_path' not in s and '/tmp/' not in s"</automated>
  </verify>
  <done>Phase 8 delta evidence is inlined in comparison.md with exact numeric values and provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten honest-disclaimer text to match known constraints</name>
  <read_first>
    - evaluation/results/comparison.md
    - .planning/STATE.md
    - .planning/phases/09-frozen-handoff-doc/09-RESEARCH.md
  </read_first>
  <files>evaluation/results/comparison.md</files>
  <action>
    Update (or append to) the `## Provenance & Honest Disclosures` section so that it explicitly includes:
    - The Phase 9 freeze constraint: “Do not rebuild Tier 4 between the sweep and the freeze” (as a process note, not an action to take now).
    - The self-grading bias acknowledgement upgraded from vague wording to a concrete citation: mention the Phase 8 multi-judge spot-check and point the reader to the section you added (no external link required).
    - A judge-cost ledger note: “RAGAS judge ledgers may underreport spend (known LiteLLM usage parsing gap); treat capture cost as reliable; treat judge cost as lower-bound unless otherwise stated.”

    Do NOT:
    - Add any secrets, environment variables, or command outputs containing keys.
    - Add links to missing artifacts; keep it self-contained.
  </action>
  <acceptance_criteria>
    - The honest disclaimers mention the Phase 8 spot-check section by name.
    - The honest disclaimers mention the judge-cost undercount caveat (LiteLLM usage parsing gap).
    - The document remains self-contained and does not reference missing JSON artifacts.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; s=Path('evaluation/results/comparison.md').read_text(encoding='utf-8'); assert 'Honest disclaimers' in s; assert 'Multi-judge spot-check (Phase 8)' in s; assert 'LiteLLM' in s or 'usage' in s and 'under' in s; forbidden=['OPENROUTER_API_KEY','Bearer ','sk-','api_key=','.env']; assert not any(tok in s for tok in forbidden)"</automated>
  </verify>
  <done>`comparison.md` disclaimers reflect Phase 9 constraints and known ledger caveats without leaking secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP Phase 9 plan list</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    In `.planning/ROADMAP.md`, under “### Phase 9: Frozen Handoff Doc”:
    - Set `**Plans:** 2 plans`
    - Add the plan list entries for Phase 9:
      - `- [ ] 09-01-PLAN.md — Inline Phase 8 delta + finalize comparison.md disclaimers`
      - `- [ ] 09-02-PLAN.md — Freeze v1.0 markdown + manifest + immutability checks`
  </action>
  <acceptance_criteria>
    - ROADMAP Phase 9 shows `**Plans:** 2 plans`.
    - The two plan bullets exist with the exact filenames above.
  </acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; s=Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); assert '### Phase 9: Frozen Handoff Doc' in s; assert '**Plans**: 2 plans' in s; assert '09-01-PLAN.md — Inline Phase 8 delta + finalize comparison.md disclaimers' in s; assert '09-02-PLAN.md — Freeze v1.0 markdown + manifest + immutability checks' in s"</automated>
  </verify>
  <done>ROADMAP Phase 9 accurately references the two Phase 9 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → frozen markdown | Risk of accidentally committing secrets into a long-lived, copy-pasted artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-09-01 | I | `evaluation/results/comparison.md` | mitigate | Add automated “secret token substring” checks in `<verify>` and keep edits limited to numeric evidence + disclaimers only |
| T-09-02 | R | Phase 8 delta evidence | mitigate | Inline exact numeric values + dual-SHA provenance; avoid linking to missing JSON artifacts |
</threat_model>

<verification>
- Run the automated content checks in each task.
- Do NOT run any live or expensive evaluation commands in Phase 9.
</verification>

<success_criteria>
- `evaluation/results/comparison.md` contains the Phase 8 multi-judge delta section with exact numbers and provenance.
- `evaluation/results/comparison.md` contains updated honest disclaimers reflecting known constraints and caveats.
- ROADMAP Phase 9 shows 2 plans and lists both plan filenames.
</success_criteria>

<output>
After completion, create `.planning/phases/09-frozen-handoff-doc/09-01-SUMMARY.md`
</output>

