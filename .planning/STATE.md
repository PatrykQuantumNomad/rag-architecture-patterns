---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: executing
stopped_at: "Phase 7 COMPLETE + VERIFIED (3/3 plans). gsd-verifier verdict 2026-05-07: status=passed, score=5/5 (SC-1/2/3/4 auto-verified; SC-5 user-confirmed via dashboard). CAP-01 closed at sweep_sha=`75f6f1b` on date 2026-05-07. Phase 7 cumulative spend $25.32 across 3 plans ($0.036 Plan 07-01 pre-flight + $24.85 Plan 07-02 host rebuild + $0.44 Plan 07-03 sweep) vs combined ceiling $54.50. Tier 4 graph rebuilt to 28597 nodes / 80419 edges from 79 papers; T4 NaN 2/30 (Phase 2 fix sticking); T5 NaN 0/30 (Phase 1 D-FORMAT sticking). Forward-contract guard intact end-to-end across all 3 plans (0 bytes diff vs pre-Phase-7 baseline 03f9ce1 on 6 harness modules). 07-VERIFICATION.md committed at .planning/phases/07-full-5-tier-rerun/07-VERIFICATION.md. Phase 8 (Multi-Judge Spot-Check, requirement CAP-02) ready to plan — primary 5-tier numbers exist for delta comparison. Phase 9 freeze prerequisite reminder still in force: DO NOT rebuild Tier 4 between this point and Phase 9 freeze (Pitfall 3 — graphml drift would invalidate the manifest citation locked at eb77cf8)."
last_updated: "2026-05-07T12:30:00.000Z"
last_activity: 2026-05-07
progress:
  total_phases: 9
  completed_phases: 7
  total_plans: 17
  completed_plans: 17
  percent: 78
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-04)

**Core value:** Produce reproducible, honest numbers for the blog — every claim backed by a captured run with full provenance.
**Current focus:** Phase 7 COMPLETE + VERIFIED (3/3 plans, CAP-01 closed; gsd-verifier verdict passed 5/5 — SC-5 user-confirmed via dashboard 2026-05-07). Next phase: Phase 8 (Multi-Judge Spot-Check, requirement CAP-02 — re-score 5q × 3 tiers with non-Gemini judge to bound family bias).

## Current Position

Phase: 7 of 9 (Full 5-Tier Rerun) — EXECUTION COMPLETE (3/3 plans complete). Plan 07-03 (Full 5-Tier Sweep — CAP-01 Closure) ran the full sweep at sweep_sha=`75f6f1b`; all 5 ROADMAP success criteria PASS (single-SHA invariant verified across 5 freshest tier-N queries JSONs; Tier 4 NaN 2/30 vs 30/30 baseline; Tier 5 NaN 0/30 vs 30/30 baseline; total spend $0.44 vs $3.00 ceiling; comparison.md regenerated with 5-row rollup + 15-row per-class + 5-row Embedder-by-tier table); forward-contract guard intact (0 bytes diff across 9 RAW-LOCKed source files); offline pytest baseline 116/116 PASS preserved. Awaiting `/gsd-verify-phase` for Phase 7 closure.
Plan: 3 of 3 complete in Phase 7. Plan 07-03 (Full 5-Tier Sweep — CAP-01 Closure) — wall ~75 min (Step 1 ~6 min eval_capture.py + Step 2 ~64 min pipeline.py incl. one transient retry + Task 4 verifier + SUMMARY). ONE source-tracked commit (this docs commit carrying SUMMARY + VERIFY.txt). Sweep artifacts (5 fresh queries+metrics+costs JSONs + regenerated comparison.md) gitignored per project policy; SUMMARY rolls all key numbers into markdown body for provenance equivalence. Forward-contract guard: 0 bytes diff vs HEAD (75f6f1b — Plan 07-02 baseline) across 9 RAW-LOCKed source files (pipeline.py / run.py / score.py / compare.py / freeze.py / smoke_gate.py + ingest_from_mineru.py / log_graph_stats.py / eval_capture.py). Offline pytest baseline preserved at 116/116 PASS with `-m 'not live'` (no regressions vs Plan 07-02 baseline). 2 Rule-3 deviations: (a) Embedder-by-tier verifier regex was wrong (compare.py emits `**Embedder by tier:**` markdown bold, not `## Embedder by tier` heading); adjusted regex; table contents correct end-to-end. (b) First pipeline.py invocation crashed at Tier 2 query 25/30 with transient Gemini 429 RESOURCE_EXHAUSTED on embedding API; reran without committing (HEAD stayed at 75f6f1b → sweep_sha preserved per plan's load-bearing invariant 2); second attempt completed all 5 tiers. Total Plan 07-03 spend $0.44 vs $3.00 ceiling (14.6% of ceiling).
Status: Phase 7 complete + verified (3/3 plans; gsd-verifier verdict passed 5/5 with SC-5 user-confirmed). Phase 8 ready to plan.
Last activity: 2026-05-07

Progress: [███████░░] 78%  (7 of 9 phases complete; 17 of 17 currently-defined plans complete; Phase 8 next)
Phase 7 progress: 3/3 plans complete. Plan 07-01: ZERO source commits per RAW-LOCK; 1 SUMMARY commit. Plan 07-02: 3 commits (manifest + SUMMARY + self-check append); host rebuild 79/79 papers (28597 / 80419 graphml); 5q smoke gate verdict=PASS for tier 4. Plan 07-03: 1 commit (SUMMARY + VERIFY.txt); full sweep at sweep_sha=75f6f1b; CAP-01 closed; all 5 ROADMAP SCs PASS; forward-contract guard intact; offline test baseline 116/116 preserved.

## Performance Metrics

**Velocity:**

- Total plans completed: 16
- Average duration: ~73 min mean (skewed by Plan 07-02's 13h55m host ingest); ~21 min median (~314 min orchestrator-side / 15 prior plans + ~5 min orchestrator-side for Plan 07-02 Tasks 3-5; the 13h55m ingest wall was unattended host-shell work)
- Total execution time: ~19.2 hours (5.2h orchestrator + ~14h Plan 07-02 host ingest)

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 01-tier-5-adapter-fix | 3 | ~22 min | ~7 min |
| 02-tier-4-graphml-regeneration | 4 | ~142 min (~50 min Plan 02-01 + ~50 min Plan 02-02 host MineRU + ~30 min Plan 02-03 + ~12 min Plan 02-04) | ~36 min |
| 03-nan-reason-instrumentation | 3 of 3 | ~27 min (~5 min Plan 03-01 + ~12 min Plan 03-02 + ~10 min Plan 03-03 with 78s live test wall) | ~9 min |
| 04-freeze-tool | 1 of 1 | ~18 min (TDD red->green, pure-offline; 3 LOC-budget compression iterations) | ~18 min |
| 05-pipeline-driver | 2 of 2 | ~37 min (~12 min Plan 05-01 pure-offline TDD red->green + ~25 min Plan 05-02 live-smoke-backstop with 222s+158s dual live walls + Rule-1 monkeypatch fix between invocations) | ~18 min |
| 06-embedder-provenance-capture | 1 of 1 | ~18 min (pure-offline TDD red->green; 6 RED+GREEN pairs across 15 files / 10 source + 5 test; sketch-LOC validation per Plan 04-01 lesson held — ZERO compression iterations, third consecutive plan) | ~18 min |
| 07-full-5-tier-rerun | 3 of 3 | ~50 min Plan 07-01 + Plan 07-02 13h55m host ingest unattended + ~5 min orchestrator + Plan 07-03 ~75 min orchestrator (Step 1 6min eval_capture.py + Step 2 64min pipeline.py incl. 1 retry + Task 4 verifier + SUMMARY) | ~62 min orchestrator avg / Plan 07-02 host wall not orchestrator time |

**Recent Trend:**

- Last 16 plans: 01-01 (8 min), 01-03 (4 min), 01-02 (~10 min), 02-01 (~50 min), 02-02 (~50 min host MineRU), 02-03 (~30 min), 02-04 (~12 min), 03-01 (~5 min), 03-02 (~12 min), 03-03 (~10 min), 04-01 (~18 min), 05-01 (~12 min), 05-02 (~25 min), 06-01 (~18 min), 07-01 (~50 min), 07-02 (13h55m host ingest + ~5 min orchestrator), 07-03 (~75 min orchestrator: Step 1 ~6 min eval_capture.py + Step 2 ~64 min pipeline.py incl. 1 transient Gemini-429 retry + ~5 min verifier+SUMMARY; total cost $0.44 vs $3.00 ceiling; ZERO source commits per RAW-LOCK; CAP-01 closed)
- Trend: live-ingest plans are 5-10× pure-code plans; smoke verification plans (Plan 02-03) sit in between; gap-closure score-only plans (Plan 02-04) are 2-3× pure-code plans; pure-offline TDD-only plans (Plan 03-01) are the cheapest at ~5 min wall; live-smoke-backstop plans (Plan 03-03) are 2× pure-offline (~10 min — code is small, but live test wall is the dominant cost); LOC-budget-tight pure-offline plans (Plan 04-01) sit at ~18 min; LOC-budget-roomy pure-offline plans (Plan 05-01 with 14% buffer) hit ~12 min; live-composition-backstop plans (Plan 05-02) hit ~25 min; pure-offline TDD multi-pair plans (Plan 06-01) sit at ~18 min when sketch-LOC validation eliminates compression iterations; **pre-flight verification plans (Plan 07-01) hit ~50 min when external-service ingest dominates wall** — pattern: when a plan's "cheap" component is dwarfed by an opaque-third-party-API wall, plan-side cost estimates remain accurate but wall estimates can swing 2-3× depending on indexing-tier behavior. **Full-sweep plans (Plan 07-03) hit ~75 min wall when 5 tiers × 30 questions × (capture + RAGAS judge) is dominated by RAGAS judge wall (~10-12 min/tier × 5 tiers = ~55 min judging vs ~9 min capture + ~5 min compare/verify); transient cloud-quota retries add 5-10 min recovery without breaking the single-SHA invariant if no commits intervene.** Phase 7 closing pattern (Plan 07-03): ZERO source commits per RAW-LOCK; sweep artifacts gitignored; SUMMARY rolls all numbers into markdown body for provenance equivalence; verifier transcript committed alongside SUMMARY for audit trail.

*Updated after each plan completion*
| Phase 03 P02 | 12min | 2 tasks | 3 files |
| Phase 03 P03 | 10min | 2 tasks | 1 source + 4 docs |
| Phase 04 P01 | 18min | 2 tasks (TDD RED + GREEN) | 4 files (freeze.py, test_eval_freeze.py, .gitkeep, deferred-items.md) |
| Phase 05 P01 | 12min | 2 tasks (TDD RED + GREEN) | 4 files (pipeline.py, test_eval_pipeline.py, run.py kwarg edit, test_eval_run.py stub-fix) |
| Phase 05 P02 | 25min | 2 tasks (cost-ack checkpoint + live test append-and-invoke) | 1 source + 4 docs (test_eval_pipeline.py +97 lines with Rule-1 monkeypatch fix; SUMMARY + STATE + ROADMAP + REQUIREMENTS) |
| Phase 06 P01 | 18min | 7 tasks (6 TDD RED+GREEN pairs + 1 verification gate) | 15 files (10 source + 5 test) — records.py / run.py / compare.py / freeze.py / tier-4 eval_capture.py + 5 tier modules + 5 test files |
| Phase 07 P01 | 50min | 4 tasks (1 cost-ack checkpoint + 3 auto: 6 gates+5q smoke per tier / Open-Q1 passthrough verify / forward-contract+offline-regression+SUMMARY) | 1 doc (07-01-SUMMARY.md) + 1 runtime artifact (.store_id, gitignored) — ZERO source commits per RAW-LOCK |
| Phase 07 P02 | 13h55m host ingest + ~5min orchestrator | 5 tasks (1 cost-ack checkpoint + 1 host-action checkpoint + 3 auto: manifest write+commit / 5q smoke gate / forward-contract+offline-regression+SUMMARY) | 1 manifest (`evaluation/results/diagnostics/tier-4-graph-stats-2026-05-07T10_31_52Z.json`) + 1 SUMMARY (07-02-SUMMARY.md carrying `tier_4_full_rebuild_manifest:` cross-plan data contract) + 1 self-check append — ZERO source commits per RAW-LOCK; Tier-4 graphml + kv_cache rebuilt as gitignored runtime state |
| Phase 07 P03 | ~75min (Step 1 ~6min eval_capture.py + Step 2 ~64min pipeline.py incl. 1 transient retry + ~5min verifier+SUMMARY) | 6 tasks (1 cost-ack pre-approved by orchestrator + 4 auto: Step 1 Tier-4 30q live capture / Step 2 pipeline sweep / Step 3 verifier-transcript / Step 5 forward-contract+offline-regression+SUMMARY + 1 human-verify auto-approved per yolo+auto mode) | 1 SUMMARY (07-03-SUMMARY.md) + 1 verifier transcript (07-03-VERIFY.txt) — ZERO source commits per RAW-LOCK; sweep artifacts (5 queries+metrics+costs JSONs + regenerated comparison.md) gitignored per project policy. Total spend $0.44 / $3.00 ceiling (14.6%). Tier 4 NaN 2/30 (vs 30/30 baseline). Tier 5 NaN 0/30 (PERFECT vs 30/30 baseline). |

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- v1.0 scope: fix-and-ship the existing 5 tiers; no new architectures, no new questions
- Multi-judge spot-check (CAP-02) is in v1.0 scope as *measurement* (5q × 3 tiers); full mitigation deferred to v1.1
- Single eval-date for all 5 tiers; budget ~$1-3 per full rerun, 2-3 reruns total
- Tier 4 MineRU ingest must run outside the sandbox (Phase 139 evidence)
- Tier 5 adapter reads `item.output` not `item.raw_item` (Pitfall 1 of 132-RESEARCH HARD invariant) — `raw_item` is a stringified Responses-API payload
- Tier 5 contexts deduped first-occurrence-wins on `(paper_id, page)` for chunks and `(paper_id)` for metadata abstracts
- Tier 5 tracing toggle gated on `RAG_DEBUG_TIER5_TRACING` env var (default disabled — byte-identical behavior; opt-in for OpenInference fallback)
- `[debug-tier5]` extra is opt-in only (CLEANUP-02 of REQUIREMENTS.md v1.1) — default `uv sync` does NOT install OpenInference / Phoenix / OpenTelemetry; `uv sync --extra debug-tier5` activates the diagnostic stack on demand (D-09)
- Fallback log persistence (`FallbackLog`) reuses `model_dump_json(indent=2)` and `_git_sha` / `_ts` / `_ts_for_filename` helpers from `evaluation.harness.run` — single source of truth for SHA + ISO 8601 Z conventions
- Fallback runbook encodes D-06 instrument-first ordering verbatim (Phoenix spans first, STACK.md mutations second); `bump_openai_agents` is LAST RESORT requiring user authorization via `checkpoint:decision`
- Live smoke gate PASS verdict on 2026-05-04 — 5/5 measurable populated, ratio 1.00, all RAGAS metrics non-NaN; Plan 01-03 fallback runbook remains dormant scaffolding for Phase 7 to read
- Tier 5 retrieved_contexts populated as expected — every entry in all 5 records starts with `[paper_id=`, n_ctx range 4-6, no Python repr leakage (Pitfall 1 of 132-RESEARCH guard intact end-to-end against live API)
- Capture cost $0.0048 + judge cost $0.00 on the 5-question smoke (well under the $0.05/run Pitfall-7 cost guard); smoke is cheap to re-run for any future regression check
- **Phase 2 Plan 02-01 Task 2b ran smoke-only (3 of 75 papers: 2005.11401, 2004.04906, 2002.08909) per orchestrator Option B; full 75-paper ingest deferred to Phase 7 pre-rerun. Reason: measured paper-1 wall time of ~21 min projected ~15–25h / $1.50–3 vs plan budget of 10–30 min / $0.50–1.00. Phase 2 must-haves are met for the smoke set; Phase 7 is the architecturally correct place for the remaining 72 papers.**
- Phase 2 graphml ground truth captured at 2026-05-05T11:14:40Z: 2886 nodes / 7056 edges from 3 smoke papers, raganything==1.2.10, lightrag-hku==1.4.15, mineru==3.1.4 — this manifest is the version-of-record for Phase 9's frozen doc
- RAG-Anything 1.2.10 + lightrag-hku 1.4.15 require two integration fixes for Tier 4: (a) bypass the mineru-CLI parser-version probe at construction time when ingesting cached JSON via `insert_content_list`, and (b) forward `OPENROUTER_API_KEY` into `os.environ` at script entry because lightrag's openai_complete_if_cache reads it lazily inside async closures, not from the SecretStr-wrapped settings object — both fixes live in `tier-4-multimodal/scripts/ingest_from_mineru.py` (committed 5bc3f24)
- Plan 02-02 host MineRU top-up: 4/4 papers parsed successfully on host (approved branch). Wall time ~49 min total. The 4 new papers are in the MineRU cache but NOT in the smoke-only graphml; Phase 7 will re-rebuild over the full 79-paper cache.
- Plan 02-03 ships --smoke-question-ids on tier-4-multimodal/scripts/eval_capture.py with single-source-of-truth import of DEFAULT_SMOKE_IDS from evaluation.harness.run (Pitfall 5 of 02-RESEARCH.md). 12 unit tests cover the new pure helper _filter_qa(); test_eval_smoke_tier4_full_pipeline live test added (collected under -m live alongside tier-1 + tier-5).
- Plan 02-03 Rule-1 auto-fix: eval_capture.py's context probe used `rag.aquery(... param=...)` which fails under RAG-Anything 1.2.10 (signature does not accept `param=`); fix calls `rag.lightrag.aquery(... param=QueryParam(...))` directly, mirroring what RAGAnything itself does internally. Without the fix, retrieved_contexts was empty for all queries, masking real signal.
- Plan 02-03 smoke verdict: FAIL on faithfulness NaN (4/5 rows). Root cause is RAGAS faithfulness metric's _create_statements step hitting Gemini's 1024-token output cap when extracting atomic claims from long Tier 4 hybrid-mode answers (2011-2575 chars). NOT a Plan 02-01 regression: n_populated=5/5, ratio=1.0, context_precision 5/5 non-NaN, answer_relevancy 5/5 non-NaN, no Python repr leak. Recommended gap-closure: add max_tokens=8192 to score._build_judge's litellm.completion config and re-run score only.
- Plan 02-04 closed the Plan 02-03 FAIL via the recommended one-line fix: added module-level constant JUDGE_MAX_TOKENS=8192 beside JUDGE_LLM_SLUG_DEFAULT in evaluation/harness/score.py (line 70) and threaded max_tokens=JUDGE_MAX_TOKENS into the llm_factory(...) call inside _build_judge (line 131). Two new unit tests in evaluation/tests/test_eval_score.py monkeypatch ragas.llms.llm_factory + ragas.embeddings.{base.,}embedding_factory and assert the wiring offline (TDD: RED commit cdbc376 → GREEN commit 7fc6d66, all 13 tests pass). Re-scored Tier 4 against existing capture: 5/5 faithfulness=1.0 (was 4/5 NaN). Smoke gate verdict=PASS for Tier 4 (gap closure target) AND Tier 5 (Phase 1 regression check — byte-identical to 2026-05-04 PASS).
- Plan 02-04 Rule-3 deviation: project .gitignore (set at Phase 131 init) excludes evaluation/results/metrics/ and evaluation/results/costs/*.json as 'regenerable runtime intermediates'. The plan's Step E artifact commit was incompatible with this convention (which Plan 02-03 already followed); adapted to roll Step E into the SUMMARY commit with both SmokeGateResult JSON blocks captured verbatim in 02-04-SUMMARY.md for provenance equivalence. No Rule 4 architectural change made.
- Plan 03-01 ships HARN-05 stage 1 (units only): NaNReasonTracer(BaseCallbackHandler) + _classify_post_evaluate_nan pure helper landed in evaluation/harness/score.py via TDD red→green (RED commit e97e864 with 15 failing tests, GREEN commit bc80825 with 109 LOC of impl). The two units are tightly coupled (classifier reads tracer.errors dict) but both pure → land in a single plan. Plan 03-02 wires them into score_query_log via evaluate(callbacks=[tracer], ...) and post-processes each row through _classify_post_evaluate_nan to populate ScoreRecord.nan_reason; Plan 03-03 is the live smoke backstop with checkpoint:human-verify.
- Plan 03-01 idempotence semantics chose 'first wins' (leaf-most error preserved) over 'last wins' (root-most error preserved). Rationale: RAGAS_PROMPT is the leaf and that's where RagasOutputParserException originates; METRIC and ROW catch-and-re-raise the same exception, so leaf is the most specific signal for HARN-05 reason classification. If a future RAGAS version starts wrapping leaf exceptions in higher-level types (e.g. METRIC catches and re-raises as RagasMetricException), this decision needs revisiting — Plan 03-02 integration tests with a stub LLM should surface that scenario.
- Plan 03-01 _classify_post_evaluate_nan precedence ladder: captured-exception-type FIRST (specific — json_parse_failure / llm_did_not_finish), then per-metric semantic NaN paths (general — empty_statements / empty_questions / invalid_verdicts), then 'unknown_nan' + WARNING log (defensive — Pitfall 7 of 03-RESEARCH.md says never silently drop NaN). Captured-but-unknown-type (e.g. 'TimeoutError') falls through to per-metric semantic mapping rather than 'unknown_nan' so a known-metric NaN with a novel exception type still gets a meaningful reason string. Test N covers this exact branch.
- Plan 03-01 placement: NaNReasonTracer + _classify_post_evaluate_nan inserted BETWEEN _short_circuit_nan (existing, byte-identical) and _to_float_or_none (existing, byte-identical) so all NaN-related helpers cluster together. score_query_log signature unchanged (verified via `git diff | grep "def score_query_log"` returning empty); Plan 03-02 will modify it.
- Plan 03-01 zero deviations: plan executed exactly as written. langchain_core + ragas.callbacks imports verified working in .venv before any code change (one extra `python -c "..."` sanity check, not a deviation). DeprecationWarnings on `from ragas.metrics import ...` in score.py:202 predate Plan 03-01 (visible in Plan 02-04 final pytest run) — out of scope per RULE 4.
- [Phase ?]: Plan 03-02 wires NaNReasonTracer into score_query_log via callbacks=[tracer] alongside existing kwargs (Pitfall 6 of 03-RESEARCH.md — RAGAS appends its own callbacks rather than replacing); _classify_post_evaluate_nan called per-metric with documented faithfulness > AR > CP precedence in BOTH the dataframe branch AND the result.scores fallback branch (parity for older RAGAS 0.4.x patches). 4 new offline integration tests + 1 compare regression test; no live API spend. Single atomic commit fe52528.
- [Phase ?]: Plan 03-02 clean-path test assertion relaxed per the plan's own docstring guidance — observed RAGAS 0.4.3 scores _CleanLLM as AR=1.0, CP~=1.0, faithfulness NaN with classified 'json_parse_failure' (RagasOutputParserException on NLI prompt). This is EXACTLY the wiring chain HARN-05 needs. Strict 'is None' replaced with 'in {None, json_parse_failure, llm_did_not_finish, empty_statements, empty_questions, invalid_verdicts}' + disallowed silent-drop state check. Rule-1 deviation tracked in 03-02-SUMMARY.md.
- [Phase ?]: Plan 03-02 verifies Architectural Responsibility Map claim 'compare.py needs ZERO change for HARN-05' end-to-end: test_aggregate_tier_with_new_reasons exercises BOTH aggregate_tier (nan_breakdown buckets json_parse_failure + empty_statements + empty_contexts) AND emit_markdown footer rendering with new reason strings + joined-sorted breakdown line, all without modifying compare.py. Gate 4 (git diff compare.py == 0 lines) confirms byte-identical.
- Plan 03-03 live smoke backstop verified — n_unknown_nan=0 against real Gemini judge output. HARN-05 closed at unit + integration + live levels. Live verdict 2026-05-05 PASS: n_total=5, n_unknown_nan=0, n_scored_post_short_circuit=5; ~$0.014 cost (45 judge calls × ~$0.0003) vs $0.05 cost guard; 78.78s wall time. The classifier covers every real RAGAS 0.4.3 NaN path actually exercised by the Tier 5 smoke set against Gemini 2.5 Flash. Phase 7's full 5-tier rerun re-running this same live test against the new captures is the regression check that the wiring continues to cover every actual NaN path post-Phase-3.
- Plan 03-03 single process deviation routed through user-approved checkpoint:decision: previous agent accidentally executed the live test once during a deselection-check verification step (~$0.005-0.02 extra spend; total live cost across both invocations bounded at ~$0.028 — well under the $0.05 cost guard). Root cause: pyproject.toml registers the `live` marker under `[tool.pytest.ini_options].markers` but does NOT declare `addopts = "-m 'not live'"`, so a bare `pytest path/to/file.py -v` (no -m flag) silently runs live tests. The Phase 1 Plan 01-02 + Phase 2 Plan 02-03 + Plan 03-03 plans all assumed live-deselect-by-default semantics that pytest does not actually enforce without explicit addopts. Out of scope per RULE 4 — pyproject.toml is not in Plan 03-03's files_modified list and the change has cross-cutting impact on every other live test in the repo. Tracked as v1.1 hardening below.
- Plan 03-03 token-counts-zero observation is provenance noise, not a gate failure: the verbatim stdout shows judge_input_tokens=0 / judge_output_tokens=0 even though real spend occurred (~$0.014 estimate consistent with plan a-priori ~$0.005-0.02). Same v1.1 follow-up tracked since Plan 02-04 (judge cost ledger underreports on LiteLLM completions) — augment score.py's token_usage_parser to parse usage from LiteLLM ModelResponse bodies even when the parser misses some calls. The cost guard is bounded by question×metric×call count regardless.
- Plan 04-01 ships HARN-03 + HARN-04 in a single 95-LOC module (`evaluation/harness/freeze.py`) with locked Phase 5 forward-contract `freeze(version, force, results_dir, source) -> Path`. Sidecar manifest pattern: byte-identical .md (via `shutil.copy2`) + `eval-numbers-v{X.Y}.manifest.json` carrying ALL provenance (git SHA + dirty bit, ISO 8601 Z frozen_at, per-tier capture/cost/metrics relative paths + mtimes, judge {model, embedder, max_tokens=8192}, library_versions for the 4 critical packages). Refuse-to-clobber default with prescribed wording `"already frozen — bump version or pass --force"`; --force overwrites BOTH md AND manifest (decision Q1); RuntimeError + exit 2 BEFORE any file write if any of the 4 critical libs is `not-installed` (decision Q2); .gitkeep committed so frozen/ exists in fresh clones (decision Q3); zero content injection in markdown body (decision Q4); LOC budget honored at exactly 95 raw lines via 3 compression iterations (decision Q5 — plan's verbatim sketch was 184 raw lines; aggressive blank-line + multi-line-construct collapse was needed). Zero modifications to run.py / compare.py / score.py (gate verified empty diff). 5-step CLI quality gate PASS (byte-identical copy / refuse-clobber wording / --force overwrite / importable freeze() / manifest schema). Live-CLI artifacts deleted post-verification so Phase 7's full-rerun freeze starts from a clean slate. RED commit 9588056 + GREEN commit 7881e87.
- Plan 04-01 LOC-budget lesson: planner must validate sketch LOC via raw `wc -l` (NOT SLOC excluding blanks/comments) before locking `max_lines` in must_haves. Plan 04-01's plan sketch estimated ~74 LOC realistic / 95 hard cap; the verbatim sketch produced 184 raw lines on first write. Compression to 95 retained all 10 tests + all 5 CLI gates green but pushed beyond stylistic comfort (no PEP 8 blank-line separation between top-level defs; semicolon-stack single-line if-bodies; comma-grouped imports). Recommend Phase 5+ planners use `printf '%s\n' "$SKETCH" | wc -l` to verify before locking.
- Plan 04-01 Rule-4 carry-forward: pre-existing `test_eval_adapters.py::test_run_tier2_extracts_grounding` failure on `main` (`AttributeError: 'str' object has no attribute 'get_secret_value'` at `tier_2.py:89`). Reproduced on clean tree via `git stash`. NOT auto-fixed per RULE 4 (adapter file not in Plan 04-01's `files_modified`; cross-cutting Tier 2 adapter concern → Phase 1 follow-up territory). Logged to `.planning/phases/04-freeze-tool/deferred-items.md`. The Plan 04-01 regression baseline is established at 88 passed tests with `--ignore=evaluation/tests/test_eval_adapters.py --ignore=evaluation/tests/test_eval_smoke_live.py` (78 pre-existing + 10 new freeze tests).
- Plan 05-01 ships HARN-01 + HARN-02 at unit + integration levels in a single 189-LOC module (`evaluation/harness/pipeline.py`) with 14% buffer under the 220-LOC hard cap. In-process composition (no subprocesses): `await run.amain(...)`, `await score.amain(...)`, `await asyncio.to_thread(compare._run, ...)`, `await asyncio.to_thread(freeze.freeze, ...)` with single `asyncio.run` boundary at top of `main()` per project Pitfall 5 convention. 12 argparse flags (`--tiers` default `1,2,3,4,5`, `--limit`, `--smoke-question-ids`, `--tier-4-from-cache`, `--results-dir` default `evaluation/results`, `--judge-model`, `--judge-emb`, `--batch-size` default 10, `--mode` choices `naive|local|global|hybrid|mix` default `hybrid`, `--tier1-k` default 5, `--freeze` default None gates freeze stage, `--yes`). Sweep-level identity captured ONCE at pipeline.amain entry: `sweep_sha = run._git_sha()` and `sweep_ts = run._ts()`; threaded into run.amain via the new `git_sha_override` + `ts_override` kwargs landed in run.py (+10 raw insertions / -3 deletions = +7 net LOC, exactly at the 10-LOC cap). HARN-02 integration test (Test 15) builds a tmp results_dir with pre-existing tier-{1,2,3,5} files mtime-backdated 1 day, monkeypatches run.amain + score.amain to write fresh tier-4 files containing the propagated SHA, leaves compare._run REAL (not mocked), and asserts (a) pipeline returns 0, (b) every pre-existing file is byte-identical, (c) regenerated comparison.md contains all 5 tier rows, (e) `json.loads(new_tier4.read_text())["git_sha"] == sweep_sha` — proving the propagated SHA actually lands inside the written JSON on disk (W5 closure). Cost-prompt consolidation (Success Criterion 4): pipeline.py prompts once if `not args.yes`; threads `args.yes=True` into both `run_args` and `score_args` so the inner prompts in run.amain (`_cost_surprise`) and score.amain (inline) are suppressed unconditionally. Compare stage hard-codes `tiers="1,2,3,4,5"` per D-Q4 so single-tier reruns produce a complete comparison.md. Freeze refusal handling: `try: await asyncio.to_thread(freeze.freeze, ...) except (FileExistsError, FileNotFoundError, RuntimeError) as e: print red "Freeze refused: ..." + return 2` — Phase 4 forward contract `freeze(version, force, results_dir, source) -> Path` honored verbatim, freeze.py byte-identical (0 bytes diff). 6/6 CLI quality gate sub-checks PASS (--help / --tiers 99 → exit 2 / importability / wc -l = 189 / byte-identical guard for score.py + compare.py + freeze.py / run.py diff ≤ 10 insertions). Two micro-deviations rolled into the GREEN commit: Rule 3 — added `**kw` to two existing `fake_capture_tier` stubs in `test_eval_run.py` so they absorb the new kwargs (test failures directly caused by my run.py edit; backwards-compat at runtime call site preserved); Rule 1 — reworded pipeline.py docstring `"subprocesses"` → `"one Python process; no shell-out"` so Test 13's grep-substring-containment check passes. Sketch LOC validation per Plan 04-01 lesson worked first try (zero compression iterations needed). RED commit 3259ab1 + GREEN commit aa1640c.
- Plan 05-01 Plan 04-01 LOC-budget-lesson application: planner read Plan 04-01's "validate sketch via raw wc -l (NOT SLOC)" lesson, set `max_lines: pipeline_py: 220` based on the research-validated 144-LOC sketch (53% buffer), and the executor came in at 189 raw LOC first try — ZERO compression iterations, vs Plan 04-01's 3 iterations to fit a 95-LOC cap that the verbatim sketch overshot at 184 raw lines. Lesson held: roomy LOC budgets backed by sketch validation eliminate compression overhead.
- Plan 05-01 Rule-1 carry-forward: pipeline.py docstring used "no subprocesses" (plural) which contains the substring "subprocess" — Test 13's `assert "subprocess" not in source` rejected this. Fix: reworded to "one Python process; no shell-out". The plan EXPLICITLY warned this would happen ("checks raw substring containment, NOT just imports — pipeline.py must NEVER reference these tokens at all"). Lesson: when a test enforces token-absence at file-text level, EVERY occurrence including docstring narrative must be scrubbed.
- Plan 05-02 ships HARN-01 closure at the live level: `test_pipeline_live_tier5_smoke` (@pytest.mark.live, +97 raw insertions) appended to `evaluation/tests/test_eval_pipeline.py`. Drives `pipeline.main(['--tiers','5','--limit','5','--yes','--results-dir', tmp_path/results])` against real OpenRouter API + real chroma_db/tier-1-naive/ + real RAGAS judge. Verdict 2026-05-06: PASS in 158.02s wall at total_usd=$0.007010 (well under $0.05 D-LIVE-COST-CEILING enforced as pytest gate per W4 closure). Single-SHA propagation verified end-to-end against real `_git_sha()` output: queries.git_sha=`fd287f0` matches HEAD; comparison.md regenerated with tier-5 row; 1/1/1/1 tier-5 queries+metrics+tier-5-eval+ragas-judge files on disk. HARN-01 now closed end-to-end at unit + integration + live levels (Plan 05-01 + Plan 05-02). Test commit 117d595 + this docs commit. Two Rule-1 deviations rolled into the single test commit: (1) cost-JSON schema-adapted to actual `{'totals':{'usd':...}}` shape (NOT top-level 'total_usd' as plan sketch said — schema verified against pre-existing files in evaluation/results/costs/ before append; ceiling preserved verbatim at $0.05); (2) monkeypatch shared.cost_tracker.DEFAULT_COSTS_DIR to tmp_path/costs/ because pre-existing harness mis-feature run.py:273 + score.py:518 call tracker.persist() with no dest_dir, ignoring caller's --results-dir. Production code untouched (pipeline.py + run.py + score.py + compare.py + freeze.py byte-identical to Plan 05-01 — 0 bytes diff total). NEW v1.1 hardening item: tracker.persist() should accept caller's --results-dir; deferred per RULE 4 (cross-cutting cost-tracker impact on every harness test). Total live spend across first-failed + second-passing invocations ~$0.014 (well under $0.05 single-run ceiling; 2× pattern bounded).
- Plan 05-02 lesson: when a live test asserts on cost files written by harness modules under a byte-identical guard, **always check whether the cost-persistence helper honors the test's tmp_path** before invoking the live test. Plan 05-02's first live invocation (~$0.007 spent + 222s wall) was burned because the test glob looked for cost files under `tmp_path/costs/` but `tracker.persist()` defaulted to `evaluation/results/costs/` (hardcoded module constant). Pre-flight test: write a no-spend smoke that runs `pipeline.main(['--tiers','5','--limit','0','--yes','--results-dir', tmp_path])` (or use a stub) and confirm cost files actually land where the assertion expects BEFORE incurring real API spend. Pattern reusable for Phase 6+ tests that exercise the harness against tmp_path isolation.
- Plan 06-01 ships CAP-03 closure at unit + integration levels via 6 atomic RED→GREEN pairs (12 commits a6c3155..5d13879). QueryLog gains Optional[embedder, embedder_source] (records.py 84→86); 5 tier modules carry per-tier EMBEDDER_SOURCE constants (tier-1-naive/embed_openai.py +1; tier-2-managed/main.py +6 with EMBED_MODEL='gemini-embedding-001' + EMBEDDER_SOURCE='google-managed' colocated next to INDEX_USD_PER_1M_TOKENS per D-Q2; tier-3-graph/rag.py +2; tier-4-multimodal/rag.py +2; tier-5-agentic/tools.py +7 — re-imports EMBED_MODEL from Tier 1 + locally declares EMBEDDER_SOURCE='openrouter' with explicit inline rejection comment for the wrong 'openai-hosted-managed' string per D-ROADMAP-OVERRIDE). run.py +29 LOC threads fields per tier branch (391→420, exactly at cap); tier-4-multimodal/scripts/eval_capture.py +7 LOC threads at the live capture path (D-CAPTURE-ENTRYPOINTS). compare.py +21 LOC: aggregate_tier pass-through + emit_markdown per-tier embedder line in capture-provenance footer + dedicated 'Embedder by tier' markdown table block with 'Managed' column derived as `embedder_source == 'google-managed'` per D-Q1 (Phase 9 frozen-doc cost ceiling cut: zero new logic in Phase 9). freeze.py +2 LOC manifest entry. +13 new offline tests across 5 test files; offline sweep 116→126 PASS. Forward-contract guard intact: 0 bytes diff for score.py / pipeline.py / smoke_gate.py vs 074e668. All 10 LOC budgets honored zero compression iterations — third consecutive plan applying Plan 04-01's 'validate sketch via raw wc -l' lesson; 2 source files landed exactly at cap (run.py 420/420, tier-5 tools.py 178/178). Zero deviations: plan executed exactly as written. gsd-verifier verdict 2026-05-06: status=passed, score=3/3 must-haves verified.
- Plan 06-01 D-ROADMAP-OVERRIDE rationale (locked in source via tier-5-agentic/tools.py inline rejection comment): 06-RESEARCH.md verified against tier-5-agentic/tools.py:47-50,90-101 that Tier 5 reuses Tier 1's local ChromaDB and embeds via OpenRouter `openai/text-embedding-3-small` — IDENTICAL to Tier 1, NOT 'OpenAI's hosted vector-store embedder' as the stale ROADMAP wording claimed. The `managed=true` boolean flag on QueryLog is REJECTED in favor of deriving 'managed' as `embedder_source == 'google-managed'` in compare.py rendering — only Tier 2 is 'managed' (Google File Search owns the indexing). Override callout added inline to ROADMAP.md Phase 6 Success Criteria + 06-01-PLAN.md frontmatter `locked_decisions` + 06-01-SUMMARY.md Decisions section. Lesson: when a research phase contradicts a ROADMAP wording, lock the override in three places (frontmatter + research doc + ROADMAP inline callout) so the verifier matches the planned behavior, not the stale wording.
- Plan 06-01 Task 4 test pattern lesson: when the production code is a thin pass-through (eval_capture.py imports EMBEDDER_SOURCE + writes to QueryLog kwargs) and mocking the surrounding library stack (build_rag, run_query, write_query_log, settings) adds no signal, prefer a static-source assertion test — read the file text and assert the expected literal substrings appear (e.g. `'EMBEDDER_SOURCE'` import + `'embedder=EMBED_MODEL'` + `'embedder_source=EMBEDDER_SOURCE'`). 06-RESEARCH.md recommended this approach explicitly; the plan offered both options and the executor picked static-source. Pattern reusable for any Phase 7+ test where mocking adds fragility without signal.
- Plan 07-01 (Pre-Flight Verification) closed all 6 pre-sweep gates without source-code change (RAW-LOCK held end-to-end). Gate 3 recreated `tier-2-managed/.store_id` via `tier-2-managed/main.py --ingest`: 100/100 papers uploaded to Gemini File Search store `fileSearchStores/ragarchpatternstier2-bmk7n7tckw7b`, ~$0.0077 synthetic indexing estimate, ~28 min wall. Gate 6 ran 5-question smoke per tier × 5 tiers (Tier 1/2/3/5 via `evaluation.harness.run`; Tier 4 via `tier-4-multimodal/scripts/eval_capture.py` per D-CAPTURE-ENTRYPOINTS); ALL 5 smoke gates exited 0 with verdict=PASS, ratio=1.00, n_populated=5/5, faithfulness/CP non-NaN. Total smoke spend $0.0282; total Plan 07-01 spend ~$0.036 (well under $1.50 ceiling). 6 forward-contract harness modules byte-identical (`git diff HEAD -- evaluation/harness/{pipeline,run,score,compare,freeze,smoke_gate}.py | wc -c` returns 0 bytes). Offline pytest baseline 116/116 PASS with `-m 'not live'` and the plan's exact ignore set. ONLY commit: `docs(07-01): pre-flight verification — Open Q1 verdict PASS, 5/5 tier smokes PASS`.
- Plan 07-01 Open Question 1 verdict: **PASS** — `pipeline.py --tier-4-from-cache` rewrites BOTH `git_sha` AND `timestamp` on every passthrough invocation. Mechanism verified end-to-end: input `tier-4-2026-05-06T19_27_35Z.json` → output `tier-4-2026-05-06T19_31_38Z.json` with INPUT_SHA=`03f9ce1`, OUTPUT_SHA=`03f9ce1`, HEAD_SHA=`03f9ce1` (all match because the Tier-4 smoke was just captured at HEAD), AND `timestamp` field rewritten from `2026-05-06T19:27:35Z` to `2026-05-06T19:31:38Z` proving pipeline.py threads BOTH `sweep_sha` AND `sweep_ts` via `git_sha_override + ts_override` kwargs into run.py:233-262 passthrough writer. Implication: Plan 07-03's CAP-01 single-SHA invariant is mechanically enforceable for Tier 4 without source change; verifier can rely on `output.git_sha == sweep_sha` for ALL 5 tiers uniformly — Tier 4 is NOT a special case requiring an exception clause.
- Plan 07-01 Tier-4 PATH gotcha (Rule 3 deviation, no source change per RAW-LOCK): `tier-4-multimodal/scripts/eval_capture.py` requires `PATH=$PWD/.venv/bin:$PATH` so `RAGAnything._ensure_lightrag_initialized()` resolves the mineru CLI via `shutil.which('mineru')`. Without this, `rag.lightrag` stays `None` and every aquery raises `AttributeError: 'NoneType' object has no attribute 'aquery'`, silently degrading captures to 5×{ctx_n=0, ans_len=0} records. First Tier-4 invocation produced exactly that pathology; second (with PATH augmentation) produced 5×{ctx_n∈[1,7], populated answer} records and PASS verdict. Plans 07-02 / 07-03 MUST set PATH before invoking eval_capture.py. v1.1 hardening candidate: have eval_capture.py prepend `Path(sys.executable).parent` to `os.environ['PATH']` before instantiating RAGAnything.
- Plan 07-01 minor invocation gotchas (Rule 1, no source change): (a) `tier-2-managed/main.py` CLI does NOT accept `--yes`; remove the flag (script has no interactive prompt). (b) Use `.venv/bin/python` directly — `python` on PATH may resolve to a pyenv shim missing project deps (manifested as `ImportError: cannot import name 'genai' from 'google'`).
- Plan 07-02 (Tier-4 Full-Corpus Rebuild) closed CAP-01's full-corpus precondition without source-code change (RAW-LOCK held end-to-end). Host ingest 13h55m wall (started 2026-05-06T19:48:58Z, ended ~2026-05-07T09:44:00Z per `kv_store_doc_status.json` mtime); 79/79 papers per `Content list insertion complete for:` markers + final summary line `Ingested 79/79 papers; cost $24.796629`. Tier-4 graphml: 28597 nodes / 80419 edges / 79 docs / 52.5 MB at `rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml` (vs the 2026-05-05 smoke baseline of 2886 / 7056 / 3 papers — 9.9× nodes for 26.3× papers, sub-linear scaling consistent with cross-paper entity dedup). kv_store_llm_response_cache.json grew to 128.8 MB. Provenance manifest committed at `evaluation/results/diagnostics/tier-4-graph-stats-2026-05-07T10_31_52Z.json` (commit `eb77cf8`); library versions match pinned (lightrag-hku 1.4.15 / raganything 1.2.10 / mineru 3.1.4); Phase 9 frozen-doc cites this manifest as version-of-record. Both 2026-05-05 smoke manifest AND 2026-05-07 full-rebuild manifest preserved in `evaluation/results/diagnostics/`. 5q post-rebuild smoke verdict=PASS (5/5 measurable populated, ratio=1.00, faithfulness/CP non-NaN; capture cost $0.0503, judge $0.00 cache hit). Forward-contract guard: 0 bytes diff vs ea711eb (Plan 07-01 baseline) across 6 RAW-LOCKed harness modules + 3 tier-4 helper scripts. Offline pytest baseline preserved at 116/116 PASS with `-m 'not live'`. SUMMARY commit `5766dc6` carries cross-plan data contract `tier_4_full_rebuild_manifest:` front-matter key — 07-03 Task 2 reads this as single source of truth (avoids mtime-ambiguity failure mode where smoke manifest could be misidentified as freshest). Total Plan 07-02 cost $24.85 vs $50 ceiling (ingest $24.7966 + capture $0.0503 + judge $0.00).
- Plan 07-02 multimodal-dropout note: ~31,170 occurrences of "402" in the host log are vision-LLM 4xx errors on individual multimodal items (figures, code blocks, page numbers) — NON-FATAL per Plan 02-01 Pitfall 1's per-paper exception isolation. User briefly hit OpenRouter $0 balance mid-run, topped up; some retries succeeded, others were dropped. The MAIN entity-extraction graph was completed for all 79/79 papers, and the 5q smoke gate passed. v1.1 may revisit per-item retry budget tuning.
- Plan 07-02 Rule-3 deviation: plan's verification jq command (`jq -r '.library_versions["lightrag-hku"]'`) assumed a nested-dict schema that does not match `log_graph_stats.py`'s actual output. The real schema (and the existing 2026-05-05 smoke manifest) uses flat top-level keys: `.lightrag_version` / `.raganything_version` / `.mineru_version`. Verification command adapted to the real schema; manifest CONTENTS are correct as written by the helper. v1.1 follow-up candidate: harmonize log_graph_stats.py output schema with downstream consumer expectations OR update plan-text examples to reference the flat schema.
- Plan 07-02 cross-plan-data-contract pattern: SUMMARY front-matter carries `tier_4_full_rebuild_manifest: <relative-path>` as the canonical pointer to the full-rebuild manifest. This avoids the mtime-ambiguity failure mode where filesystem mtime ordering could misidentify the smoke manifest as the freshest (e.g. if the smoke manifest's mtime was somehow touched after the rebuild). 07-03 Task 2 reads the front-matter key first, with a documented fallback to mtime-freshest + filename guard. Reusable pattern for any plan that needs to reference an artifact committed by a prior plan in the same phase.
- Plan 07-03 (Full 5-Tier Sweep — CAP-01 Closure) closed CAP-01 without source-code change (RAW-LOCK held end-to-end across all 3 plans of Phase 7). Sweep_sha=`75f6f1b` propagated across all 5 freshest tier-N queries JSONs at sweep_ts=`2026-05-07T10:59:10Z`; per-tier 30 records / single-SHA invariant verified (SC-2 PASS at distinct-count=1). Tier 4 NaN dropped from 30/30 baseline to 2/30 (`empty_contexts: 2`; SC-3 PASS — Phase 2 fix sticking on full 79-paper graph). Tier 5 NaN dropped from 30/30 baseline to 0/30 (PERFECT; SC-4 PASS — Phase 1 D-FORMAT context normalization sticking). Comparison.md regenerated with 5-row rollup + 15-row per-class breakdown + 5-row Embedder-by-tier table (SC-1 PASS — Phase 6 D-Q1 plumbing intact). Total Plan 07-03 spend $0.439 honest accounting ($0.287 Step 1 eval_capture.py + $0.118 Step 2 pipeline sweep capture + $0.000 Step 2 RAGAS judge [LiteLLM token-parser gap, known v1.1 item] + $0.034 wasted first-attempt Tier 1 before Gemini 429 stopped Tier 2) vs $0.84 estimate vs $3.00 ceiling (SC-5 PASS at 14.6% of ceiling). Verifier transcript `.planning/phases/07-full-5-tier-rerun/07-03-VERIFY.txt` ends with `OVERALL VERDICT: PASS`. Forward-contract guard: 0 bytes diff vs HEAD (75f6f1b — Plan 07-02 baseline) across 9 RAW-LOCKed source files. Offline pytest baseline 116 passed / 1 deselected with `-m 'not live'` (no regressions vs Plan 07-02). ONE SUMMARY commit (07-03-SUMMARY.md + 07-03-VERIFY.txt).
- Plan 07-03 Rule-3 deviation #1 (verifier-regex mismatch): plan's verifier uses `grep -E '^## Embedder by tier'` but `compare.py:278` actually emits `**Embedder by tier:**` (markdown bold-italic, not heading). The Embedder-by-tier table IS correctly emitted with all 5 rows; verifier check itself was wrong. Per RAW-LOCK constraint, adjusted the verifier regex to `^\*\*Embedder by tier:\*\*` rather than touching compare.py. Verifier transcript shows "Embedder-by-tier table: present (matched **Embedder by tier:** pattern)". v1.1 follow-up: future plans should validate verifier regexes against actual compare.py emit code, not against the spec doc.
- Plan 07-03 Rule-3 deviation #2 (transient Gemini 429 retry): first pipeline.py invocation crashed at Tier 2 query 25/30 (multimodal-005) with `google.genai.errors.ClientError: 429 RESOURCE_EXHAUSTED. {'error': {'message': 'Failed to embed content.'}}` — transient cloud-quota error, not a code defect. Reran pipeline.py without making any commits; HEAD stayed at 75f6f1b so sweep_sha was identical between attempts. Second invocation succeeded fully with all 5 tier captures landing at sweep_ts `2026-05-07T10:59:10Z`. Single-SHA invariant preserved per plan's load-bearing invariant 2 ("no commits between Step 1 and Step 2"). Wasted ~$0.034 from first-attempt Tier 1 completion included in honest cost accounting. Pattern: when Gemini 429s mid-sweep, the prescribed recovery is rerun-without-commit (NOT amend/reset/rebase to recover the original sweep_sha) — the original sweep_sha is preserved automatically because HEAD didn't move.
- Plan 07-03 honest-cost-window selection lesson: verifier's `ls -t | head -1` for cost JSON freshness got polluted by post-sweep pytest test bleed-through (test wrote 5 empty placeholder cost JSONs at `tier-N-eval-20260507T120542Z.json` — 197 bytes each, $0 — because of pre-existing `shared.cost_tracker.DEFAULT_COSTS_DIR` hardcoded-path mis-feature documented in Plan 02-04 + 05-02). Verifier's $0 result trivially PASSES SC-5, but for honest cost provenance the SUMMARY computes total from actual sweep-window cost JSONs (timestamps 10:59:10Z to 12:02:53Z). Lesson: when a freshness-mtime-based verifier runs alongside non-isolated tests in the same project, prefer ts-window grep over `ls -t | head -1` for the SUMMARY's honest accounting. Verifier verdict (with mtime-freshest selection) is still correct for SC-5 ($0 ≤ $3); SUMMARY just additionally reports the correct sweep-window total for Phase 9 frozen-doc citation.
- Plan 07-03 deferred deviation observation: eval_capture.py reported $0.287 for Step 1 vs the plan's $0.045 estimate. Discrepancy is the VLM-enhanced retrieval path (each multimodal-NNN question fans out into image-validation + fallback queries), not a bug. Total Plan 07-03 spend ($0.439) still well under $0.84 plan estimate. v1.1 hardening item: tighten Plan 07-RESEARCH.md cost estimates for Tier 4 multimodal queries to account for VLM fanout.

### Pending Todos

None yet.

### Blockers/Concerns

- Tier 5 root cause is high-confidence (5-line adapter bug at `tier_5.py:125`) but Phase 1 must include a fallback path: if smoke test still shows empty contexts after the fix, follow STACK.md decision tree (simplify schema → switch model slug → bump openai-agents) before declaring done
- OpenRouter passthrough may obscure the exact Gemini model version (`gemini-2.5-flash-001` vs bare `gemini-2.5-flash`) for provenance; Phase 9 must document the version-unknown case explicitly if unresolvable
- Phase 7 pre-rerun ingest must process 72 remaining papers (`tier-4-multimodal/output/` minus 3 smoke papers minus 4 Plan-02-02 fresh-MineRU papers); projected wall ~15–25h / cost ~$15–35
- Phase 7 ingest run will hit OpenRouter vision-LLM `400 Invalid URL format` errors on larger figures (base64 image data exceeding URL length limit); recommend pre-warming `kv_store_llm_response_cache.json` or routing vision direct to OpenAI/Gemini (off-OpenRouter) to mitigate
- ~~Plan 02-03 smoke gate FAIL pending gap-closure~~ **CLEARED 2026-05-05 by Plan 02-04** — JUDGE_MAX_TOKENS=8192 wired into score._build_judge, Tier 4 re-scored, smoke gate PASS for both Tier 4 (gap target) and Tier 5 (regression check). See 02-04-SUMMARY.md for verbatim verdict JSON.
- **Judge cost ledger underreports on LiteLLM completions**: Plan 02-04's new `ragas-judge-tier-4-20260505T151051Z.json` records $0 despite real spend (smaller now since no retries, but still non-zero per OpenRouter dashboard); manifested again in Plan 03-03's live smoke as `judge_input_tokens=0 / judge_output_tokens=0`. Tracked as v1.1 follow-up — augment score.py to parse usage from LiteLLM ModelResponse bodies even when token_usage_parser misses calls. Cross-referenced in 02-03-SUMMARY.md, 02-04-SUMMARY.md, and 03-03-SUMMARY.md.
- **Live tests not deselected by default in pyproject.toml** (NEW v1.1 hardening item, surfaced by Plan 03-03 process deviation): pyproject.toml declares the `live` marker but does NOT include `addopts = "-m 'not live'"` under `[tool.pytest.ini_options]`. Consequence: a bare `pytest evaluation/tests/test_eval_smoke_live.py -v` (no -m flag) silently runs live tests, including any newly-added ones. Plan 03-03 manifested this gap when a previous agent accidentally executed the live nan_reasons test during a deselection-check verification step. Recommended v1.1 fix: add `addopts = "-m 'not live'"` and update the live-test invocation pattern in plans + CLAUDE.md to use `-m live -k <test>` explicitly. Out of scope per Plan 03-03's RULE 4 (pyproject.toml not in files_modified; cross-cutting impact on every other live test in repo).
- **Tier-4 eval_capture.py silently degrades when mineru isn't on PATH** (NEW v1.1 hardening item, surfaced by Plan 07-01 Rule-3 deviation): `RAGAnything._ensure_lightrag_initialized()` calls `doc_parser.check_installation()` which uses `shutil.which('mineru')`. When the parent shell's PATH does not include `.venv/bin`, the check returns False and `rag.lightrag` stays `None`, so the subsequent `await rag.lightrag.aquery(...)` raises `AttributeError: 'NoneType' object has no attribute 'aquery'`. The script catches the exception silently and produces 5 records with `ctx_n=0 ans_len=0` — the smoke gate would FAIL but only after wasting the live-API spend. Plan 07-01 mitigated by setting `PATH=$PWD/.venv/bin:$PATH` for Tier-4 invocations. v1.1 fix: have eval_capture.py prepend `Path(sys.executable).parent` to `os.environ['PATH']` before instantiating `RAGAnything`. Out of scope for 07-01 per RAW-LOCK constraint.

## Deferred Items

Items acknowledged and carried forward as v1.1+:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Cleanup | CLEANUP-01: consolidate `tier-N-name/` ↔ `tier_N_name/` shim folders | Tracked in REQUIREMENTS.md v1.1 | 2026-05-04 (init) |
| Cleanup | CLEANUP-02: `[debug-tier5]` extras for OpenInference + Phoenix | Tracked in REQUIREMENTS.md v1.1 | 2026-05-04 (init) |
| Methodology | METH-01: bootstrap 95% CI in compare.py | Tracked in REQUIREMENTS.md v1.1 | 2026-05-04 (init) |
| Methodology | METH-02: full cross-judge re-scoring (all 30 × 5) | Tracked in REQUIREMENTS.md v1.1 | 2026-05-04 (init) |
| Methodology | METH-03: per-tier per-class min/max/stdev | Tracked in REQUIREMENTS.md v1.1 | 2026-05-04 (init) |
| Blog Iteration | BLOG-01: rebuttal run pipeline | Tracked in REQUIREMENTS.md v1.1 | 2026-05-04 (init) |
| Tooling | Pytest live-marker deselect-by-default: add `addopts = "-m 'not live'"` to `[tool.pytest.ini_options]` so a bare `pytest path/to/file.py -v` cannot accidentally consume API budget | New v1.1 item | 2026-05-05 (Plan 03-03) |
| Tooling | LiteLLM judge token-usage parser: augment score.py's `token_usage_parser=get_token_usage_for_openai` to parse usage from LiteLLM ModelResponse bodies even when the parser misses some calls | Tracked since Plan 02-04, re-confirmed by Plan 03-03 + Plan 05-02 | 2026-05-06 (re-confirmed by Plan 05-02 ragas-judge-tier-5-*.json showing $0.0 despite real spend) |
| Tooling | tracker.persist() should honor caller-supplied `--results-dir` / `--output-dir`: `evaluation/harness/run.py:273` and `evaluation/harness/score.py:518` call `tracker.persist()` with no `dest_dir` arg, so cost files always land at the hardcoded `shared.cost_tracker.DEFAULT_COSTS_DIR` (= `Path('evaluation/results/costs')`) regardless of `--results-dir`. Manifested as a Rule-1 fix in Plan 05-02's live test (forced to monkeypatch DEFAULT_COSTS_DIR to keep tmp_path isolation working). v1.1 plumb-through: `tracker.persist(dest_dir=Path(args.output_dir) / 'costs')` in both call sites + add a `--cost-dir` flag at the top-level pipeline.py for explicit override | New v1.1 item | 2026-05-06 (Plan 05-02) |
| Tooling | `tier-4-multimodal/scripts/eval_capture.py` should prepend `Path(sys.executable).parent` (i.e. the project venv's `bin/`) to `os.environ['PATH']` BEFORE instantiating `RAGAnything`, so `RAGAnything._ensure_lightrag_initialized()`'s mineru parser-installation probe finds the `.venv/bin/mineru` binary regardless of the parent shell's PATH. Currently, when PATH excludes `.venv/bin`, the parser check returns False, `rag.lightrag` stays None, and every `await rag.lightrag.aquery(...)` raises `AttributeError` which the script catches silently — producing degraded captures with `ctx_n=0 ans_len=0` AFTER spending live API budget. Manifested in Plan 07-01 as a Rule-3 deviation (worked around by setting PATH inline). v1.1 patch is ~2 LOC at top of eval_capture.py main() | New v1.1 item | 2026-05-06 (Plan 07-01) |

## Session Continuity

Last session: 2026-05-07T12:10:00.000Z
Stopped at: Phase 7 EXECUTION COMPLETE (3/3 plans complete) — Plan 07-03 (Full 5-Tier Sweep — CAP-01 Closure) COMPLETE 2026-05-07. ONE source-tracked commit this plan: `<filled-by-final-commit>` docs(07-03): full-sweep COMPLETE — sweep_sha=75f6f1b, tier-4 NaN 2/30, tier-5 NaN 0/30, total $0.44 (committing 07-03-SUMMARY.md + 07-03-VERIFY.txt + STATE.md). ZERO source-code modifications per RAW-LOCK; sweep artifacts (5 freshest tier-N queries+metrics+costs JSONs at sweep_ts=2026-05-07T10:59:10Z + regenerated comparison.md) gitignored. Tasks: (1) cost-ack PRE-APPROVED by orchestrator ($0.84 estimate, $3.00 ceiling); (2) Step 1 Tier-4 30q live capture via eval_capture.py — 30 records, sha=75f6f1b, embedder=openai/text-embedding-3-small + source=openrouter, $0.287 actual cost (vs $0.045 estimate; VLM fanout dominated); (3) Step 2 pipeline.py full sweep — first attempt crashed at Tier 2 query 25/30 with transient Gemini 429 RESOURCE_EXHAUSTED on embedding API; reran without committing (HEAD stayed at 75f6f1b → sweep_sha preserved); second attempt completed all 5 tiers at sweep_ts=2026-05-07T10:59:10Z; comparison.md regenerated with 10 tier rows + Embedder-by-tier table; (4) post-sweep verifier transcript captured to 07-03-VERIFY.txt with `OVERALL VERDICT: PASS` for all 5 ROADMAP success criteria (SC-1 pipeline ran capture→score→compare PASS / SC-2 single sweep_sha=75f6f1b across 5 queries JSONs PASS / SC-3 tier-4 NaN 2/30 PASS / SC-4 tier-5 NaN 0/30 PASS [PERFECT] / SC-5 total spend $0.118 verifier or $0.439 honest ≤ $3.00 PASS); (5) Step 4 human-verify checkpoint AUTO-APPROVED per yolo+auto mode (all 7 spot-checks confirmed plausible: T4/T5 rollup non-trivial vs all-NaN baseline; embedder-by-tier table 5 correct rows; tier-4 graph manifest 28597 nodes; tier-5 first context starts with [paper_id=2005.11401]); (6) forward-contract guard 0 bytes diff vs HEAD (75f6f1b) across 9 RAW-LOCKed source files + offline pytest baseline 116/116 PASS preserved with `-m 'not live'` (no regressions vs Plan 07-02). 2 Rule-3 deviations: (a) Embedder-by-tier verifier regex was wrong (compare.py emits `**Embedder by tier:**` markdown bold, not `## Embedder by tier` heading); adjusted regex; table contents correct end-to-end. (b) First pipeline.py invocation crashed at Tier 2 query 25/30 with transient Gemini 429; reran without committing → sweep_sha preserved. Total Plan 07-03 spend $0.439 vs $3.00 ceiling (14.6% of ceiling). Phase 7 cumulative spend $25.32 ($24.85 Plan 07-02 + $0.44 Plan 07-03 + $0.036 Plan 07-01). Awaiting `/gsd-verify-phase` for Phase 7 closure (orchestrator runs verifier; on verdict=passed updates ROADMAP and unblocks Phase 8 multi-judge spot-check). Phase 9 freeze prerequisite reminder still in force: DO NOT rebuild Tier 4 between Plan 07-03 commit and Phase 9 freeze.

Resume file: None
