---
phase: 07-full-5-tier-rerun
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
autonomous: false
requirements:
  - CAP-01

files_modified:
  - rag_anything_storage/tier-4-multimodal/  # WIPED then rebuilt; gitignored directory tree
  - evaluation/results/diagnostics/tier-4-graph-stats-{ts}.json  # NEW provenance manifest, COMMITTED
  # No source-code modifications. Forward-contract-locked harness modules
  # (pipeline.py / run.py / score.py / compare.py / freeze.py / smoke_gate.py)
  # untouched. tier-4-multimodal/scripts/ingest_from_mineru.py and
  # log_graph_stats.py are RUN, not edited.

cost_ack:
  total_estimate_usd: "15.00 to 35.00"
  components:
    - what: "ingest_from_mineru.py --reset --yes over full 79-paper MineRU cache"
      estimate_usd: "15.00 to 35.00"
      notes: "DOMINANT cost line in entire v1.0 scope. Extrapolation from Plan 02-01's 3-paper / $1.89 / ~50min smoke. STATE.md applies a 2× safety margin (79 papers ≠ 79 × per-paper-cost because vision-LLM 400 errors drop ~5-10% of multimodal items, saving cost on dropouts; entity-extraction LLM passes are linear in paper count and dominate the bottom line). First full rebuild establishes the actual measurement."
    - what: "Post-rebuild 5q smoke verification (eval_capture + score + smoke_gate)"
      estimate_usd: "0.01 to 0.02"
      notes: "Same path Plan 02-04 used. Cheap; only present to gate Plan 07-03 readiness."
  budget_ceiling_usd: 50.00
  ceiling_rationale: "Hard cap. If `kv_store_llm_response_cache.json` cumulative cost approaches $50, abort and surface to user — implies a runaway retry loop or pricing model change since Plan 02-01. STATE.md projection upper bound is $35; $50 cap leaves headroom for unforeseen variance."
  wall_estimate: "15-25 hours (off-hours run recommended)"

must_haves:
  truths:
    - "User MUST run this plan on the host machine (NOT in the orchestrator sandbox). The Task 2 sandbox-detection guard refuses to proceed if `$CLAUDE_CODE_SANDBOX` is set or `$ALL_PROXY` contains `socks5h` (Plan 02-02 evidence — MineRU CLI behaves differently in sandbox)."
    - "User can run `ls rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml` post-Task 2 and find a single graphml file with mtime within the rebuild window."
    - "User can read `evaluation/results/diagnostics/tier-4-graph-stats-{NEW_TS}.json` post-Task 3 and find: graphml_node_count >> 2886 (the 3-paper smoke baseline), graphml_edge_count >> 7056, and library_versions matching pinned (lightrag-hku 1.4.15 / raganything 1.2.10 / mineru 3.1.4). Both the OLD smoke manifest (`tier-4-graph-stats-2026-05-05T11_14_40Z.json`) AND the NEW full-rebuild manifest sit in `diagnostics/` — Plan 07-02 does NOT delete the smoke manifest (historical record)."
    - "User can run `python tier-4-multimodal/scripts/eval_capture.py --smoke-question-ids single-hop-001,single-hop-002,single-hop-003,multi-hop-001,multi-hop-002 --yes` + `python -m evaluation.harness.score --tiers 4 --yes` + `python -m evaluation.harness.smoke_gate --tier 4` post-Task 4 and observe verdict=PASS — proving the new full-corpus graph passes the same gate the 3-paper smoke graph passed."
    - "User can run `git diff HEAD -- evaluation/harness/pipeline.py evaluation/harness/run.py evaluation/harness/score.py evaluation/harness/compare.py evaluation/harness/freeze.py evaluation/harness/smoke_gate.py | wc -c` post-Task 5 and observe `0` (forward-contract guard intact)."
  artifacts:
    - path: "rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml"
      provides: "Full-corpus 79-paper LightRAG graph (replaces 3-paper smoke)"
      gitignored: true
      expected_node_count_minimum: 30000  # research-honest lower bound; actual measured by Task 3
      expected_edge_count_minimum: 50000
    - path: "evaluation/results/diagnostics/tier-4-graph-stats-{NEW_TS}.json"
      provides: "Provenance manifest for full-corpus rebuild — Phase 9 frozen-doc cites this"
      contains: "graphml_node_count, graphml_edge_count, paper_ids, library_versions"
      gitignored: false  # COMMITTED — this is the version-of-record per 07-RESEARCH.md
    - path: "rag_anything_storage/tier-4-multimodal/kv_store_llm_response_cache.json"
      provides: "Cache of LLM responses paid for during rebuild (re-runs hit cache)"
      gitignored: true
      notes: "Existence is NOT a must-have; mentioned for completeness — the rebuild paid for these calls and the cache amortizes future re-rebuilds (out of scope for v1.0)."
  key_links:
    - from: "tier-4-multimodal/output/ (79-paper MineRU cache)"
      to: "rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml"
      via: "ingest_from_mineru.py --reset --yes (rglob _content_list.json + insert_content_list)"
      verify_pattern: "test -f rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml && [[ -s ... ]]"
    - from: "rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml"
      to: "evaluation/results/diagnostics/tier-4-graph-stats-{NEW_TS}.json"
      via: "log_graph_stats.py reads graphml + writes Pydantic-typed manifest"
      verify_pattern: "jq '.graphml_node_count, .graphml_edge_count' <new-manifest>"
    - from: "rag_anything_storage/tier-4-multimodal/ (full graph)"
      to: "smoke_gate verdict=PASS"
      via: "eval_capture (5q smoke) + score + smoke_gate"
      verify_pattern: "python -m evaluation.harness.smoke_gate --tier 4"

user_setup:
  - service: "host-machine-shell"
    why: "MineRU CLI ingest must run outside the orchestrator sandbox per Phase 2 / Plan 02-02 evidence (Phase 139 root-cause: parser-version probe + vision-LLM URL handling differ in sandbox). The plan's Task 2 includes a sandbox-detection guard that exits 1 if running in sandbox."
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "Repo .env (verified present per Plan 07-01 Gate 1)"
        notes: "Forwarded into os.environ at script entry per Plan 02-01 commit 5bc3f24 fix; lightrag closures read lazily."
    dashboard_config:
      - task: "Confirm OpenRouter monthly budget can absorb $15-35 (one-time)"
        location: "https://openrouter.ai/account (or equivalent provider dashboard)"
---

<objective>
Wipe the current 3-paper smoke Tier 4 graphml and rebuild over the full
79-paper MineRU cache, producing a Phase-9-frozen-doc-defensible
"≈80-paper multimodal corpus" claim. Capture a fresh `tier-4-graph-stats-{ts}.json`
provenance manifest beside the existing 2026-05-05 smoke manifest. Smoke-verify
the new graph passes the same gate the 3-paper smoke passed before authorizing
Plan 07-03's full sweep.

Purpose: Three concrete reasons this is its own plan. (a) Cost magnitude:
$15-35 / 15-25h is the largest line in v1.0; deserves an isolated cost-ack
checkpoint distinct from Plan 07-03's ~$1 sweep. (b) Sandbox boundary: the
ingest MUST run on host (Phase 2 Plan 02-02 evidence — vision-LLM URL handling
and MineRU parser-version probe both behave differently in sandbox). (c)
One-time-then-locked: Once Plan 07-02 commits the new graph-stats manifest,
do NOT rebuild between Plan 07-03 and Phase 9 freeze (Pitfall 3 — graphml
drift would invalidate Phase 9's manifest citation).

Output:
- `rag_anything_storage/tier-4-multimodal/` rebuilt over 79 papers (gitignored)
- `evaluation/results/diagnostics/tier-4-graph-stats-{NEW_TS}.json` committed
  alongside the existing 2026-05-05 smoke manifest (BOTH preserved)
- 5q smoke verdict PASS recorded in 07-02-SUMMARY.md
- ZERO source code changes; ZERO harness file edits

CRITICAL CONSTRAINT — RAW-LOCK: Forward-contract-locked modules
(pipeline.py / run.py / score.py / compare.py / freeze.py / smoke_gate.py)
remain byte-identical. Plan 07-02 only INVOKES the existing
`tier-4-multimodal/scripts/ingest_from_mineru.py` and `log_graph_stats.py`
helpers; it does NOT modify them. The library pins (lightrag-hku 1.4.15 /
raganything 1.2.10 / mineru 3.1.4) MUST NOT be bumped — the manifest
provenance for Phase 9 depends on these exact versions.

CRITICAL CONSTRAINT — HOST EXECUTION: Task 2 includes a sandbox-detection
guard. If the orchestrator is running in sandbox, this plan halts at Task 2
and asks the user to re-run on host. Plan 02-02 established this pattern.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-full-5-tier-rerun/07-RESEARCH.md
@.planning/phases/07-full-5-tier-rerun/07-01-SUMMARY.md
@.planning/phases/02-tier-4-graphml-regeneration/02-01-SUMMARY.md
@.planning/phases/02-tier-4-graphml-regeneration/02-02-SUMMARY.md
@.planning/phases/02-tier-4-graphml-regeneration/02-04-SUMMARY.md

<interfaces>
<!-- Helper scripts that this plan INVOKES (not edits). Locked from edits. -->

From tier-4-multimodal/scripts/ingest_from_mineru.py (RUN, do not edit):
  python tier-4-multimodal/scripts/ingest_from_mineru.py --reset --yes
  # --reset: shutil.rmtree rag_anything_storage/tier-4-multimodal/ before ingest
  #          (Pitfall 4 zombie-state guard from Plan 02-01)
  # --yes: skips cost-acknowledgement prompt
  # Reads tier-4-multimodal/output/<paper-id>/**/*_content_list.json (rglob)
  # Forwards os.environ["OPENROUTER_API_KEY"] (commit 5bc3f24 fix)
  # Per-paper exception isolation (Pitfall 1): partial failures continue
  # Vision-LLM 400 errors are non-fatal (~5-10% multimodal-item dropout per Plan 02-01)
  # Wall: ~21 min/paper measured at Plan 02-01 (3-paper) → 79 papers ≈ 15-25h
  # Cost: ~$15-35 projected (STATE.md 2× safety margin from $1.89/3-paper smoke)

From tier-4-multimodal/scripts/log_graph_stats.py (RUN, do not edit):
  python tier-4-multimodal/scripts/log_graph_stats.py
  # Reads rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml
  # Writes evaluation/results/diagnostics/tier-4-graph-stats-{TS}.json
  # Schema: GraphStats Pydantic model — graphml_node_count, graphml_edge_count,
  #         paper_ids[], library_versions{lightrag-hku, raganything, mineru, ...},
  #         timestamp, git_sha
  # Phase 9 reads this manifest via GraphStats(**json.load(f))

From evaluation/harness/smoke_gate.py (LOCKED):
  python -m evaluation.harness.smoke_gate --tier 4
  # Verdict PASS iff: populated/measurable >= 0.8 AND faithfulness/context_precision
  # non-NaN on populated rows.

Sandbox-detection heuristic (per Plan 02-02 SUMMARY):
  [[ -n "$CLAUDE_CODE_SANDBOX" ]] || [[ "$ALL_PROXY" == *socks5h* ]]
  # If true → in sandbox; ABORT (this plan must run on host).

Standard 5q smoke set (per Phase 1 D-04):
  --smoke-question-ids single-hop-001,single-hop-002,single-hop-003,multi-hop-001,multi-hop-002
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Cost-ack — authorize Plan 07-02 Tier 4 full rebuild ($15-35; ceiling $50)</name>
  <decision>Authorize the largest cost line in v1.0 scope</decision>
  <context>
    Plan 07-02 rebuilds Tier 4's LightRAG graph from the full 79-paper MineRU
    cache. This is the dominant cost in v1.0:

    - Estimate: $15-35 (STATE.md 2× safety margin extrapolation from
      Plan 02-01's 3-paper $1.89 smoke)
    - Hard ceiling: $50 (research over-runs trigger abort)
    - Wall time: 15-25 hours (off-hours run recommended)
    - One-time: subsequent reruns hit `kv_store_llm_response_cache.json` and
      pay only marginal cost on cache misses

    Why this is its own checkpoint (not bundled with Plan 07-03's $1 sweep
    cost-ack):
    - Different cost magnitude (15-35× larger than the sweep itself)
    - Different infrastructure (host shell required; sandbox refuses)
    - Different recovery semantics (mid-rebuild crash → re-run with `--reset`
      pays full cost again unless user manually preserves cache)

    Pre-conditions (must be true before approving):
    - Plan 07-01 verdict: 5/5 tier smokes PASS, Open Q1 verdict PASS
    - Repo .env present with OPENROUTER_API_KEY
    - 79 papers under tier-4-multimodal/output/ (verify with `ls | wc -l`)
    - You are running on HOST (not orchestrator sandbox)
    - OpenRouter monthly budget can absorb $15-35

    Failure modes during rebuild (research §"Failure Modes"):
    - Vision-LLM 400 (URL too long): ~5-10% multimodal-item dropout, NON-FATAL
    - OpenRouter rate-limit (429): per-paper retry; final paper count <79 acceptable down to ~70
    - Process crash mid-ingest: re-run with --reset (no cache preservation in v1.0)

    DO NOT bump library versions between Plan 07-02 (rebuild) and Phase 9
    (freeze) — the manifest provenance breaks.
  </context>
  <options>
    <option id="approve">
      <name>Approve Plan 07-02 spend ($15-35; ceiling $50)</name>
      <pros>
        - "≈80-paper multimodal corpus" claim becomes defensible
        - Phase 9 frozen-doc cites the new manifest
        - Plan 07-03 sweep can produce CAP-01-honest Tier 4 numbers
      </pros>
      <cons>
        - Real money: $15-35
        - Long wall: 15-25h (off-hours)
        - Cannot be re-tried cheaply (re-run = full re-spend without cache preservation)
      </cons>
    </option>
    <option id="defer">
      <name>Defer Plan 07-02 (do not authorize)</name>
      <pros>
        - Zero spend
      </pros>
      <cons>
        - Plan 07-03 cannot start (Tier 4 graph at 3-paper smoke is honest only for a 3-paper-corpus claim)
        - Phase 7 stuck at gate; CAP-01 stays open
      </cons>
    </option>
    <option id="downscope">
      <name>Down-scope to ≤30 papers (re-quote at $5-15)</name>
      <pros>
        - Lower cost
        - Faster wall (~5-8h)
      </pros>
      <cons>
        - Blog claim shrinks to "≈30-paper corpus" — weaker narrative
        - Future v1.1 reruns over 79 papers re-pay original cost anyway
        - Requires editing ingest_from_mineru.py to accept a paper-list filter (out of scope for this plan as written)
      </cons>
    </option>
  </options>
  <resume-signal>Type `approve` to authorize Plan 07-02 spend, `defer` to halt Phase 7, or `downscope` to surface a re-plan request.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HOST-MACHINE EXECUTION — Tier 4 full rebuild (15-25h, $15-35)</name>
  <what-built>
    Wipe `rag_anything_storage/tier-4-multimodal/` (the 3-paper smoke graph from
    Plan 02-01 is destroyed) and rebuild from the full 79-paper MineRU cache via
    `ingest_from_mineru.py --reset --yes`. After ingest, run `log_graph_stats.py`
    to write a Pydantic-typed `tier-4-graph-stats-{TS}.json` manifest under
    `evaluation/results/diagnostics/`.

    This task is NOT autonomous — it requires running on the user's host
    machine (NOT the orchestrator sandbox), and the wall time is 15-25 hours
    making it incompatible with a single Claude Code session. The standard
    pattern (per Plan 02-02 SUMMARY) is: orchestrator emits the command,
    pauses, user runs on host, user pastes verbatim stdout/stderr back to
    orchestrator (or attaches a transcript file).
  </what-built>
  <how-to-verify>
    EXACT COMMANDS TO RUN ON HOST (NOT in orchestrator sandbox):

    # Open a host terminal (NOT inside Claude Code sandbox).
    # cd to the repo root.
    cd /Users/patrykattc/work/git/rag-architecture-patterns

    # Activate the venv (project convention).
    source .venv/bin/activate

    # ============================================================
    # GUARD A — confirm host (not sandbox)
    # ============================================================
    if [[ -n "$CLAUDE_CODE_SANDBOX" ]] || [[ "$ALL_PROXY" == *socks5h* ]]; then
        echo "ABORT: detected sandbox env — must run on host"
        echo "  CLAUDE_CODE_SANDBOX=$CLAUDE_CODE_SANDBOX"
        echo "  ALL_PROXY=$ALL_PROXY"
        exit 1
    fi
    echo "OK: host shell"

    # ============================================================
    # GUARD B — confirm cache state (≥75 papers)
    # ============================================================
    PAPER_COUNT=$(ls tier-4-multimodal/output/ | wc -l)
    [[ "$PAPER_COUNT" -ge 75 ]] || \
        { echo "ABORT: cache has $PAPER_COUNT papers; need ≥75"; exit 1; }
    echo "OK: $PAPER_COUNT papers in MineRU cache"

    # ============================================================
    # GUARD C — confirm OPENROUTER_API_KEY present
    # ============================================================
    test -n "$OPENROUTER_API_KEY" || \
        { echo "ABORT: OPENROUTER_API_KEY not exported; check .env loading"; exit 1; }

    # ============================================================
    # GUARD D — confirm clean git state (recommended)
    # ============================================================
    if [[ -n "$(git status --porcelain)" ]]; then
        echo "WARN: git tree dirty — log_graph_stats.py records git_sha=HEAD; uncommitted changes are not in provenance"
        echo "      Recommend committing/stashing before proceeding."
        # Soft warn; do not abort.
    fi

    # ============================================================
    # MAIN — Rebuild
    # ============================================================
    echo "About to rebuild Tier 4 graph from $PAPER_COUNT papers."
    echo "Estimated wall: 15-25h. Estimated cost: \$15-35."
    echo "Recommended: run in tmux/screen/nohup so SSH disconnects don't kill it."
    echo "Logging stdout+stderr to tier-4-rebuild-\$(date -u +%Y%m%dT%H%M%SZ).log for"
    echo "post-mortem if needed."

    LOG=tier-4-rebuild-$(date -u +%Y%m%dT%H%M%SZ).log
    python tier-4-multimodal/scripts/ingest_from_mineru.py --reset --yes \
        2>&1 | tee "$LOG"
    INGEST_RC=${PIPESTATUS[0]}
    [[ "$INGEST_RC" -eq 0 ]] || \
        { echo "FAIL: ingest exited $INGEST_RC — see $LOG"; exit 1; }

    # ============================================================
    # POST — Sanity check
    # ============================================================
    test -f rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml || \
        { echo "FAIL: graphml not produced"; exit 1; }
    GRAPHML_SIZE=$(wc -c < rag_anything_storage/tier-4-multimodal/graph_chunk_entity_relation.graphml)
    echo "Graphml size: $GRAPHML_SIZE bytes"
    [[ "$GRAPHML_SIZE" -gt 1000000 ]] || \
        { echo "WARN: graphml suspiciously small ($GRAPHML_SIZE bytes); 3-paper smoke was ~3MB"; }

    echo "============================================================"
    echo "PASS: Tier 4 ingest complete. Proceed to Task 3 (log_graph_stats)."
    echo "============================================================"

    EXPECTED FAILURE MODES (per 07-RESEARCH.md "Failure Modes"):
    - Red `400 Invalid URL format` errors: NON-FATAL. Continue. Expected on
      ~5-10% of multimodal items (large base64 figures exceed OpenRouter URL limit).
    - 429 rate limits: lightrag's internal retry handles; per-paper failures
      are isolated. Final paper count <79 acceptable down to ~70.
    - Embedding worker timeouts (60s): auto-retry; observed working in
      Plan 02-01.
    - Process crash mid-ingest (ctrl-C, OS reboot): re-run from this Task 2
      `python ingest_from_mineru.py --reset --yes`. The `--reset` wipes
      partial state. Cost-ack approval covers full re-spend on crash recovery.

    PASTE THE FOLLOWING BACK TO ORCHESTRATOR after the host run completes:
    1. Final paper count (`grep "✓ Ingested paper" "$LOG" | wc -l`)
    2. Final wall time (timestamps from $LOG first/last lines)
    3. INGEST_RC value
    4. Graphml file size (post-ingest `wc -c`)
    5. Any FAILURE-summary lines from the log
  </how-to-verify>
  <resume-signal>Once host ingest completes, paste the 5 metrics above + the path to the rebuild log into the chat. Type `host-rebuild-complete` to continue. Type `host-rebuild-failed` if it errored — orchestrator will help triage.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write graph-stats provenance manifest + commit</name>
  <files>
    evaluation/results/diagnostics/tier-4-graph-stats-{NEW_TS}.json  (NEW; COMMITTED)
  </files>
  <action>
    Run `log_graph_stats.py` to read the freshly-rebuilt graphml and write a
    Pydantic-typed manifest under `evaluation/results/diagnostics/`. Commit
    the new manifest beside the existing
    `tier-4-graph-stats-2026-05-05T11_14_40Z.json` (the 3-paper smoke
    manifest from Plan 02-01) — BOTH manifests stay in the diagnostics
    directory as the historical record per 07-RESEARCH.md "Claude's Discretion".

    EXACT SEQUENCE:

    # Run from repo root with .venv active
    python tier-4-multimodal/scripts/log_graph_stats.py

    # Identify the new manifest (by mtime DESC, head -1)
    NEW_MANIFEST=$(ls -t evaluation/results/diagnostics/tier-4-graph-stats-*.json | head -1)
    echo "New manifest: $NEW_MANIFEST"

    # Sanity check: should have a substantially higher node/edge count than the smoke manifest
    NODE_COUNT=$(jq -r '.graphml_node_count' "$NEW_MANIFEST")
    EDGE_COUNT=$(jq -r '.graphml_edge_count' "$NEW_MANIFEST")
    echo "Nodes: $NODE_COUNT, Edges: $EDGE_COUNT"

    # Compare to the smoke baseline (2886 nodes / 7056 edges)
    [[ "$NODE_COUNT" -gt 10000 ]] || \
        { echo "WARN: node count $NODE_COUNT seems low for 79-paper rebuild — investigate before Task 5 smoke"; }

    # Verify library versions match pinned (Phase 9 cites these)
    LRAG=$(jq -r '.library_versions["lightrag-hku"] // .library_versions["lightrag_hku"] // "unknown"' "$NEW_MANIFEST")
    RAGANY=$(jq -r '.library_versions["raganything"] // "unknown"' "$NEW_MANIFEST")
    MINERU=$(jq -r '.library_versions["mineru"] // "unknown"' "$NEW_MANIFEST")
    echo "Library versions: lightrag-hku=$LRAG, raganything=$RAGANY, mineru=$MINERU"
    [[ "$LRAG" == "1.4.15" ]] || { echo "FAIL: lightrag-hku version drift ($LRAG vs pinned 1.4.15)"; exit 1; }
    [[ "$RAGANY" == "1.2.10" ]] || { echo "FAIL: raganything version drift ($RAGANY vs pinned 1.2.10)"; exit 1; }
    [[ "$MINERU" == "3.1.4" ]] || { echo "FAIL: mineru version drift ($MINERU vs pinned 3.1.4)"; exit 1; }

    # Confirm the smoke manifest is still on disk (do not delete)
    SMOKE_MANIFEST=evaluation/results/diagnostics/tier-4-graph-stats-2026-05-05T11_14_40Z.json
    test -f "$SMOKE_MANIFEST" || \
        { echo "WARN: smoke manifest missing — was it deleted? historical record broken"; }

    # Commit the new manifest (only the new file; smoke manifest is unchanged)
    git add "$NEW_MANIFEST"
    git status --short

    git commit -m "feat(07-02): tier-4 full-corpus rebuild — $NODE_COUNT nodes / $EDGE_COUNT edges from 79 papers

Replaces the 3-paper smoke graph (2886/7056) from Plan 02-01 with a full
79-paper rebuild. The smoke manifest at
evaluation/results/diagnostics/tier-4-graph-stats-2026-05-05T11_14_40Z.json
is preserved beside the new one as the historical record.

Library versions: lightrag-hku $LRAG / raganything $RAGANY / mineru $MINERU
(matching pinned).

Phase 9 frozen-doc cites this manifest as the version-of-record.
DO NOT rebuild between this commit and Phase 9 freeze (Pitfall 3 of 07-RESEARCH.md)."

    PITFALLS to avoid:
    - Pitfall 3 (graphml drift): once committed, do NOT rerun
      `ingest_from_mineru.py --reset --yes` between this task and Phase 9
      freeze. Phase 9's manifest citation depends on this exact graph.
    - Pitfall 5 (results-dir): log_graph_stats.py writes to
      `evaluation/results/diagnostics/` by default; do NOT pass alternate
      paths. The smoke manifest committed by Plan 02-01 lives there.
    - Pitfall 8 (stale globs): the `ls -t | head -1` pattern correctly picks
      the freshest manifest. Subsequent reruns of log_graph_stats.py would
      write more manifests; v1.0 only commits the FIRST full-rebuild manifest.
  </action>
  <verify>
    <automated>
      NEW_MANIFEST=$(ls -t evaluation/results/diagnostics/tier-4-graph-stats-*.json | head -1)
      test -f "$NEW_MANIFEST" || { echo "FAIL: no manifest"; exit 1; }
      # Library version pins
      LRAG=$(jq -r '.library_versions["lightrag-hku"] // .library_versions["lightrag_hku"] // "unknown"' "$NEW_MANIFEST")
      [[ "$LRAG" == "1.4.15" ]] || { echo "FAIL: lightrag-hku drift"; exit 1; }
      # Both manifests on disk
      test -f evaluation/results/diagnostics/tier-4-graph-stats-2026-05-05T11_14_40Z.json || \
          { echo "FAIL: smoke manifest missing"; exit 1; }
      # New manifest committed (not in working tree as untracked)
      git ls-files --error-unmatch "$NEW_MANIFEST" >/dev/null 2>&1 || \
          { echo "FAIL: new manifest not committed"; exit 1; }
      echo "PASS: Task 3 manifest written + committed"
    </automated>
  </verify>
  <done>
    `evaluation/results/diagnostics/tier-4-graph-stats-{NEW_TS}.json` exists with mtime within the rebuild window.
    Both 2026-05-05 smoke manifest AND new full-rebuild manifest sit in `diagnostics/`.
    Library versions in the new manifest match pinned values.
    New manifest is committed to git.
  </done>
</task>

<task type="auto">
  <name>Task 4: Smoke-verify the new full-corpus graph (5q smoke gate PASS)</name>
  <files>
    evaluation/results/queries/tier-4-{NEW_TS}.json  (~$0.01 spend; gitignored)
    evaluation/results/metrics/tier-4-{NEW_TS}.json  (gitignored)
  </files>
  <action>
    Re-run the same 5q smoke that Plan 02-04 used to PASS the gap closure.
    The smoke set is fixed (single-hop-001/002/003 + multi-hop-001/002).
    The pre-rebuild 3-paper smoke graph passed this gate (Plan 02-04 verdict);
    the post-rebuild 79-paper graph MUST also pass.

    EXACT SEQUENCE:

    # Capture against the new graph
    python tier-4-multimodal/scripts/eval_capture.py \
        --smoke-question-ids single-hop-001,single-hop-002,single-hop-003,multi-hop-001,multi-hop-002 \
        --yes
    # → evaluation/results/queries/tier-4-{TS}.json (5 records)
    # → evaluation/results/costs/tier-4-eval-{TS}.json

    # Score
    python -m evaluation.harness.score --tiers 4 --yes
    # → metrics/tier-4-{TS}.json + costs/ragas-judge-tier-4-{TS}.json

    # Gate
    python -m evaluation.harness.smoke_gate --tier 4
    # Must exit 0 with verdict=PASS

    Expected (per Plan 02-04 baseline + 07-RESEARCH.md "Tier 4 NaN target"):
    - 5/5 measurable, populated/measurable >= 0.8
    - faithfulness/context_precision non-NaN on populated rows

    If smoke FAILs:
    - DO NOT proceed to Plan 07-03
    - Investigate likely causes (per 07-RESEARCH.md Open Q3):
      - One of the 4 newly-ingested host papers (1909.01066, 2002.06177,
        2309.15217, 2410.05779) producing graph-quality regression
      - LLM variance ±10% per Plan 02-01 Pitfall 6
      - Vision-LLM 400 dropouts hitting smoke-set source papers
    - Surface the failing-row details to the user in 07-02-SUMMARY.md and
      pause for diagnosis before authorizing Plan 07-03.

    PITFALLS:
    - Pitfall 6: this task does NOT run pytest, so the live-deselect gap is
      moot here. But Task 5's regression check DOES run pytest — must use
      `-m 'not live'`.
  </action>
  <verify>
    <automated>
      python -m evaluation.harness.smoke_gate --tier 4 || \
          { echo "FAIL: post-rebuild smoke gate"; exit 1; }
      # Confirm the smoke files came from THIS rebuild's run, not a stale older one.
      # Strict timestamp check is hard without recording rebuild start time;
      # the smoke_gate exit-0 is the load-bearing assertion.
      echo "PASS: tier-4 post-rebuild 5q smoke verdict=PASS"
    </automated>
  </verify>
  <done>
    `python -m evaluation.harness.smoke_gate --tier 4` exits 0.
    Fresh tier-4 queries+metrics JSONs exist with mtime within Task 4's run window.
  </done>
</task>

<task type="auto">
  <name>Task 5: Forward-contract verification + offline regression + SUMMARY commit</name>
  <files>
    .planning/phases/07-full-5-tier-rerun/07-02-SUMMARY.md  (NEW; committed)
  </files>
  <action>
    Three-part proof that Plan 07-02 changed only runtime state, not source.

    PART A — Byte-identical guard on the 6 forward-contract-locked harness
    modules + the 3 tier-4 helper scripts that were INVOKED but not edited:

    DIFF=$(git diff HEAD~1 -- \
        evaluation/harness/pipeline.py \
        evaluation/harness/run.py \
        evaluation/harness/score.py \
        evaluation/harness/compare.py \
        evaluation/harness/freeze.py \
        evaluation/harness/smoke_gate.py \
        tier-4-multimodal/scripts/ingest_from_mineru.py \
        tier-4-multimodal/scripts/log_graph_stats.py \
        tier-4-multimodal/scripts/eval_capture.py | wc -c)
    [[ "$DIFF" -eq 0 ]] || \
        { echo "FAIL: source modified ($DIFF bytes since rebuild)"; exit 1; }
    echo "PASS: 9 source files byte-identical"
    # Note: HEAD~1 because Task 3 added a commit (the new manifest); we're
    # asserting the source files are byte-identical to the pre-rebuild state.

    PART B — Offline pytest regression check (must use `-m 'not live'`):

    pytest evaluation/tests/ -m 'not live' \
        --ignore=evaluation/tests/test_eval_adapters.py \
        --ignore=evaluation/tests/test_eval_smoke_live.py \
        -x -q

    Expected: ≥126 passed (Phase 6 baseline), no new tests added by this plan.

    PART C — Write 07-02-SUMMARY.md per template
    (.claude/get-shit-done/templates/summary.md):
    - Front-matter: phase, plan=02, status=complete, requirements=[CAP-01],
      AND `tier_4_full_rebuild_manifest: <path>` where <path> is the
      relative path of the new manifest committed in Task 3 (e.g.
      `evaluation/results/diagnostics/tier-4-graph-stats-2026-05-06T18_22_47Z.json`).
      This is a CROSS-PLAN DATA CONTRACT: 07-03 Task 2 reads this key as
      the single source of truth for "which manifest is the full-rebuild
      one" (avoids the mtime-ambiguity failure mode where the smoke
      manifest at 2026-05-05T11_14_40Z could be misidentified as freshest
      if its mtime was somehow touched after the rebuild). 07-03 Task 2
      has a fallback to mtime-freshest + filename guard if this key is
      missing, but the front-matter key is the preferred contract.
    - Cost incurred (from cost JSONs):
        actual_capture_cost=$(jq '.totals.usd' <freshest tier-4-eval cost JSON>)
        actual_judge_cost=$(jq '.totals.usd' <freshest ragas-judge-tier-4 cost JSON>)
        # Note: ingest_from_mineru.py spend is in lightrag's own cache —
        #       not in evaluation/results/costs/ — record from $LOG transcript
    - Wall time (from $LOG first/last timestamps)
    - Final paper count (from grep "✓ Ingested paper" in $LOG)
    - Graphml node/edge counts (from new manifest)
    - Library version snapshot (from new manifest)
    - Smoke gate verdict (PASS/FAIL with details)
    - Forward-contract verdict (0 bytes diff)
    - Offline regression baseline (count passed)
    - Pre-Plan-07-03 readiness statement: "Plan 07-03 may proceed."
      OR "Plan 07-03 BLOCKED — see smoke FAIL details above."

    Commit the SUMMARY (separate commit from the manifest commit so git log
    is readable: 1) feat: rebuild + manifest, 2) docs: phase-progress).

    Commit message format:
      docs(07-02): tier-4 full-rebuild SUMMARY — verdict={PASS|BLOCKED}, wall {N}h, cost ${N}, {M} nodes / {E} edges
  </action>
  <verify>
    <automated>
      # Forward-contract guard (since pre-Plan-07-02 baseline)
      DIFF=$(git diff HEAD~1 -- evaluation/harness/pipeline.py evaluation/harness/run.py evaluation/harness/score.py evaluation/harness/compare.py evaluation/harness/freeze.py evaluation/harness/smoke_gate.py tier-4-multimodal/scripts/ingest_from_mineru.py tier-4-multimodal/scripts/log_graph_stats.py tier-4-multimodal/scripts/eval_capture.py | wc -c)
      [[ "$DIFF" -eq 0 ]] || { echo "FAIL: source modified ($DIFF bytes)"; exit 1; }
      # Offline regression
      pytest evaluation/tests/ -m 'not live' --ignore=evaluation/tests/test_eval_adapters.py --ignore=evaluation/tests/test_eval_smoke_live.py -x -q || \
          { echo "FAIL: offline regression"; exit 1; }
      # SUMMARY committed
      test -f .planning/phases/07-full-5-tier-rerun/07-02-SUMMARY.md || { echo "FAIL: SUMMARY missing"; exit 1; }
      git ls-files --error-unmatch .planning/phases/07-full-5-tier-rerun/07-02-SUMMARY.md >/dev/null 2>&1 || \
          { echo "FAIL: SUMMARY not committed"; exit 1; }
      # Cross-plan data contract: SUMMARY front-matter must carry the manifest path key
      MANIFEST_KEY=$(awk '/^---$/{n++; next} n==1' .planning/phases/07-full-5-tier-rerun/07-02-SUMMARY.md \
          | grep -E '^tier_4_full_rebuild_manifest:' \
          | head -1 \
          | sed -E 's/^tier_4_full_rebuild_manifest:[[:space:]]*"?([^"]*)"?[[:space:]]*$/\1/')
      [[ -n "$MANIFEST_KEY" ]] || { echo "FAIL: SUMMARY front-matter missing tier_4_full_rebuild_manifest key (cross-plan contract for 07-03 Task 2)"; exit 1; }
      [[ -f "$MANIFEST_KEY" ]] || { echo "FAIL: tier_4_full_rebuild_manifest=$MANIFEST_KEY does not point to an existing file"; exit 1; }
      [[ "$MANIFEST_KEY" != *'2026-05-05T11_14_40Z'* ]] || { echo "FAIL: SUMMARY claims smoke manifest as full-rebuild manifest (cross-plan contract violated)"; exit 1; }
      echo "PASS: Task 5 (SUMMARY emits tier_4_full_rebuild_manifest=$MANIFEST_KEY)"
    </automated>
  </verify>
  <done>
    `git diff HEAD~1 -- <9 files> | wc -c` returns `0`.
    Offline pytest exits 0 with ≥126 passed.
    `07-02-SUMMARY.md` exists with the smoke gate verdict, carries the
    `tier_4_full_rebuild_manifest: <path>` key in its YAML front-matter
    (cross-plan data contract for 07-03 Task 2), and is committed.
  </done>
</task>

</tasks>

<verification>
Phase-7-Plan-02 acceptance:
1. New `tier-4-graph-stats-{TS}.json` manifest committed under `evaluation/results/diagnostics/`.
2. Both 2026-05-05 smoke manifest AND new full-rebuild manifest exist (historical record preserved).
3. Library versions in new manifest match pinned (lightrag-hku 1.4.15 / raganything 1.2.10 / mineru 3.1.4).
4. Post-rebuild 5q smoke gate verdict=PASS for tier 4.
5. Forward-contract guard: 0 bytes diff on 6 harness modules + 3 tier-4 scripts.
6. Offline pytest baseline ≥126 passed.
7. SUMMARY committed with rebuild metrics (paper count, wall time, cost, node/edge counts) and Plan 07-03 readiness statement.
8. SUMMARY front-matter carries `tier_4_full_rebuild_manifest: <path>` pointing to the new manifest committed in Task 3 (cross-plan data contract — 07-03 Task 2 reads this as the single source of truth for which manifest represents the full-corpus rebuild).
</verification>

<success_criteria>
Plan 07-02 is COMPLETE when:
- [x] Cost-ack approved (Task 1)
- [x] Tier 4 ingest completed on host (Task 2; user-confirmed via resume-signal)
- [x] Graph-stats manifest written + committed (Task 3)
- [x] Post-rebuild 5q smoke verdict=PASS (Task 4)
- [x] Forward-contract guard intact + offline regression baseline preserved (Task 5)
- [x] 07-02-SUMMARY.md committed
</success_criteria>

<output>
After completion, create `.planning/phases/07-full-5-tier-rerun/07-02-SUMMARY.md`.

Two commits expected from this plan:
1. `feat(07-02): tier-4 full-corpus rebuild — N nodes / E edges from 79 papers`
   (the new graph-stats manifest only)
2. `docs(07-02): tier-4 full-rebuild SUMMARY — verdict=PASS, wall Nh, cost $N, ...`
   (the SUMMARY only)

The kv_store_llm_response_cache.json + the rebuilt graphml itself are
gitignored runtime state; do not attempt to add them to the index.
</output>
